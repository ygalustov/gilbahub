# План реализации — оставшиеся дефекты Hoxton v6

Исходный аудит: `GAIP_Hub_Defect_Handover_Hoxton_v6_2026-08-16.md` (та же папка). Этот план покрывает каждый дефект из документа, который ещё не закрыт. Структура следует собственной последовательности клиента (раздел 12), а не номерам дефектов, с поправками там, где текущий код расходится с предпосылками аудита.

Ссылки на код в разделах по отдельным дефектам проверялись 07.09.2026, когда план писался. **Номера строк с тех пор сместились** — за неделю закрыто около сорока тикетов, — поэтому искать по имени функции, а не по номеру строки. Там, где доказательства аудита (только docx и скриншоты, без ссылки на код) не подтвердились проверкой, это сказано в самом разделе.

Цитаты клиента приведены **на языке оригинала (английском)** — намеренно, чтобы это оставались настоящие verbatim-цитаты, проверяемые по исходному документу, а не мой пересказ.

## Что изменилось с момента написания плана

**План написан 07.09.2026, состояние обновлено 12.09.2026.** С тех пор закрыты тикеты **GH-369…GH-409, GH-413…GH-417, GH-421…GH-428 и GH-430…GH-438** — около пятидесяти. Номеров GH-410, GH-411, GH-412 и GH-429 в changelog нет: первые три — работы, снятые решением пользователя (перебор фосфора при подборе, верхняя граница статуса азота, оценщик дозы), GH-429 занят правкой подписи в блоке выверки. Объединение движка требований и половины доставки завершено целиком; проведён аудит расчёта и подбора на одиннадцати сайтах (50 генераций плана, 14 сравнений с документом), его находки закрыты пакетом GH-413…GH-417. Полный отчёт аудита — `AUDIT-calculation-and-selection-RU.md`, план работ по нему — `PLAN-GH413-audit-followup-RU.md`.

**Где мы сейчас.** Продукт готовится к передаче клиенту **на тестирование**, не к релизу для пользователей. Единственное, что этому мешает, — потеря лабораторных проб при синхронизации (Вопрос 1); её этап 0 в работе. Для помощи клиенту на странице плана временно включён блок выверки расчёта (GH-425): он показывает, из чего сложилось каждое число, чтобы клиент мог указать на конкретную строку, а не на цифру в целом.

Ниже актуальная картина; разделы по отдельным дефектам ниже переписаны там, где реальность их обогнала.

Технический разбор каждого тикета — в `gilbahub/docs/instructions.md`, раздел Change log. Здесь только состояние дефектов и открытые вопросы; дублировать сюда описание изменений не нужно, иначе два текста разойдутся.

| Закрыто после плана | Как |
|---|---|
| **D06** — фосфор рекомендуется в уже насыщенную почву | Проверено вживую, не воспроизводится: потолок достаточности закрыл P как побочный эффект работы по D07. Настоящий пробел был в тестах — P у calendar-движка не был закреплён нигде. GH-374 |
| **D01** — устаревшая копия климата | Программа штампуется координатами, при расхождении кэш сбрасывается; плюс серверная очистка. GH-371 + доработка гонки между двумя эндпоинтами |
| **D31, баг единиц измерения** | ppm больше не складывается с кг/га без конверсии. GH-370, подтверждено на реальных ниже-пороговых данных |
| **D31, этап 1 объединения** | Общее чистое ядро расчёта вынесено рядом с двумя движками, паритет доказан на фикстурах. Само переключение движков — отдельный этап. GH-376 |
| **D31, объединение движков — ЗАКРЫТО** | Обе поверхности считают требования по нутриентам в одном ядре `nutrition-requirement-core.js`, входные данные собирает один модуль `nutrition-program-inputs.js`. Движок экспорта стал фасадом, собственная арифметика из него удалена; из календаря удалены мёртвые дубликаты. GH-383 (экспорт) + GH-384 (Plan) + GH-385…GH-389 (доводка). Осталось два этапа, не про арифметику требований: трафик (сохранение расписания и вывод уровня нагрузки) и помесячное распределение азота — см. `PLAN-D31-unify-engines.md`, Stage 3 и Stage 4 |
| **Решение 1** (тканевая коррекция) | Решено: коррекцию не строим, цифры не подгоняем друг под друга; вместо этого статус ткани показан в одной строке с числом. GH-369, формулировки поправлены в GH-373/375 |

**Сверх плана — найдено и починено по ходу:** расхождение выбора tissue-образца между Plan и экспортом (GH-372), рассогласование сортировки образцов между сервером и клиентом (GH-375), кэш программы не сбрасывался при смене вида травы и методологии (GH-377), дублирующееся событие смены сайта, из-за которого в экспорте пропадали почвенные данные (GH-378), регистрозависимое сравнение методологии, из-за которого AA-сайт считался по MLSN (GH-379), и потеря программы сайта при переключении сайтов в комбинированном экспорте (GH-385).

### Какой тикет какой дефект закрывает

Единственное, чего нет в changelog: привязка к дефектам клиентского аудита. Тикеты без дефекта в колонке найдены нами, а не клиентом.

| Тикет | Дефект аудита | Состояние |
|---|---|---|
| GH-360 → GH-362 → GH-367 | **D30** фильтр дистрибьютора не доходит до экспорта | закрыт |
| GH-361, GH-366, GH-369 | **D07a** нет gate по почве и ткани для K/P; **Решение 1** | закрыт |
| GH-371 + GH-377 | **D01** устаревшая копия климата | закрыт |
| GH-374 | **D06** фосфор в насыщенную почву | закрыт |
| GH-370 | **D31**, баг единиц ppm → кг/га | закрыт |
| GH-376 | **D31**, этап 1: общее ядро вынесено | закрыт |
| GH-381 | **D31**, база азота у двух движков разная | закрыт |
| GH-382 | **D31**, pH-лестница порога P для SLAN | закрыт |
| GH-383 … GH-389 | **D31**, объединение движков и общий контракт входов | закрыт |
| GH-372, GH-375, GH-378, GH-379 | — (найдено при проверке) | закрыт |
| GH-380, GH-390 | — (харнесс паритета и доступы) | инфраструктура |
| GH-391, GH-392 | — (потерянный азот в итоге доставки; лишний пересчёт единиц AA) | закрыт |
| GH-393 | — (три правила разрешения методологии) | не чинён: поставлены тесты-ловушки, решение отложено |
| GH-394 | **D31**, этап 3: трафик — сохранение расписания и вывод уровня | закрыт |
| GH-395 | — (MLSN предлагался на новозеландских сайтах) | закрыт |
| GH-396 | — (отчёт и план показывали одно разными словами и единицами) | закрыт |
| GH-397 | — (в отчёте цвет остался только на Balance и Status, как в плане) | закрыт |
| GH-398 | **D31**, этап 4: помесячное распределение сведено в общий модуль | закрыт — **D31 закрыт целиком** |

## Уже сделано — не перепланировать

| Дефект | Статус |
|---|---|
| **D06** вносится P при насыщенных почве и ткани | Закрыт: GH-374. Проверено вживую — P req = 0 и на Plan, и в экспорте; продукт с фосфором не планируется. Добавлено тестовое покрытие, которого не существовало. |
| **D01** расходящиеся значения GP | Закрыт: GH-371. Программа несёт штамп координат, при несовпадении не восстанавливается; серверная очистка при записи координат. Доработан отдельно: гонка между двумя эндпоинтами, которые оба меняют координаты. |
| **D31** UI и экспорт считают K/P по-разному | Закрыт для расчёта требований: баг единиц (GH-370), общее ядро вынесено (GH-376), обе поверхности на него переведены (GH-383 экспорт, GH-384 Plan), доводка GH-385…GH-389. Решение 11 принято пользователем 09.09.2026 в пользу варианта 1 (единый движок), а не согласования констант двух движков. Гейт паритета `tests/e2e/ui-vs-export-parity.test.js` — 15/15 на трёх сайтах с разными методологиями (AA, MLSN, SLAN). Не закрыто и вынесено отдельно: трафик и помесячное распределение азота. |
| **D30** фильтр дистрибьютора не доходит до экспорта | Закрыт: GH-360 (временная защита, впоследствии заменена) → GH-362 (пер-семпловое разрешение региона + фикс фолбэка `isNewZealand()`) → GH-367 (громкий отказ при нерезолвленном биндинге дистрибьютора + обнаружение координат-заглушек). Утверждения 14–16 закреплены тестами `tests/gh362-nz-region-detection-fallthrough.test.js` и `tests/gh367-d30-distributor-binding-fail-loud.test.js`. |
| **D07** регрессия K req 0↔100 | Закрыт ранее через GH-300/305/308/319 (проверка потолка достаточности добавлена в `computeProgram()` файла `nutrition-calendar.js`), проверено — регрессии против цифр E2/E3 аудита нет. Утверждение 18 (K req = 0 выше потолка) сегодня проходит. |
| **D07a** нет gate по почве/ткани для K/P | Закрыт через GH-361 (gate по tissue-ratio в `nutrition-calendar.js`) + GH-366 (проброс tissue-данных на страницу Plan, которую GH-361 сам по себе не достигал) + GH-368 (тот же gate добавлен в `nutrition-requirement-engine.js`, второй движок). **Остаётся один открытый агрономический вопрос — см. Решение 1 ниже.** |
| **D02** привязка климата | Закрыт в E3, проверено против норм NIWA 1991-2020 в v5.1 (RMSE 2.9). Изменений кода не требуется. |
| **D04** тихая невязка по N | Закрыт в E3 (204 против цели 200, больше не тихо). Изменений кода не требуется. |
| **D03** (частично) | Паттерн "громкого отказа" реализован для каталога продуктов (GH-362/367) и для основного пути климат/питание (`nutrition-calendar.js:1255-1258`, `climate-normals-service.js:130-146`). Общее правило "нигде не должно быть тихого фолбэка" применено **не везде** — в ходе этой проверки найдены ещё четыре места с тихим фолбэком, см. D03 ниже. |
| D11, D12, D37, D45 | Отозваны самим клиентом (v2/v5.1); изменений кода не требуется. Остаток D45 (хранить исходный документ, из которого взята ставка) включён в схемную работу D33, отмечено там. |
| D39 | Объединён с D40; отдельной записи ниже нет. |

## Поправки к собственным предпосылкам аудита

Два вывода поменяли статус в ходе проверки — вынесены сюда отдельно, потому что план, который планирует уже сделанную работу или работу, отменённую более поздним обоснованным решением, тратит деньги клиента впустую.

**Атрибуция FoliMAX в D30.** Собственная таблица доказательств аудита по D30 перечисляет "FoliMAX NRG-NK" и "FoliMAX N-Forcer-N" как утечку из "каталога AU", и сам аудит помечает эту атрибуцию как непроверенное воспоминание (см. "Flagged as unverified" в конце исходного документа). `assets/nz-fertiliser-products.js:961-1016` показывает оба продукта под этими же названиями, с `"distributor": "PGG Wrightson Turf"` и `"availability": "NZ National"` — то есть в этой кодовой базе они каталогизированы как NZ-линейка, а не AU. (Одноимённый "FoliMAX NRG-NK" также существует в `assets/au-fertiliser-products.js:2627` как AU-продукт, распространяемый Nuturf, — бренд продаётся разными дистрибьюторами в обеих странах, что, вероятно, и объясняет ошибку атрибуции у клиента.) Это не меняет статус D30 (уже закрыт по другим доказательствам — экспорт читал регион из устаревшего/дефолтного состояния DOM, а не реально протекал настоящим AU-only SKU), но собственную таблицу доказательств клиента не следует повторять как факт в дальнейшей переписке.

**Блок Base Saturation в D25.** Решение аудита просит восстановить весь блок Base Saturation (с Sodium над ним), потому что "удаление было неверным фиксом". Текущий код показывает, что блок **не был удалён** — он существует целиком, включая строку `Sodium (Na)` (`assets/word-export.js:11339-11356`), — но сейчас намеренно подавляется **именно для образцов методологии Ammonium Acetate** (`word-export.js:11337`, `suppressBaseSaturation = methodology === 'AMMONIUM_ACETATE'`), согласно более позднему, процитированному фиксу (`b35fix438`/`b35fix439`, блок комментариев в `word-export.js:11304-11336`), аргументирующему, что %BS/BCSR "не валидирован для газонной травы" (Kopittke & Menzies 2007, *SSSAJ* 71:259-265), и что Carrow, Waddington & Rieke (2001) — те же авторы, которых цитирует сам клиент — рекомендуют интерпретацию по уровню достаточности (SLAN/MLSN) вместо BCSR для газона. Лабораторный набор Hoxton — Ammonium Acetate (Hill Labs NZ), так что это подавление — ровно то, что видел читатель E3. Это тот же самый аргумент, который клиент сам, независимо, приводит в v5.2, убирая свою же цитату Carrow из D07 ("Carrow, Waddington & Rieke критикуют BCSR, а не выступают за него"). См. Решение 2 ниже — это не просто "восстановить", нужно решение клиента с учётом его же заявленного скепсиса к BCSR в другом месте этого документа.

---

## Порядок сборки 1 из 4 — D30 (сделано, см. выше)

## Порядок сборки 2 из 4 — кластер регрессии по K (D06 / D07 / D07a)

Клиент прямо просит рассматривать D06, D07 и D07a как одну единицу работы ("D06, D07 и D07a — одна агрономическая проблема с тремя проявлениями"). D07 и D07a сделаны; D06 — нет.

### D06 — ЗАКРЫТО GH-374. P рекомендуется вносить, хотя и почва, и ткань уже выше оптимума, S1, Tier A

> Olsen P soil 40 mg/L. The export's own Glossary states the typical adequate range for turf is 12 to 28 mg/L. Tissue P 0.62%, flagged above optimal by the export itself. P requirement nonetheless prints at 18.0 kg/ha in E3, and the Prebbles UI schedules MAP Tech in March.
>
> The irrigation source is a surface lake (Simpsons lake), so applied P has a direct pathway. This remains the single line item most likely to be picked up by a council environmental officer.
>
> **Fix shape unchanged:** removal-replacement logic gated by a sufficiency ceiling. See D07, which is the same fix and which has already been written once.

**Что код реально делает сегодня.** Проверка по потолку, которую просит клиент, **уже реализована, и уже универсальна по нутриентам, а не только для K**, в обоих движках:

- `assets/nutrition-calendar.js:1537-1542` — `['P', 'K', 'Ca', 'Mg', 'S'].forEach(nutrient => { if (range.max && soilPpm[nutrient] >= range.max) annualRequirements[nutrient] = 0; })`.
- `assets/nutrition-requirement-engine.js:387-401` — проверка потолка в AA-ветке внутри `calculateNutrientRequirement(nutrient, currentLevel, config)` написана универсально по аргументу `nutrient`, а не захардкожена под K.
- Диапазон P резолвится через тот же Olsen-калиброванный источник, что использует лабораторный набор Hoxton: `assets/hill-labs-sample-types.js:83` (`P: {min:20, max:30, unit:'mg/L', extractant:'Olsen'}` для семейства S277) с фолбэком на `assets/ammonium-acetate-methodology.js:83-93`. При Olsen P 40, `40 >= 30` (или `>= 28` по собственной цифре глоссария экспорта) — потолок срабатывает при любом источнике.

**Статус: похоже, уже исправлено как побочный эффект работы по D07, при условии живой проверки.** Собственная регрессионная фикстура клиента уже требует именно эту проверку (утверждение 19: "то же утверждение для P и S, симметрично"). Это **не следует переделывать заново**; это следует **проверить вживую** на фикстуре Hoxton, и если баг воспроизведётся — рассматривать как однострочную регрессию (скорее всего, `HillLabsSampleTypes.deriveCode()` не резолвит диапазон P для этой конкретной комбинации типа образца/вида и падает на неограниченный дефолт — то же "плавное деградирование до неограниченного поведения при отсутствии совпадения по сертификату", задокументированное в GH-300).

**Трудоёмкость:** только проверка (прогнать экспорт на фикстуре Hoxton, подтвердить P req = 0), если живая проверка не провалится — в этом случае это баг разрешения диапазона в `deriveCode()` того же рода, что GH-364 уже чинил для текстуры почвы. В любом случае добавить утверждение 19 в регрессионную фикстуру — оно пока не закреплено отдельным именованным тест-файлом, в отличие от утверждения 18 (файлы `tests/gh3xx-*` закрепляют потолок K; эквивалентного теста для P/S не найдено).

**Зависимости:** нет — проверить раньше всего, до D31 (который трогает те же два движка).

### D07 / D07a — см. "Уже сделано" выше. Один открытый вопрос остаётся, включён в Решение 1.

---

## Порядок сборки 3 из 4 — устаревшая копия климата (D01, D01a, D02a)

### D01 — ЗАКРЫТО GH-371. Расходящиеся значения GP на разных поверхностях в одном документе, S2, Tier A (был Tier B)

> **Reframed.** ... The operator changed the site coordinates between E2 and E3. The Monthly N Distribution picked the change up. The Monthly Schedule did not, and still renders the pre-correction series byte-identical to E2's. ... So the divergence is not two bindings reading different data, it is one binding plus a cached or persisted copy that survives a site-record write.
>
> **Proof that the UI shares the stale copy.** [таблица перенормировки за девять месяцев, UI совпадает с устаревшей серией с точностью до 0.3 кг]

**Что код реально делает сегодня.** Живой производитель данных правильно ключуется по координатам, и он не проблема: `assets/climate-normals-service.js:174-202` кэширует по `lat.toFixed(2)+','+lon.toFixed(2)` — смена координат даёт другой ключ кэша. Устаревшая копия — это **материализованный, сохранённый объект программы**, а не сам производитель климатических данных:

- `assets/nutrition-calendar.js:1786-1787` — каждый результат `computeProgram()` отправляется на сохранение через `persistSiteConfigPatch({ nutritionCalendarProgram: program, ... })`.
- `assets/site-config-persistence.js:543-551` — блок carry-forward в `snapshotConfig()` копирует `nutritionProgram`/`nutritionCalendarProgram` из существующего конфига вперёд при **каждом** сохранении, безусловно, без проверки, изменилась ли локация сайта с момента последнего расчёта программы.
- `assets/site-config-persistence.js:571-597` — `restoreConfig()` записывает этот блоб напрямую в `window.GAIP_NUTRITION_CALENDAR_PROGRAM` при каждой загрузке страницы.
- `assets/nutrition-calendar.js:2189-2256` — `restoreFromPersisted()` рендерит его через `this.renderResults()` (строка 2235) **без пересчёта против текущего климата** и вызывает `gaip:nutrition-calendar-generated`, как будто это свежий расчёт.
- Именно это событие слушает Prebbles UI (`assets/nutrition-prebble-integration.js:53-64`), поэтому он не может отличить свежую генерацию от устаревшего восстановления и заново сохраняет тот же устаревший результат (`nutrition-prebble-integration.js:371-380`).
- `assets/word-export.js:9724-9744` читает `window.GAIP_NUTRITION_PROGRAM` для Monthly Schedule; защищается от устаревания между *сайтами* (`progSiteId !== currentSiteId`), но не проверяет "посчитано до или после текущих координат".

**Проблема, точно.** Запись координат не инвалидирует ничего. В памяти это переживает мягкую перезагрузку (`nutrition-calendar.js:2205-2208`, `if (this.program) return`); в сохранённом виде переживает жёсткий рефреш. Это соответствует утверждению клиента 13 (запись координат должна инвалидировать каждую закэшированную климатическую серию) и утверждению 1 (все поверхности GP согласуются для данного месяца/семантики).

**Предложенные решения.**
1. **(Рекомендовано.) Проштамповать сохранённую программу координатами, на которых она посчитана, и инвалидировать при несовпадении.** В `computeProgram()` сохранять `{lat, lon}` (уже разрешены, дёшево) вместе с `program`. В `restoreFromPersisted()` и в шаге carry-forward `snapshotConfig()` сравнивать сохранённые координаты с текущими координатами сайта; при несовпадении — выбрасывать закэшированную программу и заставлять живой пересчёт (либо рендерить состояние "нет программы, пересчитайте", которое GH-245 уже построил для случая недоступности климата). Это инженерное решение с рабочим прецедентом уже в кодовой базе: `assets/eue-integration-bridge.js:1271-1284` делает ровно такой паттерн (`_lastClimate = null` + повторный запрос) на событие выбора площадки GSSH.
2. **Полностью отказаться от сохранения посчитанной программы и всегда пересчитывать заново от текущего климата при загрузке страницы.** Проще, но меняет поведение при загрузке (сетевой/вычислительный round-trip при каждом открытии страницы) и убирает оптимизацию, которая, возможно, существует не просто так (ограничение частоты обращений к climate API). Нужно подтверждение, что эта оптимизация не критична, прежде чем выбирать этот вариант вместо первого.
3. **Серверная инвалидация**: заставить `SiteController::update()` (эндпоинт, владеющий широтой/долготой, `app/app/Http/Controllers/SiteController.php` ~строка 162-197) очищать закэшированные поля программы в `SiteConfig` напрямую при смене координат, чтобы клиент вообще не получал устаревший блоб для восстановления. Дополнение к варианту 1, не замена — клиентская персистентность (localStorage, если есть) всё равно нуждалась бы в клиентской проверке.

Рекомендуется **1 + 3 вместе**: штамп-и-проверка на клиенте (быстро, ловит случай с localStorage/браузерной персистентностью) плюс серверная очистка на эндпоинте записи координат (ловит любого другого клиента, читающего устаревшую строку `SiteConfig` напрямую, например, будущий мобильный клиент или собственный поиск сайта в Word-экспорте).

**Трудоёмкость и радиус поражения.** Все четыре региональные интеграции по питанию используют идентичный паттерн `window.GAIP_NUTRITION_PROGRAM = program` + `persistSiteConfigPatch` (`nutrition-au-fertiliser-integration.js:402`, `nutrition-nz-fertiliser-integration.js:380`, `nutrition-uk-fertiliser-integration.js:860`, `nutrition-prebble-integration.js:378-380`) — штамп-и-проверку нужно внедрить один раз в общем слое персистентности (`site-config-persistence.js`), а не четыре раза. Также затрагивает `word-export.js:9727-9744` и эквивалентный блок в `word-export-combined.js`. Риск регрессии умеренный — это ровно тот тип изменения, который может внести регрессию "пересчёт при каждой загрузке", если сделать как вариант 2, или тонкую гонку, если сравнение координат использует устаревшее состояние в памяти; тестировать, утверждая, что (a) сохранённый блоб используется, когда координаты совпадают, (b) смена координат заставляет пересчёт, и все из Monthly Schedule / Monthly N Distribution / UI monthly programme меняются вместе, соответствуя утверждению 13.

**Тест:** новый `tests/ghNNN-d01-coordinate-invalidation.test.js` — задать координаты, сгенерировать, изменить координаты, утверждать, что `nutritionCalendarProgram` либо отсутствует, либо пересчитан при следующем чтении, и что числа Monthly Schedule и Monthly N Distribution совпадают до одного знака после запятой (утверждение 1).

### D01a — поверхности GP не указывают семантику своей температуры, S3, Tier A

> Four GP figures in one session, at 8.3, 9.4, 10.8 and monthly-normal temperatures. Each is right for its own input. None says which input it used.

**Что код реально делает сегодня.** Уже исправлено на `/analysis`: `assets/growth-light-analysis.js:896-909` и `:917-940` явно подписывают "8-Day Average GP" против "[Month]'s Normal GP" с указанием исходной температуры в тексте подсказки. **Не исправлено** на: карточке дашборда (`assets/dashboard-init.js:1256-1257`, печатает `todayGP + '%'` без температуры или семантики рядом с меткой "Today's"); блоке Word-экспорта "Climate & Growth Conditions" (`word-export.js:11386-11396`, печатает `Growth Potential (C3) 26%` без семантики); таблице Word-экспорта Monthly Schedule (`word-export.js:5940-5964`, колонка `GP%` не несёт пометки семантики, в отличие от таблицы Monthly N Distribution двумя разделами дальше).

**Предложенное решение (инженерное решение, низкий риск).** Переиспользовать точно такой же паттерн подписи, уже проверенный на `/analysis` (`growth-light-analysis.js`), на трёх оставшихся поверхностях: добавлять "Live (X°C)" / "[Month] normal (X°C)" перед каждой цифрой GP. Новой логики не требуется, просто подключить то же значение температуры, которое уже в области видимости в каждой точке вызова (подтверждено — `climateMetrics`/`GAIP_STATE.climate` уже читается рядом во всех трёх местах), в существующие строки меток.

**Трудоёмкость:** небольшая, три точки вызова, Tier A. **Тест:** расширить регрессионный тест D01, чтобы также утверждать, что каждая из четырёх поверхностей печатает тег семантики; визуально подтвердить на карточке дашборда, в блоке Climate Word-экспорта и в заголовке Monthly Schedule.

### D02a — провенанс климата печатается без координат или температур, S2, Tier A

> Neither prints the coordinates, the resolved grid cell, or the twelve monthly temperatures the series was built from.

**Что код реально делает сегодня.** Подтверждено присутствие во всех трёх местах, где рендерится строка: `word-export.js:7212-7226` ("Monthly temperatures: long-term climate normals for this site's coordinates (NASA POWER climatology), not live weather" — только метка источника), `assets/nutrition-summary-integration.js:1001-1008` (тот же пробел, всплывающая подсказка на экране), `assets/plan-ui.js:44-48` (запись в глоссарии, тот же пробел). Во всех трёх `monthlyTemps` и координаты уже в области видимости в точке вызова — это пробел в печати, а не проблема отсутствующих данных (соответствует утверждению 12).

**Предложенное решение.** Печатать разрешённые широту/долготу и двенадцать месячных температур рядом с существующей строкой провенанса, во всех трёх местах, переиспользуя объект `monthlyTemps`, уже разрешённый в области видимости. Чисто аддитивный текст, Tier A, изменений движка не требуется.

**Трудоёмкость и радиус поражения:** три файла, никакой общей зависимости кроме того, что те же переменные `monthlyTemps`/координаты уже доступны (подтверждено проверкой агента). **Тест:** утверждать, что секция Climate экспорта содержит координаты и 12-элементный вектор температур (утверждение 12: "месячная серия GP воспроизводима из напечатанного вектора месячных температур, и этот вектор присутствует в экспорте").

**Зависимости для этой сборки (D01/D01a/D02a):** блокирующих нет; сделать D01 первым, поскольку и температурные подписи D01a, и вывод температур D02a хотят то же самое "разрешённое, помеченное семантикой значение температуры", которое фикс инвалидации D01 сделает надёжным. Замечание клиента, что D27 и D36 частично разрешатся как побочный эффект D01 (не перемерять D27 до этого), верно по коду — оба читают из одного и того же устаревшего-или-свежего объекта программы.

---

## Пакет рендереров Tier A

Ниже соблюдается собственный порядок приоритетов клиента; D40 и D41 идут первыми по его инструкции ("вместе они дают всю ошибку отчётности по N").

### D41 — UI применяет гарантированный анализ к объёму без конвертации по плотности/SG, S2, Tier A

> The guaranteed analysis on a liquid fertiliser is % w/w. ... So volume must be multiplied by SG before the analysis is applied. The export does this. The UI does not.
>
> | | UI prints | Correct at SG 1.24 |
> |---|---|---|
> | 150 L/ha, (QR) row | 33.0 kg N | **40.9** |
> | 150 L/ha, (Balance) row | 33.0 kg N | **40.9** |
> | 300 L/ha combined | 66.0 kg N | **81.8** |

**Поправка к предпосылке аудита.** Решение аудита v5 утверждает, что экспорт корректно обрабатывает плотность и неверен только UI. **Это больше не соответствует текущему коду — ни одна из поверхностей не применяет конвертацию по плотности.** Поиск по всему репозиторию использования `.density` (`grep -n "\.density\b" assets/*.js`) даёт **ноль совпадений** где-либо, включая `word-export.js` и `word-export-combined.js`. Единственное поле `density`, существующее в каком-либо каталоге продуктов (`assets/prebbles-products.js:798`, `1.34 kg/L` на несвязанном продукте "XXTRA Liquid Iron"), — мёртвые данные, их никто не читает. У самого Ammos 22 поля `density` вообще нет (`prebbles-products.js:804-814`). Наивный расчёт N в UI подтверждён ровно в тех строках, которые цитирует аудит: `prebbles-products.js:1367-1368` (зимняя листовая подкормка: `nDelivered = rateLHa * (analysis.N/100)`, без плотности) и `prebbles-products.js:2204-2209` (строка "(Balance)": `rateLHa = nForThisApp / 0.22`, снова без члена плотности) — это точно воспроизводит цифру аудита 33.0 кг N (30 L/ha × 0.22 × ... ≈ 6.6/применение). Экспорт потребляет те же самые предвычисленные значения `totalKg`/`nutrients.N` из того же модуля продуктов — отдельного шага с учётом SG ниже по потоку тоже нет ни в одном из файлов экспорта.

**Проблема, точно.** Либо доказательства аудита по документам продуктов для пути экспорта (P1/P2, подразумеваемый SG 1.333/1.35) были проверены на более старой сборке, которая больше не соответствует текущему коду, либо цифры "экспорт применяет SG", которые наблюдал аудит, пришли из какого-то другого, с тех пор удалённого пути кода. В любом случае **фикс должен добавить плотность к обеим поверхностям, а не скопировать её с одной на другую** — это меняет форму фикса с "перенести правильную логику экспорта в UI" на "добавить конвертацию в общий код потребления продуктов, поскольку ни у экспорта, ни у текущего UI её нет". Соответствует утверждению 26 (Ammos 22 при 300 L/ha должен вернуть 81.8 кг N, а не 66.0) и утверждению 21 (продукты, указанные в объёме, печатают единицу и SG).

**Предложенные решения.**
1. **(Рекомендовано.) Добавить `density` (кг/л) в схему продукта и заполнить её для каждого жидкого продукта с этикетки, затем применить в единственной точке, где происходит конвертация L→kg.** Каждая этикетка Growth Products несёт вес на литр в одной и той же позиции (по собственной заметке клиента), так что это ввод данных, а не исследование, для этого бренда; каталогам других брендов нужно то же поле, взятое с их собственных этикеток (помечать, а не угадывать, любой продукт, для которого его не найти — то же правило "никаких выдуманных чисел", что и везде в этой кодовой базе). Конкретно для Ammos 22: `density: 1.24 // kg/L, этикетка P1`.
2. Вычислять подразумеваемый SG централизованно, один раз, из `analysis.N%` и задокументированного допущения %w/w-в-w/v, без поля на каждый продукт — отклонено: это ровно тот тип выдуманного коэффициента конвертации, который правило проекта запрещает, когда реальное, взятое с этикетки число уже предоставлено клиентом (P1/P2).

**Трудоёмкость и радиус поражения.** Добавление в схему (поле плотности, дёшево — см. примечание D33 об общей схеме ниже, это должно попасть в **тот же** проход миграции/ввода данных, что и D33, и сужение по вторичным нутриентам D44) + одна общая функция конвертации, используемая как калькуляторами ставок `prebbles-products.js`, так и любым эквивалентным файлом интеграции AU/UK/NZ, делающим ту же математику L→kg (`nutrition-au-fertiliser-integration.js`, `nutrition-nz-fertiliser-integration.js`, `nutrition-uk-fertiliser-integration.js` — подтвердить, что у каждого есть эквивалентная наивная конвертация, прежде чем оценивать полный радиус поражения). Риск регрессии: **каждая существующая цифра N/K/S по жидким продуктам в каждом экспорте изменится** после применения плотности (один только Ammos сдвигается примерно на 24% по N) — это должно выпускаться с явного согласия клиента, что это изменение значения, а не просто фикс отображения, поскольку это меняет то, что Fertiliser Purchasing Summary сообщает поставщику о поставке. **Тест:** утверждение 26 точно как формулирует клиент; также добавить проверку в стиле паритета UI-и-экспорт утверждения 20 специально для доставленного N из объёма.

### D40 — окна переноса оставляют январь и ноябрь непокрытыми, S1, Tier A

> The carry-forward logic works in three-month blocks and drops months that fall outside a block.

**Что код реально делает сегодня.** Подтверждено точным механизмом, а не только симптомом: `assets/prebbles-products.js:1688` строит фиксированный 12-слотовый массив `coveredMonths` (Jan=0…Dec=11); `prebbles-products.js:1882`, `windowMonths = 2` — это **захардкоженное** упреждение "консервативно: считаем, что продукт покрывает ~2 месяца" — текущий месяц + 2 = трёхмесячный блок клиента, применяемый одинаково независимо от того, что реальные данные о продолжительности высвобождения (`releaseWeeks`, см. D33) говорят для выбранного продукта. Циклы отметки покрытия (`prebbles-products.js:1883`, `:1939`, `:1973`) все ограничены `< 12` / `< monthlyData.length` **без модуля/переноса через границу года** (`grep -n "% 12"` по файлу даёт ноль совпадений) — окно покрытия декабрьского применения никогда не может дотянуться назад до января. Пробел ноября следует из того же фиксированного 2-месячного потолка, начинающегося там, где приземлилось последнее полное (непокрытое) применение.

**Предложенные решения.**
1. **(Рекомендовано, соответствует форме фикса клиента.) Заменить фиксированное 2-месячное упреждение окном, управляемым балансом массы**: продлевать покрытие, пока собственный распавшийся остаток N/K продукта (уже вычисляется помесячно, см. логику `decayFactor` в `prebbles-products.js:1941-1946`) не упадёт ниже порога, который сейчас запускает `isCovered` (`monthData.N * 0.3`, строка 1700), вместо остановки на фиксированном числе месяцев. Это естественным образом продлевается в январь от декабрьского применения, когда распавшийся остаток всё ещё значим, без изобретения новой константы.
2. **Проще, соответствует альтернативному варианту клиента: выдавать явный флаг непокрытого месяца** везде, где месяц имеет ненулевую потребность и ни запланированного применения, ни записи `coveredBy`, и проваливать экспорт (или громко флагировать на экране) вместо тихого рендера прочерка. Ниже инженерный риск, чем вариант 1, не устраняет базовый пробел, только проявляет его — собственная формулировка клиента трактует их как альтернативы ("либо... либо"), а не последовательные шаги, так что это одно удовлетворило бы букву формы фикса, но оставило бы реально доставленный N заниженным.

Рекомендуется сделать **оба**: вариант 1 закрывает реальный пробел (это то, что двигает цифру 59%-от-дефицита, вычисленную клиентом), вариант 2 — страховочная сетка на случай остаточного пробела после реальной модели баланса массы (всегда найдётся какая-то комбинация сайт/продукт, которую не удастся покрыть полностью). Это прямо собственная инструкция клиента: "Исправление этого закрывает примерно 60% дефицита N, не трогая выбор продукта" — чисто фикс окна расписания, без изменения того, какие продукты выбираются.

**Трудоёмкость и радиус поражения:** ограничено блоком переноса `prebbles-products.js` (~строки 1688-1990); подтвердить, дублируют ли независимо файлы региональных интеграций AU/UK/NZ ту же логику фиксированного окна (их собственный код покрытия следует проверить на `windowMonths`, прежде чем считать это специфичным только для Prebbles). Риск регрессии: меняет ровно то, какие месяцы получают "Covered by" против свежего применения, что меняет количество применений продукта в Monthly Schedule — нужен тест паритета (утверждение 25) как жёсткий гейт.

**Тест:** утверждение 25 точно ("каждый месяц с ненулевой потребностью по нутриенту имеет либо запланированный продукт, либо явную ссылку covered-by; непокрытый месяц с потребностью — это провал"). Перезапустить против скорректированных цифр плотности D41, как только оба лягут, поскольку D41 меняет, сколько N реально доставило покрывающее применение.

### D42 — Lo Biuret Urea меняет единицу измерения между расписанием (L) и сводкой (kg), S2, Tier A

> Fifty of one becomes fifty of the other with no conversion. It is also a soluble solid being scheduled in litres in the first place.

**Что код реально делает сегодня.** Корневая причина прослежена полностью: запись продукта корректно смоделирована как твёрдое вещество (`prebbles-products.js:1029-1042`, `form: 'soluble'`, `maxRateKgHa: 150`, без `maxRateLHa`), но живёт внутри массива `PrebbleProducts.liquid` (отдельной категории растворимых твёрдых веществ в пайплайне выбора не существует). `selectFoliarNitrogen()` (`prebbles-products.js:3147-3150`) явно рассматривает `form === 'soluble'` как жидкого кандидата; математика ставки (`prebbles-products.js:3311`) безусловно основана на литрах (`rateLHa = N / nPct`), подставляя общий потолок 30 L/ha для жидкостей (`prebbles-products.js:3267/3314`, `product.maxRateLHa || 30`) вместо реального твёрдого потолка продукта 150 kg/ha, поскольку `maxRateLHa` отсутствует; возвращаемый объект затем принудительно устанавливает `form: 'liquid'` независимо от исходной записи (`prebbles-products.js:~3408-3410`). Далее по потоку Annual Product Summary отдельно повторно определяет "растворимость" эвристикой по **строке названия** (`nutrition-prebble-integration.js:1024`, `data.name?.includes('soluble')`) и переключает *метку* на kg/ha без применения какой-либо конвертации к *числу* — так что одна и та же сырая цифра (например, 50) печатается и под меткой "L/ha" в расписании, и под меткой "kg/ha" в сводке.

**Предложенное решение.** Дать пайплайну выбора настоящую ветку `form === 'soluble'` (отдельную от `'liquid'`), которая несёт `rateKgHa` вместо `rateLHa`, использует `maxRateKgHa` как свой потолок и рендерится с единицей `kg/ha` на каждой поверхности с момента вычисления — устраняя необходимость в нижестоящей эвристике по строке названия целиком. Это та же корневая причина, которую клиент определяет как общую с D08 ("единица — это метаданные отображения, а не часть значения") — фикс здесь должен следовать тому же принципу "единица движется вместе со значением", а не патчиться поверхность за поверхностью.

**Трудоёмкость и радиус поражения:** функции выбора жидкостей в `prebbles-products.js` (`selectFoliarNitrogen` и что бы ещё ни итерировало `PrebbleProducts.liquid`, предполагая `form==='liquid'`⇒литры) плюс эвристика `nutrition-prebble-integration.js:1024` (удалить, как только вышестоящий фикс сделает её ненужной). Проверить, разделяет ли какой-либо другой растворимый-твёрдый продукт, кроме Lo Biuret Urea, эту неверную классификацию, прежде чем оценивать масштаб (grep `form: 'soluble'` по всем файлам каталогов продуктов). Низкий риск регрессии, если ограничиться растворимыми — жидкости не трогаются.

**Тест:** новое утверждение фикстуры — цифра растворимого продукта в Monthly Schedule и цифра в Annual Product Summary должны быть численно согласованы под заявленной единицей на обеих поверхностях (расширить собственную фикстуру аудита, ни один номер утверждения сейчас конкретно это не покрывает — добавить его).

### D44 — сере даётся требование и строка сверки, должна быть только информационной, S2, Tier A

> **Agronomic ruling, recorded at blocking question 7:** sulphur is a secondary element and must not be a factor in product selection over N, P and K. Delivered S is informational only.
>
> **Fix shape.** Remove S from the requirement and reconciliation path. Render delivered S as an informational figure with no requirement, no balance and no status.

**Что код реально делает сегодня.** Подтверждено — не устранено, S всё ещё первоклассный везде, кроме одной таблицы UI:
- `nutrition-requirement-engine.js:649`, `nutrition-calendar.js` (пять отдельных циклов `['P','K','Ca','Mg','S']` на строках 1405/1446/1468/1477/1537) — S проходит через ту же самую логику потолка/дефицита/статуса, что P и K, без спецобработки.
- `word-export.js:11496-11499` — печатает `Sulphur (S)` с красно-зелёным цветом статуса точно как первоклассный нутриент.
- `word-export-combined.js:3220` — задокументированная раскладка колонок таблицы ANR даёт S собственную колонку `req`, идентичную по типу P и K.
- **Единственное** место, где S реально исключён — это живая таблица сверки на странице Plan (`nutrition-prebble-integration.js:898`, `['N','P','K'].map(...)` — S не включён), но это выглядит как случайный побочный эффект явного решения GH-361/368 ограничить область только P/K, а не намеренная реализация этого решения (ни один коммит не ссылается на D44; подтверждено grep по `docs/instructions.md`).

**Предложенное решение.** Агрономическое решение не требуется — клиент уже вынес его (заблокированный вопрос 7, закрыт). Это чистая инженерия: убрать S из пяти циклов `nutrient` в обоих движках (либо отключить его поля статуса/цвета/требования, продолжая вычислять и отображать доставленную цифру), и убрать выделенную колонку `req`/статуса S из обоих путей Word-экспорта, заменив её на простую информационную строку "Sulphur delivered: X kg/ha" без цвета, без баланса, без бейджа статуса. **Не распространять** это на работу по характеристике высвобождения (D33) — клиент явно указывает, что 40% медленного высвобождения N у Ammos остаётся в области действия там; это решение касается S как *нутриента*, а не характеристик высвобождения продукта.

**Трудоёмкость и радиус поражения:** затрагивает те же пять циклов в обоих движках, которые также трогают D06/D31/фикс единичного бага — **делать это после работы по согласованию движков D31, не до**, чтобы реструктуризация циклов произошла только один раз. Рендеринг колонки S в обоих файлах Word-экспорта. Низкий риск регрессии — только убирает поля/колонки, не меняет арифметику никакого другого нутриента.

**Тест:** утверждение 27 точно ("никакая строка требования или баланса по сере не выдаётся; доставленная S рендерится только как информационная цифра").

### D26 — продукт в сводке без месяца в расписании, S2, Tier A

> Every month is either an explicit application... a "covered by" carry-forward, or a dash. The client procures 152 kg/ha of a product the programme never applies.

**Что код реально делает сегодня.** Две таблицы **не** гарантированно остаются синхронизированными — есть как минимум один живой, выпущенный путь кода, который может добавить продукт в Annual Product Summary без соответствующей строки в Monthly Schedule: `word-export.js:9894-9970` (слияние K-сверки) добавляет записи spot-K продуктов в `_productUsageA` (питающий таблицы Summary/Purchasing) **безусловно** (`word-export.js:9923-9925`), в то время как соответствующее размещение в Monthly Schedule условно зависит от разрешения индекса месяца и может тихо провалиться, только логируя в консоль (`word-export.js:9949-9971`, `_missedCountA` отслеживается, но никогда не проявляется в самом документе). Это демонстрирует ровно тот класс сбоя, который описывает D26, живьём в текущем коде, независимо от того, какой именно механизм породил конкретный экземпляр FoliMAX NRG-NK из аудита (который, скорее всего, идёт через путь фолбэка каталога D30, уже в значительной степени закрытый GH-362/367 — стоит перепроверить на свежем экспорте, прежде чем предполагать, что этот конкретный экземпляр продукта всё ещё воспроизводится).

**Предложенное решение.** По собственной форме фикса клиента: утверждать (и обеспечивать, не только тестировать), что набор продуктов в Annual Product Summary равен набору продуктов хотя бы с одним запланированным применением — проваливать экспорт вместо тихого рендера фантомной строки закупки. Применить это как инвариантную проверку после сборки, прямо перед финализацией документа, покрывая и путь слияния K-сверки, и общий путь сбора продуктов, чтобы любой будущий путь кода, добавляющий продукт только в сводку, ловился тем же способом.

**Трудоёмкость и радиус поражения:** сама инвариантная проверка небольшая и централизованная (одна функция, запускается один раз на экспорт); сложнее решить, что означает "провал" для экспорта платящего клиента — рекомендуется убирать несовпадающий продукт из таблиц Summary/Purchasing и логировать предупреждение в собственную секцию Data Quality экспорта (видимую клиенту, не только в консоли), а не прерывать весь документ, поскольку это вопрос полноты данных, а не корректности того, что показано. Это инженерное решение о режиме отказа, не агрономическое.

**Тест:** утверждение 17 точно. Перезапустить как против конкретно пути слияния K-сверки, так и против общего мультипродуктового экспорта.

### D23 — Hub Version рендерится как `unknown`, S2, Tier A

> Populate it from whatever the SaaS build identifies itself by, a release tag or commit reference, and treat a failure to resolve it as a blocking export error rather than a printed string.

**Что код реально делает сегодня, и почему это всегда "unknown".** `export-metadata.js:320` читает `global.GAIP_HUB_VERSION || 'unknown'`. `GAIP_HUB_VERSION`, согласно собственным комментариям кода, инжектировался через **`wp_add_inline_script`** (`assets/gilba-hub-v2.js:1570-1580`) — механизм, существующий только в WordPress-плагине. Этот репозиторий — Laravel/SaaS-сборка; ничто в `app/resources/views/layouts/db-shell.blade.php` (текущая оболочка страницы, подтверждено прочтением — она инжектирует `window.GAIP_HUB_CONFIG` с `nonce`/`userId`/`activeSiteId`/и т.д., но без поля версии) или где-либо ещё в `app/` не устанавливает эту глобальную переменную. Так что "unknown", которое видит клиент, — это не баг в логике фолбэка (фолбэк намеренно видим, согласно собственному комментарию `b35fix315`, конкретно чтобы отсутствующая инъекция была громкой, а не тихо неверной) — дело в том, что точка инъекции вообще никогда не была перенесена из WordPress в SaaS-оболочку. Подтверждено: ни `APP_VERSION`, ни `GIT_COMMIT`, ни эквивалентный идентификатор сборки нигде не существует сегодня в `app/config`, `.env.example` или `composer.json`.

**Предложенные решения.**
1. **(Рекомендовано.) Инжектировать SHA деплоящегося git-коммита.** Этот репозиторий деплоится покоммитно через GitHub (согласно собственному CLAUDE.md проекта), так что `git rev-parse --short HEAD` во время деплоя/загрузки — естественный, всегда доступный идентификатор с нулевой неоднозначностью о том, какая сборка произвела данный экспорт — `gilbahub` сам является git-репозиторием с реальной историей коммитов (`HEAD` сейчас `c3b5b57c`), так что SHA доступен в среде деплоя уже сегодня, просто никогда не проявляется. Закэшировать его (файл или конфиг, разрешается один раз во время деплоя, не вызывается шеллом на каждый запрос) и инжектировать через существующий блок `window.GAIP_HUB_CONFIG` в `db-shell.blade.php`, затем читать его в `GAIP_HUB_VERSION` так же, как это делала WordPress-сборка. Рабочий прецедент именно для такого серверно-разрешённого, blade-инжектируемого значения уже существует в этом приложении: `app/app/Providers/AppServiceProvider.php:33-37` делится URL легаси-ассета со строкой запроса `?v=filemtime(...)` для сброса кэша через `View::share()` — тот же механизм (поделиться серверно-разрешённым значением с каждым view) может нести SHA коммита.
2. Строка семантической версии из `composer.json`/выделенного файла `VERSION`, вручную обновляемая при каждом релизе — более читаема человеком в разговоре поддержки ("v2.3.1" вместо SHA), но требует, чтобы кто-то не забыл её обновить; SHA бесплатен и не может разойтись.

Рекомендуется **1**, с SHA, отрендеренным рядом с коротким человекочитаемым лейблом, если нужно ("Hub build a1b2c3d"), и половина формы фикса "блокирующая ошибка экспорта" реализована как: если версию не удаётся разрешить во время экспорта, экспорт должен явно сказать об этом в секции метаданных (не печатать "unknown" молча) — более сильное требование клиента (полностью прервать экспорт) — продуктовое решение, достойное подтверждения, поскольку оно превращает пробел в метаданных в клиентский сбой экспорта; рекомендуется видимая-и-честная версия (печатать "Hub build: not available" как отдельную, чётко помеченную строку) вместо жёсткого прерывания, если только клиент конкретно не хочет блокировать экспорты по этому поводу.

**Трудоёмкость и радиус поражения:** изменение пайплайна деплоя (разрешить и раскрыть SHA — согласовать с тем, кто владеет конфигом GitHub Actions/деплоя) плюс однострочная blade-инъекция, а существующий путь чтения `export-metadata.js` уже работает, как только глобальная переменная установлена. Низкий риск регрессии. **Тест:** утверждение 10 ("экспорт прерывается, а не печатает Hub Version: unknown") — решить точную формулировку режима отказа с клиентом перед написанием этого теста, поскольку "прерывается" и "печатает честную строку недоступности" — разное поведение.

### D14 — бор помечен как низкий и одновременно объявлен достаточным, S2, Tier A

> 1. `Issues identified: B low (0.2 ppm).`
> 2. `All measured nutrients are at or above Ammonium Acetate (Hill Labs) guideline levels.`

**Что код реально делает сегодня.** Корневая причина точнее собственной гипотезы клиента (он подозревал строку по умолчанию для пустого набора решений; на самом деле это захардкоженное несовпадение списка нутриентов): строка "Issues identified" (`word-export.js:~797`) идёт из цикла по микронутриентам, покрывающего Fe/Mn/Zn/Cu/**B**. Строка "All measured nutrients are at or above..." (`word-export.js:1169`) генерируется **отдельной функцией**, чей массив `nutrients` (`word-export.js:1106-1112`) захардкожен только на `[P, K, Ca, Mg, S]` — бор не является членом этого массива, так что эта функция структурно не способна знать, что B низкий; она выдаёт "all adequate" исключительно потому, что её собственный ограниченный список оказался чистым. Третий экземпляр того же бага существует в собственной строке "no standalone amendments required" таблицы поправок (`word-export.js:3617`, зависит от `decisions.length === 0` из ещё одного цикла только по `[P,K,Ca,Mg,S]`, `word-export.js:3491-3497`) — тоже слепа к B.

**Предложенное решение.** Расширить список входных данных всех трёх функций статуса нутриентов, включив в него микронутриенты (B, Fe, Mn, Zn, Cu), уже отслеживаемые циклом "Issues identified", либо — дешевле и с меньшим риском — заставить две функции, выдающие строки "all adequate"/"no amendments required", сверяться с уже построенным массивом `issues[]` (из цикла по микронутриентам) перед срабатыванием, и подавлять/квалифицировать своё собственное утверждение, когда этот массив непуст. Второй вариант меньше: он не требует повторного вывода статуса дефицита микронутриентов внутри функции, которая никогда не была построена, чтобы его хранить, он просто делает эту функцию осведомлённой о том, что другая часть того же документа уже нашла проблему.

**Трудоёмкость и радиус поражения:** две-три точки вызова в `word-export.js`, все производящие повествовательные строки, читающие те же самые исходные данные почвы, уже вычисленные в другом месте той же области видимости функции — новых вычислений не требуется, только перекрёстная ссылка на уже существующий массив. Низкий риск регрессии.

**Тест:** фикстура с микронутриентом ниже порога и всеми макро-адекватными не должна печатать предложение "all measured nutrients are at or above guideline levels" (или должна квалифицировать его: "all measured **macro**nutrients...").

### D47 — третий баннер-противоречие ("Conditions favorable"), S2, Tier A

> Directly beneath a Traffic score of 100 out of 100, the page prints "Conditions favorable. Good window for maintenance activities."

**Что код реально делает сегодня.** `assets/stress-trajectory-engine-pure.js:1015` (внутри `generateRecommendation()`, той же функции построения рекомендаций, из которой берётся траектория стресса, используемая D24): `rec.message = 'Conditions favorable...'` срабатывает в финальном `else` цепочки, зависящей исключительно от `peakScore` против `CONFIG.thresholds.caution` — той же самой композитной оценки стресса, из которой строится окно вмешательства D24, с тем же самым отсутствием какого-либо входа GP/DLI. Рендерится отдельной функцией, `assets/stress-analysis.js:196-232` (`renderVerdict(stress, traj)`), которая читает `traj.summary.recommendation.message` — подтверждая, что этот баннер, D46's Component Breakdown (`stress-analysis.js:436-467`) и список окон D24 — три различные функции рендеринга в одном файле, все потребляющие один объект `traj` без арбитража между собой или против GP/DLI.

**Предложенное решение.** Поскольку этот баннер управляется тем же `peakScore`/композитным объектом, что D24, исправление композита D24 (включение gate по GP/DLI — см. D24 ниже) следует реализовать как одно изменение в общей функции, и этот баннер, окно D24, и разбивка D46 будут все читать скорректированное, ограниченное gate значение — не как три отдельных патча. Это ровно собственная заметка клиента: "Исправление D24 без gate также для этого баннера и карточки Wear Resistance оставляет две из четырёх поверхностей всё ещё противоречащими пятой". Оформить фикс этого баннера как **часть тикета D24**, а не отдельного, даже несмотря на то, что в аудите он перечислен отдельно.

**Трудоёмкость и радиус поражения:** ноль дополнительных файлов сверх того, что D24 уже трогает (`stress-trajectory-engine-pure.js`), при условии, что D24 исправлен на уровне общего композита/gate, а не только в конкретной функции выбора окна. **Тест:** покрывается собственным тестом D24, как только общий gate существует — утвердить, что сообщение "Conditions favorable" не может сработать, когда GP ниже порога культивации.

### D16 — заявленный механизм (высокожелезистая ирригация) не измерен, S2, Tier A

> The Fe:Mn narrative cites "high-Fe irrigation water" as a driver. Fe is not in the water panel for this site.

**Что код реально делает сегодня.** `assets/mulders-interaction-checker.js:161` — фраза представляет собой константную строку времени компиляции, вшитую в поле `detail` объекта правила `MULDER_INTERACTIONS`, безусловно склеенную с каждым флагом Fe:Mn независимо от того, измерялось ли Fe воды вообще. Ни один путь кода в `checkMuldersInteractions()` или его вызывающих функциях вообще не читает `data.water.Fe` — данные водной панели даже не передаются в эту функцию.

**Предложенное решение.** Сделать фразу "irrigation water" условной от фактического присутствия `data.water.Fe`, либо переписать её, чтобы называть потенциальные драйверы обобщённо ("высокое Fe в почве относительно Mn, будь то из материнской породы, ирригации или предыдущего применения Fe"), а не утверждать один конкретный неизмеренный путь. Второй вариант дешевле (не требует новой прокладки данных в проверщик взаимодействий) и соответствует собственной предложенной формулировке клиента ("переписать, чтобы называть потенциальные драйверы условно").

**Трудоёмкость:** одна правка строки плюс, если выбран первый (gate-based) вариант, прокладка `data.water` в точку вызова `checkMuldersInteractions()`. Рекомендуется вариант "переписать обобщённо" — меньший радиус поражения, тот же результат для читателя. **Тест:** подтвердить, что повествование на сайте без измерения Fe воды не называет ирригацию конкретно.

### D17 — повествование о корнеобитаемом слое игнорирует тип образца; нет предупреждения об OM на песке, S2, Tier A

> `CEC is moderate (5.9 meq/100g), typical of sandy loam rootzones`. The selected lab suite is TURF Ryegrass, Sand (S277).
>
> organic matter is 3.7% on a sand profile. That is an OM accumulation flag carrying surface sealing, infiltration loss and black layer risk... it appears nowhere in the report.

**Что код реально делает сегодня.** Подтверждено по обеим половинам. Повествование о CEC (`word-export.js:5493-5514`) ветвится **только** по числовому значению CEC (`CEC < 5` / `< 12` / `< 25` / иначе) без какой-либо ссылки на `soilData.texture`, тип образца или методологию где-либо в функции — 5.9 попадает в корзину `< 12` и всегда печатает "typical of sandy loam rootzones" независимо от того, какой лабораторный набор произвёл образец. Отдельно, OM захватывается (`data.soil.OM`, `word-export.js:7954-7999`) и печатается как голое значение ("Organic Matter 3.7%", `word-export.js:10854`) и питает путь **выбора** поправок `HIGH_OM` (`word-export.js:12015`, выбор продукта, связанного с pH/известью) — но нигде нет повествования, предупреждающего о последствиях накопления OM в виде поверхностного запечатывания/потери инфильтрации/риска чёрного слоя конкретно на песчаной конструкции; `grep` по "black layer", "surface sealing", "infiltration" по `word-export.js` ничего не даёт.

**Предложенное решение.** (a) Привязать формулировку повествования о CEC к фактическому полю текстуры/конструкции образца (уже разрешено в другом месте согласно работе по порядку разрешения текстуры GH-364 — переиспользовать это разрешённое значение вместо повторного вывода) вместо вывода типа почвы только из числа CEC. (b) Добавить новый триггер повествования: OM выше некоторого порога (нужна агрономическая цифра от клиента — не изобретать её, по правилу проекта) **конкретно на образце, классифицированном как песок**, печатает предостережение о поверхностном запечатывании/инфильтрации/чёрном слое, которое описывает клиент. Эта вторая половина нуждается в пороговом числе клиента до реализации — см. Решение 3.

**Трудоёмкость и радиус поражения:** (a) небольшой, самодостаточный фикс в той же функции, переиспользующий уже разрешённое значение текстуры — низкий риск. (b) новая ветка повествования, аддитивная, не меняет существующее поведение — низкий риск, но заблокирована на пороге клиента. **Тест:** (a) утвердить, что повествование о CEC называет фактический тип образца на образце S277/песок; (b) как только будет порог, утвердить, что предупреждение об OM срабатывает при/выше числа клиента на песчаном образце и не срабатывает на суглинке/глине при том же %OM.

### D13a — вердикт по ткани не имеет названного набора/вида/цитаты/диапазона, S2, Tier A

> The Tissue Analysis section renders `Phosphorus, Manganese, Zinc are above optimal levels` with no named set, no species qualifier, no citation, and no visible band.

**Что код реально делает сегодня.** `generateTissueNarrative()` (`word-export.js:3887-3969`) строит своё предложение исключительно из измеренного значения против `{lo,hi}` — ни название набора, ни вид, ни цитата никогда не попадают в её вывод. Метаданные существуют рядом, но не читаются: `data.tissue.rangeSpecies` установлен (`word-export.js:~8549`, например "Perennial Ryegrass") непосредственно перед **третьей захардкоженной копией** тех же диапазонов ткани PR, уже задублированных в `tissue-engine.js` и `soil-nutrition-analysis.js` — но `generateTissueNarrative` никогда не читает `rangeSpecies`. У почвенной стороны уже есть паттерн для копирования: таблица AA "Interpretation Framework Comparison" (`word-export.js:10884-10954`) печатает название фреймворка, экстрагент, философию и строку цитаты на каждую строку. **Самой цитаты тоже пока не существует**: в `citation-registry.js` нет записи для Spencer's *Nutrition of Sports Turf in Australia* (CSIRO/Landlinks) — источника диапазонов ткани PR и проверок соотношений N:S/N:K (D13c) — подтверждено grep по всему объекту `CITATIONS`.

**Предложенное решение.** (1) Добавить запись `spencer-nutrition-sports-turf` в `citation-registry.js` (тот же паттерн реестра, который всё равно нужно исправить для D21 ниже — оформить последовательно). (2) Прокинуть уже вычисленный `rangeSpecies` и диапазон `{lo,hi}` по каждому нутриенту (уже в `tissueData.ranges`) в строку вывода `generateTissueNarrative`, плюс новую цитату. Новых вычислений не требуется — каждый кусок данных, который нужен, уже в области видимости или в одном свойстве от неё.

**Трудоёмкость и радиус поражения:** одна функция (`generateTissueNarrative`), одна запись реестра. Напрямую открывает утверждение 7a ("каждый флагированный элемент рендерит название своего набора, колонку вида, диапазон и цитату"). **Оформить последовательно с D21** (перепрокладка реестра цитат), поскольку оба трогают паттерн потребления реестра — сделать добавление в реестр один раз, потребляемое обоими.

### D13c — проверки соотношений N:S и N:K не реализованы, S3, Tier A

> N:S should sit between 10:1 and 18:1. Above 20:1 signals potential sulphur deficiency. N:K should sit between 1.2 and 2.2. Neither is computed in any of the three exports.

**Что код реально делает сегодня.** Подтверждено отсутствие — единственное связанное совпадение где-либо в файлах ткани — статичная декоративная строка подсказки (`assets/tissue-progressive-disclosure.js:532`, "S correlates with N; ratio typically 15:1 N:S"), которая не является вычисляемой проверкой и даже не совпадает с диапазоном 10:1–18:1, который цитирует клиент. Ссылки на N:K нигде не существует.

**Предложенное решение.** Добавить два вычисляемых поля (`N/S`, `N/K`) рядом с существующими значениями ткани, с диапазонами, заявленными клиентом (10:1–18:1 N:S, порог 20:1; 1.2–2.2 N:K), взятыми из той же ссылки Spencer, которую нужно зарегистрировать для D13a. Чисто аддитивно — новая небольшая функция, потребляемая тем же повествованием о ткани, которое расширяет D13a, так что реализовать оба за один проход.

**Трудоёмкость:** небольшая, одна функция плюс две константы (взятые из собственной цитаты клиента, не изобретённые). **Тест:** утверждение 7b точно — N:S вычисляется как 9.7:1 с пометкой ниже порога; N:K вычисляется как 1.50 без пометки, на фикстуре Hoxton.

### D08 — Ca:Mg в ppm, K:Mg в meq, напечатаны рядом, S3, Tier A

> `Ca:Mg Ratio 6.2:1 (Optimal)`, this is 803/129, a mass ratio. `K:Mg Ratio (meq) 0.48 (Optimal)`, this is 0.51/1.06, correctly meq.

**Что код реально делает сегодня.** Подтверждено присутствие, и необычно дёшево починить: `word-export.js:4713`, `var CaMg = Ca / Mg;` — сырой ppm. Двумя строками ниже, `word-export.js:4714`, `var KMg = (K / 39.1) / (Mg / 12.15);` — корректно в meq-эквиваленте. И двумя строками *выше*, `word-export.js:4700-4701` уже вычисляет `Ca_meq = Ca / 200` и `Mg_meq = Mg / 121.5` для расчёта base saturation в той же функции — **значения meq, которые нужны CaMg, уже находятся в области видимости, неиспользуемые, в точке, где вычисляется CaMg.** Вторая, независимая реализация существует в `assets/soil-nutrition-analysis.js:881-898` (`calcRatiosFromNutrients()`, питающая страницу Soil всякий раз, когда серверный кэш пуст — подтверждено, что бэкенд всегда оставляет `ratios: null`, `app/app/Http/Controllers/SampleAnalysisController.php:111`, так что этот JS-путь и есть живой) — и она **хуже** версии экспорта: и Ca:Mg, *и* K:Mg там сырые ppm-соотношения, вообще без конвертации в meq.

**Предложенное решение.** В `word-export.js` изменить строку 4713, чтобы использовать уже вычисленные `Ca_meq`/`Mg_meq` (или применить те же самые делители по атомному весу, что уже использует соседняя строка `KMg`, для согласованности внутри функции). В `soil-nutrition-analysis.js` добавить ту же конвертацию в meq к **обоим** Ca:Mg и K:Mg в `calcRatiosFromNutrients()` — K:Mg страницы Soil сейчас тоже неверен, чего аудит не заметил (он смотрел только на экспорт). Изменения порогового диапазона на стороне экспорта не требуется — уже используемый вердикт-диапазон "Ideal 3–10" (`word-export.js:4734-4744`) по-прежнему корректно применяется к соотношению на основе meq — на этой фикстуре значение meq (3.78) всё равно попадает в "Optimal" под существующим диапазоном, просто ближе к нижней границе, так что на самом Hoxton вердикт не меняется — только на почвах близко к границе, согласно собственному рабочему примеру провала клиента.

**Трудоёмкость и радиус поражения:** однострочный арифметический фикс в `word-export.js` (исправление соседней логики двумя строками дальше), небольшой фикс функции в `soil-nutrition-analysis.js`. Очень низкий риск регрессии — это строго коррекция соотношения масс в соотношение meq с использованием данных, уже разрешённых в той же области видимости; новых входных данных нет. **Тест:** утверждение 6 точно (Ca:Mg из meq утверждает 3.78, не 6.2) плюс собственный рабочий пример провала клиента (Ca 1000/Mg 300 ppm: масса 3.3:1 "Optimal" → meq 2.02:1, должно флагироваться) как вторая фикстура.

### D10 — колонки Annual Product Summary без заголовка единиц, S3, Tier A

> Columns headed N, P, K, S with no unit.

**Что код реально делает сегодня.** Уже исправлено в комбинированном экспорте (`word-export-combined.js:4237-4239`, `'N kg'`, `'P kg'`, `'K kg'` — единицы присутствуют), но всё ещё открыто в экспорте одиночного образца: `word-export.js:5882-5895`, ячейки заголовка буквально `text: 'N'`, `'P'`, `'K'`, `'S'` без суффикса единиц.

**Предложенное решение.** Соответствовать конвенции комбинированного экспорта точно (добавить `' (kg/ha)'`, соответствуя подтверждённой семантике D04) к четырём ячейкам заголовка в `word-export.js`.

**Трудоёмкость:** тривиальная, четыре строковых литерала `TextRun`. **Тест:** подтвердить текст заголовка на экспорте одиночного образца.

### D09 (половина раскрытия информации) — конвертация L→kg не раскрывается, S2, Tier A

> Print the unit the product is sold in and state the SG used wherever a mass figure is derived from a volume. Do not silently change the unit between the schedule and the procurement table.

Это оставшаяся половина D09 после того, как его арифметическая половина была включена в D41 выше. **Оформить строго после D41** — раскрывать нечего, пока D41 не добавит реальное поле `density`; раскрывать число, которого ещё не существует, невозможно. Как только D41 приземлится, добавить "at SG 1.24 kg/L" (или разрешённое значение по продукту) рядом с каждой массовой цифрой, выведенной из объёмной, и в Monthly Schedule, и в Purchasing Summary. **Трудоёмкость:** небольшая, только форматирование строк, как только D41 предоставит значение. **Тест:** утверждение 21.

### D43 — один продукт рендерится как две строки закупки ("(QR)"/"(Balance)"), S4, Tier A

> The (QR) and (Balance) suffixes are internal selection-pathway labels reaching client output, same class as crownRust (D29).

**Что код реально делает сегодня.** Подтверждён точный механизм: `prebbles-products.js:2211-2234`, проход балансировки N создаёт вторую запись с ключом `ammos.id + '-BAL'` и буквально устанавливает `name: ammos.name + ' (Balance)'` прямо в строку названия продукта; `nutrition-prebble-integration.js:1037-1039` отдельно добавляет `' (QR)'` к названию базовой записи всякий раз, когда `release === 'standard'` (что для Ammos является дефолтом, по D33 ниже, поскольку у него нет установленного поля `release`).

**Предложенное решение.** Объединить две записи в одну строку закупки по ID продукта перед рендером Purchasing Summary — суммировать их количества, полностью убрать суффикс пути из отображаемого названия (сохранить его внутренне как поле тега/для отладки, не конкатенировать в строку названия, которую видит клиент). Это тот же класс "внутренний идентификатор достигает клиентского вывода", что и D29's `crownRust`; исправить оба по одному принципу: внутренние метки живут в поле `_pathway`/`_releaseTag`, отображаемые названия идут из чистого `product.name`, никогда не мутируемого логикой пути выбора.

**Трудоёмкость и радиус поражения:** шаг слияния по ID — небольшая агрегация, добавленная непосредственно перед построением Purchasing Summary в `word-export.js`/`word-export-combined.js` и эквивалентной таблице UI в `nutrition-prebble-integration.js`; лежащее в основе двухзаписное вычисление (которое существует по реальной причине — отслеживание QR против Balance отдельно во время планирования) **не** нужно менять, только финальное отображение схлопывает их. Низкий риск — чисто слияние на уровне представления, Monthly Schedule (который уже корректно показывает 8 применений на 300 L согласно аудиту) не затронут. **Тест:** утвердить, что Purchasing Summary содержит ровно одну строку на физический ID продукта.

### D18 — осиротевшая секция Soil Amendment Recommendations, S4, Tier A

> Renders a heading, a rationale paragraph... No table, because no amendments were generated. Suppress the whole section when the decision set is empty, including the product-specific note.
>
> **Aggravated in E3:** the Annual Nutrient Requirements note now reads "cation deficit-correction recommendations appear in the Soil Amendment table above." There is no such table.

**Что код реально делает сегодня.** Корневая причина закреплена точно: `word-export.js:10143`, `data.amendment.hasData = true` устанавливается **безусловно** всякий раз, когда анализ поправок запускается успешно — независимо от того, произвёл ли он реальное решение по Ca/Mg (`assets/soil-tissue-integration.js:755-800`'s `recommendSoilAmendments()` возвращает `{Ca: null, Mg: null, ...}`, когда ни один не дефицитен — ровно случай Hoxton). Гейт секции в `word-export.js:11980`, `if (data.amendment && data.amendment.hasData)`, поэтому открывается на "анализ запустился", а не на "есть что показать". Внутри блоки по каждому продукту корректно защищены (`word-export.js:12144-12151`, `if (data.amendment.Ca && data.amendment.Ca.primaryProduct)`), так что сама таблица корректно подавляется — но заголовок, вступительный абзац обоснования (`word-export.js:11982-11995`) и примечание о раскладе/дисклеймере kieserite (`word-export.js:12170-12179`) **не** защищены на существование какого-либо решения, только тем же блоком `hasData`, так что они рендерятся независимо. Перекрёстная ссылка, которую цитирует клиент ("...appear in the Soil Amendment table above"), тоже подтверждена как живая и безусловная: `word-export.js:11454-11461`, часть подписи методологии AA, утверждает существование таблицы без проверки `data.amendment.hasData` или количества решений вообще.

**Предложенное решение.** Изменить гейт секции с `data.amendment.hasData` на "хотя бы один из `data.amendment.Ca`/`.Mg`/(любой другой нутриент, который это когда-либо покрывает) не null" — то есть гейт на существование решений, а не на то, что анализ запустился. Это одно изменение чинит заголовок, вступление и примечание-дисклеймер вместе, поскольку все они внутри одного и того же блока `if`. Отдельно, сделать предложение перекрёстной ссылки в подписи AA условным от той же проверки "существует ли какое-либо решение по поправкам", поскольку это совсем другой блок `if` (подпись находится внутри секции Annual Nutrient Requirements, а не в секции Amendment) — ей нужна собственная защита, читающая тот же самый флаг.

**Трудоёмкость и радиус поражения:** два условия в `word-export.js`, оба читающие булево значение, которое следует вычислить один раз (например, `data.amendment.hasDecisions = !!(data.amendment.Ca || data.amendment.Mg)`, установленное рядом с существующим `hasData` на строке 10143-10147) и потребляемое обоими гейтами. Низкий риск регрессии — только подавляет текст, который раньше всегда показывался; численных изменений нет. **Тест:** утверждение 8 точно ("ноль решений по поправкам подавляет всю секцию Soil Amendment Recommendations, включая примечания и любую перекрёстную ссылку на неё").

### D19 / D20 — глоссарий и список источников не отфильтрованы под то, что реально в документе, S4, Tier A

> Full-length definitions render for ESP, sodicity, gypsum, muriate of potash, SARadj and soil structure [D19]. Bermudagrass shade tolerance... bermudagrass ET... three salinity papers carried into a perennial ryegrass report [D20].

**Что код реально делает сегодня.** Оба подтверждены как полностью статичные блоки. `generateGlossary()` (`word-export.js:5109-~5170`) не принимает аргументов и возвращает захардкоженный список терминов безусловно; вызывается без какой-либо фильтрации в `word-export.js:13904`. Блок References (`word-export.js:13833-13870`) — захардкоженный массив из 22 источников, включающий работы по bermudagrass и статьи о солёности, которые называет клиент, печатается в каждом экспорте независимо от вида/EC/SAR. Полезная находка для фикса: записи `CITATIONS` в `citation-registry.js` **уже несут** поля метаданных `validatedFor`, `climateZone` и `topic` во многих записях (например, `mlsn-guidelines.validatedFor`, `harivandi-1992.validatedFor`) — инфраструктура для фильтрации уже существует в собственной схеме реестра, просто блок References в `word-export.js` — это отдельный локальный массив, который никогда не читает реестр вообще.

**Предложенное решение.** (1) Для блока References: мигрировать захардкоженный массив на чтение из `citation-registry.js`, и фильтровать каждую запись по тому, совпадают ли её метаданные `topic`/вид/`climateZone` с тем, что реально присутствует в этом документе (вид, диапазон EC/SAR, используемая методология) — схема реестра уже это поддерживает, это прокладка, а не новый дизайн. (2) Для глоссария: передавать набор терминов, реально упомянутых в других местах отрендеренного документа (названия методологий, использованные продукты поправок, реально напечатанные термины качества воды) в `generateGlossary()` и фильтровать её статичный список против этого набора, вместо того чтобы всегда печатать всё.

**Трудоёмкость и радиус поражения:** (1) зависит от того, что перепрокладка реестра D21 приземлится первой (тот же реестр, тот же паттерн потребления) — оформить последовательно. (2) самодостаточна в рамках `word-export.js`, но нуждается в определённом наборе "терминов в использовании", построенном из тех же данных, из которых остальная часть документа уже рендерится (сбора новых данных не требуется, только сбор существующих решений о рендере в набор по мере построения документа, затем фильтрация глоссария в конце). Умеренная трудоёмкость для (2), поскольку затрагивает порядок рендера (глоссарий должен строиться после всего остального, что решает, какие термины "в использовании"). **Тест:** утверждение 9 ("блоки глоссария и списка источников не содержат ни одного термина или цитаты, отсутствующей в отрендеренном теле документа").

### D21 — MLSN и SLAN цитируются непоследовательно на разных поверхностях, S4, Tier A

> Establish one canonical citation per framework in a single citation registry and have every surface read from it.

**Что код реально делает сегодня.** `citation-registry.js` существует и содержит запись MLSN (`CITATIONS['mlsn-guidelines']`, Woods/Stowell/Gelernter 2014) — но **вообще без записи SLAN**. Из поверхностей, печатающих цитаты MLSN/SLAN, только одна взята из реестра: блок "Methodology References" в `export-metadata.js` (`export-metadata.js:523-553`), который корректно печатает `"PACE Turf MLSN (2014)"` из реестра. Каждая другая поверхность захардкоживает собственную копию, независимо: глоссарий (`word-export.js:5130`, без имён авторов, атрибутируется "the Asian Turfgrass Center" — противоречит собственной атрибуции реестра PACE Turf/Woods-Stowell-Gelernter); секция References (`word-export.js:13835`, "Woods, Stowell & Gelernter (2014)... PACE Turf" — совпадает по году с реестром, но это независимая копия; `word-export.js:13837`, "Kreuser, W.C. (2015)... University of Nebraska-Lincoln" для SLAN — эквивалента в реестре, с которым можно было бы сверить, не существует); таблица сравнения фреймворков (`word-export.js:10939-10953`, "Woods et al. **2016** PeerJ Preprints..." — другой год, чем на каждой другой поверхности — и "Carrow et al. (2004) GCM 72(1):194-198" для SLAN); и комментарий в коде движка (`nutrition-requirement-engine.js:39`, ещё одна форма). **Пять разных форм цитаты MLSN на 2014/2016, две формы SLAN, ноль записей SLAN в реестре.**

**Предложенное решение.** (1) Добавить запись `slan-guidelines` в `citation-registry.js` (Kreuser 2015, соответствующую той из двух форм, которую клиент подтвердит как каноническую; отметить, что у самого MLSN есть реальная неоднозначность 2014-против-2016 в исходной литературе, которую собственный документ клиента не разрешает, поскольку Woods et al. опубликовали и руководство PACE Turf 2014, и статью PeerJ Preprints 2016 — подтвердить у клиента, какая из них имеется в виду как "та самая" цитата MLSN, или обе легитимно цитируемы для разных утверждений — см. Решение 4). (2) Перепрокладать глоссарий, секцию References и таблицу сравнения фреймворков, чтобы они вызывали `GilbaCitationRegistry.getCitation()`/`formatCitations()` вместо собственных захардкоженных литералов.

**Трудоёмкость и радиус поражения:** добавление в реестр небольшое; перепрокладка затрагивает три функции рендера в `word-export.js` плюс чистку комментария движка (косметическая, без изменения поведения). Оформить последовательно с D13a (тот же реестр, нужна запись Spencer, добавляемая одновременно) и D19/D20 (секция References потребляет тот же реестр, как только он перепроложен). **Тест:** утверждение 9 частично покрывает это (никакая цитата не отсутствует в теле документа); добавить прямую проверку, что все четыре поверхности разрешаются в единую сохранённую форму реестра и для MLSN, и для SLAN.

### D22 — Dew Prediction ограничивает доверие на экспорте, исключающем погоду, S3, Tier A

> Header states the export "captures soil chemistry and recommendations only." Footer reports "Confidence capped by Dew Prediction (50%)."

**Что код реально делает сегодня.** `harvestEngineConfidence()` (`export-metadata.js:105-125`) итерирует `Object.keys(computed)` из `GaipOrchestrator.getComputed()`/`GAIP_STATE.computed` — **глобального, на всю сессию** мешка результатов, не ограниченного тем, что конкретный тип экспорта реально рендерит. Если Dew Prediction случайно запустился раньше в браузерной сессии (например, пользователь посещал дашборд), его значение доверия сидит в `computed.dew` и складывается в общую оценку доверия каждого последующего экспорта независимо от того, включает ли этот экспорт содержимое dew/погоды вообще.

**Предложенное решение.** Правильно ограниченный список, который для этого нужен, уже существует в том же файле: `detectEnginesUsed(data)` (`export-metadata.js:52-76`) уже строит список движков конкретно для этого экспорта из собственного объекта `data` экспорта (`if (data.dew) engines.push('dew-prediction-engine')` и т.д.) — просто он никогда не сверяется с `harvestEngineConfidence()`, которая вместо этого независимо сканирует весь мешок `computed` на всю сессию. Передать результат `detectEnginesUsed(data)` в `harvestEngineConfidence()` и отфильтровать `Object.keys(computed)` против него перед вычислением `lowest`/`average`. Это соединение двух уже корректных частей кода вместе, а не написание новой логики ограничения области.

**Трудоёмкость:** небольшая, изменение сигнатуры одной функции плюс обновление двух точек её вызова, чтобы передавать собственный список модулей экспорта. Низкий риск регрессии — это только *убирает* движки из расчёта доверия, которых там быть не должно; оно не может ухудшить оценку легитимно включённого движка. **Тест:** подтвердить, что подвал Data Quality экспорта только-по-почве не содержит ссылки на Dew Prediction, когда dew никогда не вычислялся для этого экспорта, даже если вычисление dew существует где-то ещё в состоянии сессии.

### D25 — Base Saturation / отображение Na почвы, S2, Tier A (см. "Поправки к предпосылкам" выше)

**Что код реально делает сегодня** — уже изложено полностью выше. Два действительно отдельных пробела остаются независимо от решения по Base Saturation:

1. **Na почвы никогда не проявляется на простой панели химии почвы, ни в одной методологии.** Массив `nutrients` в `assets/hub-tissue-v3.js` (~строка 2711, `["P","K","Ca","Mg","S","Fe","Mn","Zn","Cu","B"]`) и рендереры панелей, которые его потребляют (`renderNutrientCards()`/`renderPhCec()` в `assets/soil-nutrition-analysis.js`), никогда не включают Na, ни на образцах AA, SLAN, ни MLSN. `data.soil.Na` заполняется и используется вычислительно (ESP, проверки содичности/антагонизма в `water-balance-analysis.js`), но никогда не рендерится как собственная строка. Подтверждена та же находка независимо в `word-export.js:10840-10844` (простые строки почвенной панели P/K/Ca/Mg/S) — строки Na там тоже нет.
2. **Пробел Na в D13.** Движок классификации достаточности AA (`mlsnEngine()` в `hub-tissue-v3.js`) оценивает `["P","K","Ca","Mg","S","Fe","Mn","Zn","Cu","B"]` — Na нет и в этом списке тоже, так что Na почвы никогда не классифицируется как низкий/адекватный/высокий вообще, помимо того, что никогда не отображается.

**Предложенное решение.** (1) Добавить самостоятельную строку "Sodium (Na)" в основную панель химии почвы (и в `renderNutrientCards()` UI, и в простые строки почвенной панели экспорта), независимо от того, рендерится ли Base Saturation — это бесспорно и должно выпускаться независимо от результата Решения 2. (2) Восстановление Base Saturation конкретно для образцов AA нуждается в решении клиента (Решение 2) — не отменять подавление `b35fix438` без него, учитывая, что обоснование этого фикса зеркалит собственную заявленную позицию клиента по BCSR в другом месте этого документа.

**Трудоёмкость и радиус поражения:** (1) небольшая и аддитивная — одна новая строка в двух рендерерах, используя уже захваченные данные. (2), если клиент решит восстановить Base Saturation для образцов AA, — однострочная отмена условия `suppressBaseSaturation`, но не должна делаться без взвешивания против цитируемой литературы, на которой основано текущее подавление. **Тест:** утвердить, что Na почвы рендерится как собственная строка независимо от методологии; если (2) реализовано, утвердить, что Base Saturation снова рендерится на образцах AA с включённым Na.

### D28 — данные NTEP из несовпадающего региона применены без оговорки; износ выведен из качества, S2, Tier A

> North Central Region is continental United States... using it to set a wear modifier and disease resistances for a maritime New Zealand site needs a stated caveat at minimum.
>
> The wear modifier is inferred, not measured. ... NTEP publishes traffic tolerance as its own trait. Deriving wear from quality, and then calling 6.3 good where the table beside it says moderate, is two errors in one line.

**Что код реально делает сегодня.** Подтверждено по обеим половинам, и собственные комментарии кодовой базы уже флагируют вторую половину как известное ограничение: `assets/gilba-variety-traits.js:3953-3958` (задублировано в `assets/gssh-variety-traits.js:3339`) — запись трейта `wear` для Soprano читает `multiplier: 0.95, confidence: 'medium', source: 'NTEP 2004 quality data - good overall quality suggests adequate wear', notes: 'No specific traffic data available, estimate based on quality scores'` — код уже документирует, в собственном поле `notes`, ровно то ограничение, которое определяет клиент; эта оговорка просто никогда не доходит до клиентского текста экспорта/UI. Строка региона испытания (`gilba-variety-traits.js:3911-3913`, `qualityRating: 6.3, qualitySource: 'NTEP 2004 Trial, North Central Region 2008 (pr04_09-11/pr0409t06.txt)'`) не несёт оговорки о несовпадении региона — примечательно, что **другие записи в том же файле уже используют эту точную конвенцию** (например, `qualitySource: 'NTEP 2019-2023 only - NOT tested in Australian cold climates'`, `'NOT TESTED'`) — паттерн оговорки-в-строке-источника уже существует в этой кодовой базе, он просто не применён к записи Soprano. Массив `testedRegions` действительно существует в записи Soprano (`gilba-variety-traits.js:3915`, включает `'nzsti_nz'`), но потребляется только `assets/variety-traits-integration.js:868-927` для решения, в каких региональных выпадающих списках появляется сорт — никогда не читается во время рендера для привязки оговорки; Soprano помечен `nzsti_nz` чисто чтобы быть выбираемым для NZ-сайтов, а не потому что за ним стоят данные испытаний NZ. Само противоречие рендерится в Word-экспорте, точно: `assets/word-export.js:6222` (`profile.ratingSource = resolvedQualitySource...`) печатает сырую строку `qualitySource` дословно в `word-export.js:6434-6438`; строка Quality-rating помечает 6.3 как **"Moderate"** в `word-export.js:6493` (`val >= 5.5 ? 'Moderate' : ...`); отдельная карточка Wear Tolerance trait (`word-export.js:6234-6248`, текст источника рендерится в `:6532-6538`) печатает захардкоженную строку "good overall quality suggests adequate wear" — обе управляются идентичным значением 6.3, один раздел друг от друга, напрямую противоречащие друг другу. Отдельно, реальные данные traffic-tolerance (не выведенные из качества) существуют в этой кодовой базе для других сортов: `assets/geves-variety-traits.js` несёт настоящее поле `wearTolerance` (данные испытаний французского GEVES, значения 6.75-7.15 по собственной шкале) для сортов, которые он покрывает — Soprano среди них нет, так что лучших данных для этого конкретного сорта сегодня нет, но подход фолбэка-на-качество должен быть видимо флагирован именно как таковой, когда используется, согласно тому, что уже говорит поле `notes` внутренне.

**Предложенное решение.** (1) Проявить существующее поле `notes` ("estimate based on quality scores, no specific traffic data") в клиентском выводе профиля сорта в `word-export.js:6532-6538` — небольшое изменение, честность уже существует в данных, её просто не печатают. (2) Добавить проверку совпадения региона: сравнить заявленный регион/климатический класс испытания с собственным регионом сайта (уже разрешённым в другом месте для целей климата), и печатать оговорку, когда они не совпадают, следуя уже используемой в том же файле конвенции "строки оговорки qualitySource" вместо изобретения нового механизма, и переиспользуя `testedRegions` (уже присутствует, просто не читается во время рендера) вместо добавления нового поля. (3) Там, где поле `wearTolerance` существует для того же сорта из лучшего источника (например, GEVES, для сортов, которые он покрывает), предпочитать его выведенной из качества оценке — неприменимо к Soprano сегодня, но стоит выстроить порядок предпочтения сейчас, чтобы он был автоматическим, когда позже добавятся лучшие данные. (4) Исправить прямое одноэкранное противоречие независимо от (1)-(3): метка "Moderate" строки Quality (`word-export.js:6493`) и утверждение "adequate wear" карточки Wear (`:6532-6538`) не должны выводиться из одной и той же цифры 6.3 без хотя бы перекрёстной сверки формулировок друг друга.

**Трудоёмкость и радиус поражения:** все четыре изменения ограничены рендерингом профиля сорта в `word-export.js` (строки ~6200-6540) плюс чтением одного уже присутствующего поля (`testedRegions`), которое сейчас ничего не потребляет для этой цели; (3) дополнительно затрагивает функцию, которая сейчас читает только выведенное из NTEP-качества поле износа `gilba-variety-traits.js` и должна проверять `geves-variety-traits.js` первым, когда оба существуют для сорта. Низкий риск, аддитивные изменения только оговорок, никакие числовые значения не меняются. **Тест:** утвердить, что отображение модификатора износа включает оговорку "оценено, не измерено" всякий раз, когда `source` указывает на выведенную из качества оценку; утвердить, что оговорка о несовпадении региона печатается, когда регион испытания из `testedRegions` ≠ климатический класс сайта; утвердить, что метка строки Quality и текст карточки Wear не противоречат друг другу на одной и той же исходной оценке.

### D29 — идентификаторы болезней в camelCase и строки с нулевым модификатором, S4, Tier A

> `crownRust` is an internal identifier reaching client output. Both rows carry a 0% modifier at confidence none.

**Что код реально делает сегодня.** Подтверждено точно, и это пробел на одну запись в в остальном хорошо построенной системе, а не отсутствующая функция: `resolveDiseaseDisplayName()` (`assets/disease-engine-pure.js:492-511`) — настоящий, учитывающий вид резолвер меток (добавлен в `b35fix419`/C4, корректно обрабатывающий видо-специфичное именование `takeAll`/`takeAllPatch` и дюжину других болезней), который падает на `_DISEASE_DISPLAY_DEFAULTS[diseaseKey] || diseaseKey` — а `_DISEASE_DISPLAY_DEFAULTS` (`disease-engine-pure.js:301-317`) просто не имеет записи `crownRust`/`rust`, так что возвращает сырой ключ без изменений. Строка с нулевым модификатором тоже по замыслу, не случайно: запись `crownRust` в `gilba-variety-traits.js:3967-3969` читает `riskMultiplier: 1.0, confidence: 'none', source: 'No verified trial data; fabricated value removed b35fix471'` — предыдущий фикс корректно *убрал* выдуманное число вместо того, чтобы оставить неверное, что абсолютно правильно по собственному правилу проекта "никаких непроверенных допущений"; честное состояние "нет данных" верно, только его **видимость** в клиентской таблице — оставшийся дефект. Примечательно, что рендерер чипов на экране (`cultivar-profile-ui.js:371`) уже гейтует на `mod.confidence !== 'none'` и подавляет их — **только таблица Word-экспорта** (`word-export.js:6374-6389`, `diseaseCards.push(...)` без проверки доверия/модификатора) не фильтрует их, тот же паттерн "UI починили, экспорт отстаёт", что виден в других местах этой кодовой базы (D01, D31, D07a).

**Предложенное решение.** (1) Добавить `rust: 'Crown Rust'` (подтвердить точную желаемую метку с клиентом — собственная карта чипов `cultivar-profile-ui.js` уже использует простое `'Rust'` и для `rust`, и для `rustZoysia`, так что есть небольшое решение по именованию, должна ли каноническая метка быть видо-специфичной "Crown Rust" или общей "Rust"; низкие ставки, выбрать одно и использовать везде) в `_DISEASE_DISPLAY_DEFAULTS`. (2) Добавить тот же фильтр `confidence !== 'none'`, который уже есть в рендерере чипов UI, в цикл построения `diseaseCards` в `word-export.js` (`word-export.js:6374-6389`) — скопировать существующую, работающую логику UI вместо написания новой логики фильтрации.

**Трудоёмкость:** тривиальная — одна запись карты, одно добавленное условие, скопированное из существующего кода UI. Нулевой риск для каких-либо реальных данных (и болезни, и значения доверия не меняются, меняется только фильтрация отображения). **Тест:** утвердить, что ни одна строка болезни с `confidence: 'none'` не появляется в таблице Disease Resistance Profile Word-экспорта; утвердить, что `crownRust` рендерится как человекочитаемая метка.

### D34 — растворимые продукты рендерятся под заголовком Granular Products, S4, Tier A

> The UI monthly program's GRANULAR PRODUCTS column contains only carry-over annotations for soluble products.

**Что код реально делает сегодня — корневая причина прослежена через два файла, не один.** Источник: `prebbles-products.js:1957-1985`, путь фолбэка переноса `v10.3.37 FIX` делает `const allProducts = [...rec.granular, ...rec.liquid]; const mainProduct = allProducts[0];`, затем `coveredByProduct: mainProduct.name` — когда единственный продукт месяца растворимый (например, MAP Tech, `rec.granular` пуст), `allProducts[0]` становится растворимым продуктом, и результирующий объект `coveredBy` несёт название растворимого продукта **вообще без прикреплённого флага `form`**. Рендер: `nutrition-prebble-integration.js:1013`, `<td class="prebble-cell prebble-granular">${granularList}${coverageDisplay}${activeDisplay}</td>` — `coverageDisplay` (построенный на строках 949-950 из `m.coveredBy`) безусловно добавляется к ячейке гранулированной колонки без какой-либо проверки типа покрывающего продукта. Выделенная колонка liquid/soluble уже существует одной строкой ниже (`prebble-cell prebble-liquid`, строка 1014) с собственными конвенциями рендера с учётом растворимости (обработка `p.form === 'soluble'`, строки 958-969) — аннотация переноса просто никогда через неё не проходит.

**Предложенное решение.** Исправить у источника, не только в рендере: заставить построение объекта покрытия (`prebbles-products.js:1932-1937` и фолбэк `v10.3.37` в `:1957-1985`) захватывать `form` покрывающего продукта вместе с его названием, затем маршрутизировать `coverageDisplay`/`activeDisplay` в подходящую колонку (гранулированная против liquid/soluble) во время рендера на основе этого захваченного `form`, переиспользуя логику назначения колонок, которая уже есть у остальной части таблицы для обычных (не-переносных) строк.

**Трудоёмкость:** небольшая — одно добавление поля в точке записи покрытия, одно условие в точке рендера. Низкий риск, чисто фикс маршрутизации/отображения, без изменения лежащей в основе математики покрытия. **Тест:** аннотация переноса растворимого продукта должна рендериться в колонке liquid/soluble, не в гранулированной.

### D35 — метка "Reapply" перекрывает аннотации графика, S4, Tier A

> PGR response curve renders overlapping text. Cosmetic.

**Что код реально делает сегодня.** Глубоко не расследовано — сам клиент оценивает это как чисто косметическое (S4), и это не заслуживает той же доказательной строгости, что пункты S1/S2. Вероятно, столкновение позиционирования графика/метки в рендерере кривой отклика PGR (`assets/pgr-forecast.js` или где бы ни рисовалась кривая) между слоем аннотаций и меткой статуса.

**Предложенное решение.** Фикс CSS/раскладки — переместить или изменить z-порядок метки "Reapply" от текста аннотации Suppression/Rebound, либо добавить смещение для избегания коллизий. Никакого агрономического содержания, чисто визуально.

**Трудоёмкость:** небольшая, один компонент, визуальный контроль качества, а не тест данных. **Тест:** только визуальная проверка — скриншот кривой отклика PGR в точке данных, где ранее было перекрытие, подтвердить читаемость.

### D36 — Monthly N Need в 0 стилизован как позитивное состояние, S4, Tier A

> Zero N is a consequence of the stale climate series (D01), not a good state.

**Что код реально делает сегодня.** `assets/plan-ui.js:857-860` — любое значение ≤10 кг/га, включая ровно 0, получает `nCls = 'green'` и метку бейджа `'Low'`, без различия между "реально низкое, потому что покой" и "ноль из-за проблемы данных/привязки". Эта карточка читает из `window.GilbaNutritionCalendar.program` — того же самого устаревшего объекта D01 — так что её *значение* самокорректируется, как только D01 приземлится, но сама логика стилизации — отдельный, всё ещё открытый баг, согласно собственной инструкции клиента ("исправить семантику в любом случае").

**Предложенное решение.** Различать "0, потому что реально в покое/нет потребности" от "0, потому что данные недоступны" на уровне стилизации — например, отдельное серое/нейтральное состояние для точно-нуля, а не сворачивание его в тот же зелёный бакет "Low", что и просто малое-но-ненулевое значение, либо требовать сопутствующий флаг от лежащего в основе объекта программы, указывающий, отражает ли ноль реальное вычисленное значение или фолбэк по отсутствию данных.

**Трудоёмкость:** небольшая, одна ветка стилизации в `plan-ui.js`, зависит от того, может ли лежащий в основе объект программы различать оба случая (подтвердить перед тем, как фиксироваться на точном условии — если вывод `computeProgram()` сейчас не несёт этого различия, небольшой флаг нужно добавить и туда тоже, в том же проходе, что и фикс инвалидации D01). **Тест:** перемерять после того, как D01 приземлится (собственная инструкция клиента); отдельно утвердить, что логика стилизации обрабатывает подтверждённо-нулевой месяц иначе, чем просто-низкий.

### D46 — Component Breakdown показывает сырые оценки под заголовком "weighted", S4, Tier A

> The panel is headed "Weighted contribution to today's ESI score" and prints Thermal 1, Light 0, Moisture 0, Traffic 100. Those are the raw component scores.

**Что код реально делает сегодня.** `assets/stress-analysis.js:436-457`, `renderComponentBreakdown()` — `var score = comps[key] || 0;` читает сырую, довзвешенную оценку компонента напрямую из `day0.components` и отображает её (`scoreRounded`) рядом с процентом веса компонента (`meta.weight + '%'`) — но никогда не перемножает их. Композитная сумма в другом месте разрешается корректно (веса применяются на шаге суммирования в `stress-trajectory-engine-pure.js`, подтверждено проверкой агента), так что это чисто ошибка уровня отображения, не арифметическая ошибка, распространяющаяся куда-либо ещё.

**Предложенное решение.** Либо (a) перемножить `score × (weight/100)` перед отображением, чтобы панель реально показывала взвешенный вклад, как утверждает её заголовок — соответствует заголовку, меняет отображаемое число; либо (b) изменить заголовок на что-то вроде "Component scores and their weights" и продолжать отображать сырые оценки — соответствует тому, что сейчас показано, числового изменения нет. Рекомендуется (a): "взвешенный вклад в оценку ESI" более полезен читателю, пытающемуся понять, что определяет сегодняшний индекс, чем сырые под-оценки 0-100, и это то, что заголовок уже обещает.

**Трудоёмкость:** тривиальная, одна строка в `stress-analysis.js:449`. Нулевой риск — только отображение, не трогает корректно вычисленную композитную сумму. **Тест:** утвердить, что отображаемое значение для каждого компонента равно его сырой оценке × его весовой доле, и что четыре значения суммируются в напечатанную композитную сумму.

---

## Инженерная работа Tier B

### D03 (общее правило) — применить "никакого тихого фолбэка на нерезолвленной привязке" везде, S1/S2, Tier B

> Consider fixing both behind one rule: no module substitutes a default for an unresolved binding without emitting a blocking error.

**Что код реально делает сегодня.** Уже громкий отказ: `nutrition-calendar.js:1255-1258` (флаг недоступности климата), `nutrition-summary-integration.js:1352-1365` (явная заглушка "climate data unavailable"), `climate-normals-service.js:130-146` (возвращает `null`, никогда не догадку). **Всё ещё тихо, найдено в ходе этой проверки:**
1. `assets/event-planner-engine.js:508-535` — `estimateClimateFromNormals()`/`estimateDLI()`: чистый генератор по широте с косинусом (`meanAnnual + amplitude·cos(phase)`), тот же самый *тип* артефакта, который выявила подгонка кривой аудитом D03 в E1 — используется для любого дня симуляции Event Planner за пределами окна реального прогноза ~8 дней, тихо помечен `source:'climatology'` без пути ошибки и никогда не консультируется с реальными данными `GilbaClimateNormalsService`/NASA POWER, которые использует остальная часть приложения.
2. `assets/disease-forecast.js:1630-1657` — `buildDailyPatternFallback()`: сфабрикованный синусоидальный 7-дневный паттерн температуры (`Math.sin(i*0.8)*1.5`) вокруг последнего известного среднего, без какого-либо нижестоящего флага, отличающего его от реальных данных прогноза, срабатывает всякий раз, когда почасовые данные прогноза Open-Meteo недоступны. Всё ещё живой и достижимый — потребляется моделями Red Thread и Brown Patch согласно GH-236/237.
3. `assets/nutrition-summary-integration.js:641-658` — устаревший локальный путь расчёта GP (достижим только если основной движок не загрузится) тихо подставляет 15°C для любой falsy месячной температуры, включая легитимный `0`. Ниже серьёзность, уже радиус поражения.

**Предложенное решение.** Применить тот же паттерн, уже проверенный на трёх "уже исправленных" местах: заменить каждый тихий генератор явным состоянием недоступности/деградации, которое вызывающая сторона должна обработать, следуя прецеденту, который уже задаёт `climate-normals-service.js` (возвращать `null`/флагированный результат, никогда догадку). Это механический проход с низким уровнем суждения теперь, когда паттерн существует в кодовой базе трижды, чтобы с него копировать — рекомендуется сделать это как один сфокусированный проход по всем оставшимся местам, а не по частям, поскольку "правило" имеет ценность, только если оно реально применяется везде, согласно собственной формулировке клиента.

**Трудоёмкость и радиус поражения:** три файла, каждый независим (разные потребляющие модули — Event Planner, Disease Forecast, устаревший путь питания), так что это можно разбить на небольшие PR без кросс-зависимости. Риск регрессии зависит от того, сколько живых сайтов сейчас полагаются на срабатывание тихого фолбэка незаметно для кого-либо — проверить вызывающие стороны каждого места перед переключением на громкий отказ, поскольку вызывающая сторона, которая не обрабатывает новое состояние недоступности изящно, регрессирует хуже, чем состояние тихих-неверных-данных, которое она заменяет (это стандартный риск для любой миграции на громкий отказ; тестировать обработку на каждой точке вызова до выпуска, не после).

**Тест:** для каждого из трёх мест фикстура с условием срабатывания фолбэка (превышено окно прогноза, отсутствуют почасовые данные) должна производить явный флаг недоступности/деградации, а не правдоподобно выглядящее сфабрикованное значение; утверждение 11 ("климатическая серия, чей подразумеваемый годовой диапазон превышает 12°C, не должна рендериться на сайте NZ") — хорошая общая регрессионная защита для добавления по всем климат-смежным местам фолбэка, не только тем, что изначально назвал D03.

### D05 — требование по P/S не согласовано, как N/K, S2, Tier B

> The reconciliation layer added for N and K does not extend to P or S.

**Что код реально делает сегодня.** Уже частично исправлено, неравномерно: на живом UI страницы Plan P **уже** согласовывается идентично N и K (`nutrition-prebble-integration.js:792-895`, `classifyBalance()`, вызывается для `['N','P','K']` на строке 898) — судя по всему, побочный эффект работы по tissue-gate GH-361/368, а не намеренный фикс D05. S в этот массив не включён. В **обоих** путях Word-экспорта P и S остаются голыми цифрами требования с только двухсостоянейным цветом — нет Delivered, нет Balance, нет сравнения Required-против-Delivered; только у K есть выделенный модуль сверки (`k-reconciliation-decision.js`, `synthesiseDecision()`), эквивалента для P нигде в кодовой базе нет (подтверждено grep — `PRecon`/`_classifyPReconState` не существуют).

**Предложенное решение.** Извлечь нутриент-универсальную версию логики сверки, которую уже реализует `k-reconciliation-decision.js` (она структурно уже близка к универсальной — `nutrient: 'K'` представляет собой единственный литерал, согласно собственному комментарию заголовка, отмечающему, что он был извлечён именно для того, чтобы другие страницы могли переиспользовать его без загрузки всего `word-export.js`), параметризовать по нутриенту, и применить к P в обоих путях Word-экспорта (соответствуя тому, что UI уже делает). **Не** применять к S — согласно решению D44, S должен двигаться в противоположном направлении (из фреймворка сверки, а не в него) — оформить расширение D05 только-для-P и удаление-S D44 как два изменения одних и тех же общих циклов, в идеале в одном PR, чтобы эти два не конфликтовали при правках одних и тех же массивов нутриентов.

**Трудоёмкость и радиус поражения:** обобщение `k-reconciliation-decision.js` — умеренный рефакторинг (сейчас это единственный источник истины для формы Required/Delivered/Balance/Status у K, потребляемый обоими путями Word-экспорта — обобщение требует аккуратности, чтобы не регрессировать собственное поведение K, поэтому это Tier B, не Tier A). Новые P-специфичные точки вызова в обоих файлах экспорта, заменяющие текущий рендер голой цифры. **Тест:** утверждение 5 ("то же утверждение [сверка в допуске или флагировано] для P, K и S, симметрично" — читать это с учётом решения D44: симметричная сверка применяется к N/P/K, S явно исключён по более позднему решению, так что это утверждение нужно обновить, чтобы явно это сказать, а не буквально включать S).

**Зависимости:** оформить после D31 (использует те же циклы движка) и вместе с D44 (использует те же массивы нутриентов).

### D13 — конвертация диапазонов Hill Labs S277, S3, Tier B (пункт расследования, разрешён проверкой)

> If the Hill Labs bands are natively expressed in me/100g, confirm the bands were converted alongside the values.

**Что код реально делает сегодня — разрешено без необходимости предложенного клиентом сканирования живых данных.** `assets/hill-labs-sample-types.js` определяет пороги S277 нативно в me/100g и предоставляет `getRangesPpm(code, nutrient, cec)`, который корректно конвертирует их (делители K:391, Ca:200, Mg:122, Na:230) перед тем, как классификатор `hub-tissue-v3.js` сравнивает их с ppm-значениями почвы. **Низкий флаг структурно может сработать для K/Ca/Mg/P/S** — предложенный клиентом дешёвый тест ("ни один катион никогда не может флагироваться низким" подтвердил бы баг) **ложен** против текущего кода; конвертация корректно подключена для этих пяти нутриентов. **Единственный реальный пробел: Na исключён из этой классификации полностью** — цикл переопределения по сертификату движка AA покрывает только `["P","K","Ca","Mg","S"]`, а собственный список нутриентов классификатора (`["P","K","Ca","Mg","S","Fe","Mn","Zn","Cu","B"]`) не имеет члена Na вообще, так что Na почвы никогда не классифицируется ни одной методологией как низкий/адекватный/высокий, не только никогда не отображается (это тот же пробел, что D25 покрывает со стороны отображения).

**Предложенное решение.** Закрыть D13 как проверено-корректный для K/Ca/Mg/P/S — изменений кода там не требуется. Свернуть пробел Na в область D25, а не рассматривать как отдельный фикс D13, поскольку это та же самая проблема отсутствующего Na с другого угла (классификация против отображения), а фикс (добавить Na в те же списки нутриентов) — одно и то же изменение в любом случае.

**Трудоёмкость:** ноль для половины P/K/Ca/Mg/S (только проверка). Половина Na покрыта оценкой трудоёмкости D25. **Тест:** нового теста для разрешённой половины не требуется; покрытие тестами классификации Na следует добавить вместе с тем, какой бы тест ни получило добавление строки Na D25.

### D13b — широкие обзорные диапазоны рендерятся как жёсткие 3-состояниевые гейты, S3, Tier B

> These are survey spreads from field plots, not calibrated sufficiency ranges... Wide survey-derived bands return a fourth verdict state, "measured, insufficient calibration to assess."

**Что код реально делает сегодня.** Концепции качества/доверия диапазона нигде не существует в схеме диапазонов ткани. `classify()` файла `tissue-engine.js` возвращает 5-состояниевый результат позиции значения (`Missing/Deficient/Marginal/Sufficient/High`) чисто из `value` против `{lo,hi}` — ни одно поле объектов диапазона не записывает происхождение, разброс или доверие калибровки. `generateTissueNarrative` в `word-export.js` использует более простую 3-состояниевую схему против тех же голых объектов `{lo,hi}`. Диапазон Fe (`{lo:97, hi:934}`) задублирован дословно в трёх файлах без метаданных, отличающих его от калиброванного диапазона ни в одном из них. Четвёртого состояния вердикта нигде не существует (`grep` по `bandQuality`/`insufficient calibration`/подобному: ноль совпадений).

**Предложенное решение — это нуждается в решении по модели данных, согласно собственной группировке клиента D13b с D24 и D48 ("каждому нужна модель вердикта, которая может выразить 'ниже порога, при котором происходит восстановление' и 'не существует калиброванного порога для этого случая'... оформить их вместе").** Добавить атрибут `bandQuality` (или подобный) к определениям диапазонов — например, `{lo, hi, quality: 'calibrated'|'survey'}` — и четвёртое возвращаемое состояние классификатора, единообразно потребляемое каждым рендерером, который сейчас предполагает 3 или 5 состояний. Это общее решение по схеме, которое нужно D13b/D24/D48 всем; сделать это один раз, как изменение контракта модели вердикта, не три раза по-дефекту. См. Решение 5.

**Трудоёмкость и радиус поражения:** затрагивает схему определения диапазона как минимум в `tissue-engine.js`, `soil-nutrition-analysis.js`, задублированную копию `word-export.js` (три задублированных определения диапазона Fe нуждаются в одном и том же теге качества, добавленном согласованно — рассмотреть консолидацию трёх копий в один общий источник, пока это трогается, поскольку дублирование — это ровно то, что позволило пробелу с цитатой D13a остаться незамеченным тоже), плюс каждый рендерер логики классификации-и-отображения нуждается в ветке для нового состояния. Значимо, но ограниченно — это изменение контракта *вердикта*, которое, согласно поправке v6 клиента, теперь дешевле в SaaS, чем было бы в WordPress-плагине (больше не флагируется как архитектурно рискованное).

**Тест:** дух утверждения 7a расширен — элемент с широким обзорным диапазоном (Fe, Cu) должен рендерить четвёртое состояние вместо ложного вердикта "sufficient"/"optimal" на значении, которое просто внутри очень широкого разброса.

### D15 — предупреждение о почвенном антагонизме Fe:Mn противоречит ткани Mn, S2, Tier B

> Where tissue data exists for an element, the tissue measurement governs. Suppress the soil-derived antagonism narrative for that element.

**Что код реально делает сегодня.** `mulders-interaction-checker.js:152-330` — правило Fe:Mn (порог 2.5, тяжёлое при 5.0, совпадает с цифрами аудита точно) вычисляется чисто из значений на основе почвы; данные ткани нигде не читаются в `checkMuldersInteractions()` или её вызывающей функции. Предупреждение срабатывает и печатается безусловно, когда почвенное соотношение пересекает порог, независимо от того, что показывает ткань Mn — на Hoxton ткань Mn (189 ppm, 2.6× верхней границы своего диапазона) напрямую противоречит почвенному прогнозу "Mn подавлен".

**Предложенное решение (инженерное решение, агрономический принцип уже установлен собственными цитируемыми доказательствами клиента).** Прокинуть данные ткани в аргумент `context` `checkMuldersInteractions()` (сейчас вообще не передаётся), и когда данные ткани существуют для подавляемого элемента, либо подавить почвенное повествование об антагонизме, либо понизить его до условного примечания ("почвенная химия предсказывает подавление Mn; измерение ткани этого не подтверждает") — собственная форма фикса клиента предлагает оба варианта, а эмпирический случай, который он цитирует (197 парных образцов почвы/ткани, только N и Cu показывают какую-либо корреляцию), поддерживает понижение широко, не только для этой одной пары элементов. Рекомендуется вариант понижения-до-условного-примечания вместо полного подавления — он сохраняет видимой почвенно-химическую логику (полезный контекст), делая при этом ясным, что она не подтвердилась, вместо удаления информации, которую читатель всё ещё может хотеть.

**Трудоёмкость и радиус поражения:** сигнатура функции `mulders-interaction-checker.js` и её точка(и) вызова нуждаются в прокладке данных ткани — проверить, сколько точек вызова существует и надёжно ли доступны данные ткани во всех них до этого вызова (порядок вычисления важен: анализ ткани должен запуститься до проверщика взаимодействий, иначе это станет собственным крайним случаем "ткань ещё не доступна"). Умеренная трудоёмкость с учётом прокладки, низкий вычислительный риск. **Тест:** на фикстуре с противоречащими доказательствами почва-против-ткани Fe:Mn (как Hoxton), утвердить, что повествование об антагонизме понижено/условно, а не заявлено как факт.

**Зависимости:** нет блокирующих, но затрагивает тот же файл проверщика взаимодействий, на который ссылаются комментарии об отмене BCSR Ca:Mg D08 (`b35fix439`/`b35fix440`) — прочитать эту историю перед редактированием, в файле уже есть недавние, тщательно процитированные изменения.

### D24 — окно вмешательства инверсно композиту, доминируемому траффиком, без прогнозных данных, S1, Tier B

> The gate should be two-factor: growth potential at or above 50% AND daily light integral at or above the species-and-height recovery threshold. Either one below floor returns an empty window naming the limiting factor.

**Что код реально делает сегодня.** Подтверждено с точным механизмом. Композит ("ESI") вычисляется в `stress-trajectory-engine-pure.js` с весами `{thermal:0.20, light:0.15, moisture:0.20, traffic:0.20, nutrition:0.15, biotic:0.10}` (~строки 97-107) — воспроизводя цифру аудита `100×0.20 + 0.7×0.20 = 20.14` точно. Само окно вмешательства (`stress-trajectory-engine-pure.js:891-916`) построено **исключительно** из `trajectory[i].totalScore < CONFIG.thresholds.normal` — ни GP, ни DLI нигде в этом цикле или окружающей функции; `grep` по `GrowthPotential`/`growth-potential` в этом файле ничего не даёт. Константы порога GP какого-либо рода нигде сейчас не существует в файлах stress-trajectory (подтверждено отсутствие). Отдельно, термический стресс (`stress-trajectory-engine-pure.js:187-266`, кусочно-линейная рампа от `{min:15,max:24,peak:20}`) и growth potential (`growth-potential-engine.js:38-56`, гауссиан PACE, `optimum:20, sigma:5.5`) — две структурно независимые модели температуры (это D38, то же самое сопряжение файлов, включено сюда, поскольку это механизм, на котором реально работает термический компонент D24) с нулевой перекрёстной ссылкой в любом направлении.

**Рекомендация, которую окно вмешательства должно консультировать, уже существует и уже корректна**, подтверждена в `assets/growth-light-analysis.js:1850-1851` (функция `gpRec`): `if (gp < 40) { ... 'growth potential critically low (' + fmt(gp,0) + '%). Defer aggressive cultural practices — focus on stress recovery.' }` — это "хаб уже держит правильное правило", на которое ссылается клиент; это другой модуль (`growth-light-analysis.js`, питающий панель Recommendations), который движок stress-trajectory просто никогда не вызывает. **Важно: это существующее правило срабатывает при `gp < 40`, не установленном клиентом пороге 50%** — фикс D24 должен ввести собственную именованную константу `CULTIVATION_GP_FLOOR = 0.50` согласно решению клиента v5.1, не копировать порог `40` этого модуля, который отвечает на связанный, но другой вопрос (когда сайт находится под критическим стрессом вообще), а не на вопрос пригодности к культивации, которым занимается D24. Никакой именованной константы порога GP какого-либо рода сейчас нигде не существует в кодовой базе (подтверждено отсутствие grep'ом) — это будет новая константа в любом случае.

**Механизм затухания, в закрытой форме.** Таблица весов `stress-trajectory-engine-pure.js` (~строки 95-113) сопрягает каждый компонент с собственным `recoveryDays` (`thermal:7, light:10, moisture:5, traffic:7, nutrition:14, biotic:14`); прогноз вперёд (`stress-trajectory-engine-pure.js:798-836`) затухает каждый непрогнозированный компонент как `current × (1 − 1/recoveryDays)^n`. Поскольку композит нулевого дня Hoxton — это только thermal(0.7)+traffic(100), и оба случайно разделяют `recoveryDays=7`, вся траектория сводится к единой экспоненте `20.14 × (6/7)ⁿ` (база 0.857), чрезвычайно близкой, но не побитово идентичной подогнанной клиентом `20×0.851ⁿ` — тот же механизм (снимок нулевого дня, затухший без прогнозных данных, принятый за прогноз), небольшой разрыв в базовой ставке, скорее всего, объясняется округлением отображения `Math.round`/`ceil`, накапливающимся за 14 шагов, либо реальными температурами прогноза, подставляемыми вместо затухания в дни, когда они доступны (`stress-trajectory-engine-pure.js:801-808` ветвится на данные прогноза, когда они присутствуют). Подтверждает предпосылку утверждения 23 напрямую: это сфабрикованное однопараметрическое затухание, а не реальный 14-дневный прогноз.

**Предложенное решение (агрономический параметр уже установлен клиентом — порог GP 50%, решение v5.1; порог DLI нуждается в том, чтобы сначала существовала таблица вид/высота D48).**
1. Добавить gate по порогу GP в цикл построения окна `stress-trajectory-engine-pure.js` (~строка 891): перед добавлением интервала в `interventionWindows`, дополнительно требовать, чтобы текущий GP сайта был ≥ новая именованная константа (`CULTIVATION_GP_FLOOR = 0.50`), и когда он ниже порога, возвращать пустое окно со строкой причины ограничивающего фактора, согласно форме фикса клиента.
2. Добавить gate по DLI таким же образом, один раз — **но только когда D48 произведёт реальную таблицу порогов вид/высота** (см. D48 ниже); до этого эта половина не может быть реализована без изобретения числа, что противоречит правилу проекта. Приземлить GP-gate первым как собственное изменение (оно независимо ценно и разблокировано сегодня), DLI-gate как последующее, как только D48 приземлится.
3. **Этот же фикс следует реализовать на уровне общего композита/gate**, не дублировать по-поверхностно — и D46 (Component Breakdown), и D47 (баннер "Conditions favorable") читают из того же объекта `trajectory`/`peakScore`, из которого построено это окно; gate на общем источнике означает, что все три поверхности (окно, разбивка, баннер) плюс карточка Wear Resistance, которую клиент отдельно флагирует, становятся согласованными в одном изменении, соответствуя собственной инструкции клиента, что исправление одного D24 "оставляет две из четырёх поверхностей всё ещё противоречащими пятой".

**Второй, меньший фикс (тоже D24, согласно аудиту): 14-дневная траектория затухает единственное известное значение (сегодняшний traffic=100) до нуля без прогнозных данных, и затухание читается модулем как "улучшение".** Согласно форме фикса клиента: удерживать последнее известное значение, либо убрать беспрогнозный компонент из взвешивания и перенормировать, либо усечь горизонт прогноза до реально доступных данных — не затухать до нуля и называть это улучшением. Рекомендуется усечение горизонта прогноза (проще всего, наиболее честно — сама логика окна не должна отображать или гейтовать по дням, для которых у неё нет реальных данных) вместо удержания-последнего-значения (которое рискует выглядеть как реальный прогноз, когда это не так) или перенормировки (добавляет сложность за незначительное преимущество над усечением).

**Трудоёмкость и радиус поражения:** основной gate — умеренное, хорошо ограниченное добавление в одну функцию `stress-trajectory-engine-pure.js`; часть "тот же фикс достигает D46/D47/Wear Resistance" означает, что нижестоящим рендерерам нужно читать скорректированное значение вместо сырого композита, что более крупное, но механическое продолжение, как только источник исправлен. Это самый ценный элемент, кроме D30, в этом списке согласно собственному ранжированию серьёзности клиента (S1) и его же замечанию, что маршрутизация Tier B WordPress-плагина больше не применяется — сильный кандидат на это понижение, поскольку это дешевле в SaaS, чем подразумевала исходная метка.

**Тест:** утверждение 22 точно (пустое окно ниже порога GP 50%, с напечатанной причиной, на фикстуре Hoxton, где GP 10-26%); утверждение 23 (траектория стресса не должна быть воспроизводима однопараметрической подгонкой затухания по всей своей длине — это напрямую тестирует фикс "удержать/усечь/перенормировать").

**Зависимости:** половина GP-gate может приземлиться независимо; половина DLI зависит от D48; согласованность D46/D47/карточки Wear Resistance должна приземлиться в том же изменении, что и основной gate, не как отдельный последующий проход.

### D27 — Monthly Schedule и Monthly N Distribution расходятся по времени N, S2, Tier B

> Note this defect will move once D01 is fixed, because the two tables are currently on different climate series. Re-measure after D01 rather than scoping from these figures.

**Что код реально делает сегодня.** Подтверждено как две по-настоящему отдельные точки вызова, независимо читающие климат: Monthly Schedule через `computeProgram()` в `nutrition-calendar.js:1230` (собственный `extractMonthlyTemps()`); Monthly N Distribution через `compute()` в `nutrition-requirement-engine.js:787`, вызываемый из `nutrition-summary-integration.js:1384-1400` с собственным независимым `extractMonthlyTemps()`. Один и тот же лежащий в основе живой производитель климата питает оба, но они читаются в разное время разным кодом и расходятся всякий раз, когда одна сторона показывает сохранённую/устаревшую программу (D01), пока другая пересчитывает вживую. Собственная запись changelog GH-368 независимо документирует те же два движка, расходящиеся и по измерению tissue-ratio тоже (D07a/D31) — эта пара движков — повторяющийся, структурный источник расхождения, не разовый случай.

**Предложенное решение.** Согласно собственной инструкции клиента: не оформлять фикс из текущих цифр — перемерять после того, как приземлится D01 (который исправляет половину *устаревания* расхождения) и после работы по согласованию движков D31 (которая устраняет *структурную двухдвижковую* половину). Если остаточный пробел останется после того, как оба приземлятся, это подлинная третья проблема, нуждающаяся в собственном проходе оформления в тот момент, не раньше.

**Трудоёмкость:** ноль прямо сейчас — это пункт "подождать и перемерять", не сборка. **Зависимости:** жёстко заблокировано на том, что и D01, и D31 приземлятся первыми.

### D31 — ЗАКРЫТО для расчёта требований: GH-370 (единицы), GH-376 (общее ядро), GH-383/384 (обе поверхности переведены), GH-385…GH-389 (доводка). Помесячное распределение закрыто в GH-398. UI и экспорт вычисляли требования K/P разными методами, S2, Tier B

> The UI derives K and P as fixed ratios off the monthly N series (D07a). The export derives them from removal-replacement in the requirements engine. The two land 10% apart on the same site.

**Что код реально делает сегодня.** Два по-настоящему разных метода, теперь сошедшихся по вопросу tissue-gate (GH-361/368 оба применяют одно и то же правило tissue-ratio-когда-доступен), но всё ещё структурно разных в том, против чего они масштабируют это соотношение: `nutrition-calendar.js` масштабирует соотношение ткани/общее против реальной цели `annualN` сайта (`nutrition-calendar.js:1317-1318`, `Math.round(annualN * _pRatio)`); `nutrition-requirement-engine.js` масштабирует его против **фиксированной константы N таблицы вида** (`nutrition-requirement-engine.js:195`, `table.N * ratio`, `table.N = 180` для perennial ryegrass) — так что если реальная цель годового N сайта (например, 200 у Hoxton) отличается от статичных 180 таблицы, два движка всё равно приземлятся на разные цифры P/K даже с идентичными данными ткани. Также структурно разные модели скашивания — `nutrition-calendar.js:139-151` (`collected: nFactor 1.0, pFactor 1.0, kFactor 1.0`; `returned: nFactor 1.0, pFactor 0.4, kFactor 0.5`) против `nutrition-requirement-engine.js:216-219,604` (`CLIPPING_COLLECTION_FACTOR = 2.5`, `returned` фактически 1.0×) — и разные модификаторы траффика (`nutrition-calendar.js:155`, `{low:0.85, moderate:1.0, high:1.15, extreme:1.3}` против `nutrition-requirement-engine.js:223`, `{low:0.8, moderate:1.0, high:1.2, extreme:1.5}`).

**Подтверждён, независимо, ровно как подозревалось: баг единиц ppm→kg/ha.** В `nutrition-requirement-engine.js` **ноль** вхождений `bulkDensity`, `soilDepth` или коэффициента конвертации `* 0.1` где-либо в файле. Каждый член коррекции во всех трёх ветках методологии вычисляет сырую ppm-дельту, делённую на годы, и добавляет её напрямую к цифре выноса в kg/ha без размерной конвертации:
- AA: `nutrition-requirement-engine.js:404`, `const aaCorrection = (aaRange.min - currentLevel) / yearsToCorrect;` → `:412`, `annualRequirement: Math.round((removal + aaCorrection) * 10) / 10`.
- SLAN: `:578`, `slanCorrection = (rangeFloor - currentLevel) / yearsToCorrect;`.
- MLSN: `:607`, `const correctionRequired = currentLevel < threshold ? (target - currentLevel) / yearsToCorrect : 0;`.

`nutrition-calendar.js` делает конвертацию корректно, дважды — `calculateDeficit()` (`:1008-1009`, `const kgHa = deficit * bulkDensity * soilDepth * 0.1;`) и инлайново (`:1497`, `(floor - currentPpm) * inputs.bulkDensity * inputs.soilDepth * 0.1`). Этот баг **не** специфичен для AA — он присутствует идентично во всех трёх ветках методологии `nutrition-requirement-engine.js` — и он реально размерный: величина ppm (единицы почвенного теста, обычно 10²-10³) обрабатывается так, как будто она уже величина kg/ha (обычно 10⁰-10²), всякий раз, когда сайт реально ниже порога/floor, что завысит или занизит член коррекции примерно на недостающий коэффициент `bulkDensity × depth × 0.1` (порядок величины ~2-3× для типичного песчаного профиля на глубине 15см). Он не проявляется заметно на конкретной фикстуре Hoxton именно потому, что P/K у Hoxton находятся выше потолка (член коррекции = 0 в обоих движках для этого сайта) — это латентный баг, который произведёт материально неверное число при первом же реальном сайте, действительно находящемся ниже floor на пути этого движка.

**Предложенные решения по структурному расхождению — это нуждается в инженерном решении о том, какая модель авторитетна, не агрономическом; арифметический принцип (соотношение должно масштабироваться против реальной цели годового N, и использовать одну модель скашивания/траффика) не оспаривается.**
1. **(Рекомендовано.) Упразднить независимую таблицу ставок выноса и константы скашивания/траффика `nutrition-requirement-engine.js`; заставить его вызывать то же самое вычисление, которое уже выполняет `nutrition-calendar.js` (либо извлечь оба в одну общую чистую функцию, которую вызывают оба движка), вместо поддержания двух параллельных реализаций одной и той же арифметики.** Это единственный вариант, который реально закрывает D31, а не сужает его — что-либо меньшее, чем объединение, оставляет структуру "два движка" на месте для следующего расхождения, чтобы снова открыть её (как уже предупреждает собственная запись changelog GH-368: "держать эти два в шаге друг с другом, если что-либо изменится").
2. Оставить оба движка, но масштабировать соотношение ткани `nutrition-requirement-engine.js` против реального `annualN` вызывающей стороны (прокинутого, так же как это делает calendar-движок) вместо его собственной статичной константы таблицы, и согласовать константы скашивания/траффика к одному общему источнику истины, используемому обоими — меньшее изменение, но оставляет две кодовые базы для бесконечной синхронизации, что ровно тот режим отказа, который породил расхождение D31 и D07a изначально.

Рекомендуется **1**. Это Tier B, работа, затрагивающая движок/схему, но собственная поправка v6 клиента понижает несколько таких пунктов с "рискованно в WordPress" до "дёшево в SaaS" — это сильный кандидат на это понижение, поскольку это убирает целый задублированный движок, а не патчит его.

**Исправить баг единиц независимо от того, какой структурный вариант выбран** — он существует независимо от вопроса двух движков и нуждается в той же конвертации `bulkDensity × soilDepth × 0.1`, которую `nutrition-calendar.js` уже имеет, применённой ко всем трём веткам методологии в `nutrition-requirement-engine.js`.

**Трудоёмкость и радиус поражения:** крупная, если вариант 1 (убирает/переписывает основное вычисление целого файла движка — каждый потребитель вывода `nutrition-requirement-engine.js` нуждается в переповерке, особенно поверхность Monthly N Distribution, которую тоже трогают D01/D27); фикс бага единиц сам по себе небольшой и самодостаточный независимо от того, какой структурный вариант выбран. Учитывая масштаб, рекомендуется сначала приземлить фикс бага единиц как независимую, низкорисковую коррекцию корректности, затем взяться за структурное объединение как за собственную оформленную единицу работы, как только D01/D27 стабилизируются (поскольку Monthly N Distribution, потребитель именно этого движка, в процессе изменений из-за фикса D01).

**Тест:** утверждение 20 точно (UI и экспорт возвращают идентичные годовые требования N/P/K для одного сайта) — это реальный приёмочный тест для структурного фикса; целевой тест конвертации единиц (сайт, реально ниже floor по P/K, утверждающий, что член коррекции приземляется в том же порядке величины, что эквивалент `nutrition-calendar.js`) для самого бага единиц, независимо от таймлайна структурного фикса.

**Зависимости:** у фикса бага единиц зависимостей нет. Структурный фикс должен следовать за D01 (сначала стабилизировать вопрос устаревания климата/программы) и предшествовать D05/D44 (оба трогают те же циклы нутриентов, которые это объединение тоже тронет — сделать объединение сначала, чтобы D05/D44 нужно было редактировать циклы только одного движка, не двух).

### D33 — нет атрибута характеристики высвобождения на продуктах, S2, Tier B

> Add a release-characteristic attribute to the product record, populated from the guaranteed analysis where the label breaks out slowly-available N.

**Поправка к предпосылке аудита: поле release уже существует, оно просто неправильно смоделировано и неправильно заполнено, не отсутствует.** `release: 'slow'|'standard'` плюс `releaseTech: 'mesa'|'mu'|'ibdu'|'pcu'|'scu'|'pcscu'|'as'|'standard'` уже существуют на гранулированных продуктах (карта меток в `nutrition-prebble-integration.js:670-689`; например, MESA помечен `release:'slow', releaseTech:'mesa'` в `prebbles-products.js:379-386`). Остаются два реальных пробела: (1) это **бинарное/категориальное** поле, так что даже MESA — чьи собственные заметки о продукте говорят "частичный быстрый отклик + расширенное высвобождение MU" — вынужден в единственный бакет `'slow'`, неспособный представить процентную смесь; (2) **у собственной записи Ammos 22 не установлено ни одно из полей** (`prebbles-products.js:804-814`) — а нижестоящий код по умолчанию присваивает отсутствующему `release` значение `'standard'` (`prebbles-products.js:2008`, `product.release || 'standard'`), то есть данные хаба сейчас *утверждают*, что Ammos на 100% быстрого высвобождения, неверное направление, не просто молчание об этом. У `density` та же самая форма (существует как имя поля, у одного несвязанного продукта оно установлено, у Ammos — нет, см. D41).

**Предложенное решение.** (1) Изменить схему с бинарного флага `release` на **процент** (`slowReleaseFraction: 0-1`), заполненный из гарантированного анализа ровно так, как описывает форма фикса клиента — Ammos: `0.40` (8.8% от общих 22% N медленно доступны согласно P1/P2). Сохранить `releaseTech` как отдельный описательный тег (MESA, PCU и т.д.) рядом с дробью, а не вместо неё — эти два несут разную информацию (механизм против пропорции). (2) Заполнить `slowReleaseFraction`/`density` для каждого продукта с его собственной этикетки как прямой проход ввода данных, помечая (не угадывая) любой продукт, чья этикетка это не расписывает. (3) Включить оставшийся запрос D45 (хранить, из какого исходного документа взята ставка/цифра анализа) в тот же проход схемы, как предлагает сам клиент — добавить поле `sourceDoc` рядом с новыми полями release/density, поскольку это та же самая миграция "добавить поля в запись продукта" в любом случае.

**Трудоёмкость и радиус поражения: это единственный пункт, который клиент помечает как перевешивающий последовательность ("схемное изменение D33 следует урегулировать рано, независимо от его позиции в списке... дёшево сейчас и дорого, как только реальные программы будут сохранены под текущей формой").** Согласны — рекомендуется сделать добавление схемы (новые поля: `slowReleaseFraction`, `density`, `sourceDoc`) как собственное раннее, самостоятельное изменение по всем файлам каталогов продуктов, отделённое от заполнения значений каждого продукта (которое может следовать постепенно, файл за файлом, бренд за брендом, ничего не блокируя). И D41 (плотность), и D44 (сужение по вторичным нутриентам, которое трогает те же записи продуктов) потребляют эту схему, так что она должна по-настоящему приземлиться до любого из них.

**Тест:** тест уровня схемы, утверждающий, что каждая запись продукта имеет новые поля присутствующими (даже если null/незаполненные приемлемо для продуктов без данных этикетки — суть в том, что поле существует, чтобы быть заполненным, не в том, что каждое значение заполнено в первый день); Ammos 22 конкретно должен получить реальные значения в том же проходе, поскольку тест D41 (утверждение 26) зависит от того, что его плотность корректна, а собственная логика долговечности выбора D33 (`estimateLongevity()`, `prebbles-products.js:3099-3114`) уже читает строку `release`, которая должна стать точнее, как только модель дроби заменит бинарную.

### D38 — термический стресс и growth potential расходятся без объяснительной связи, S3, Tier B

Включён в D24 выше — то же самое сопряжение файлов (`growth-potential-engine.js` против `stress-trajectory-engine-pure.js`), тот же фикс (gate D24 заставляет композит консультироваться с GP напрямую, что и есть вариант "включить член холодовой ёмкости в термический компонент", который клиент предлагает как лучший из двух своих вариантов; реализация gate D24 достигает этого без отдельного переписывания термической модели). Отдельного пункта сборки нет; проверить как часть покрытия тестами D24, что термический стресс и GP больше не производят необъяснимо выглядящее противоречие на одном экране, как только GP гейтует окно.

### D48 — порог DLI не ветвится по виду или высоте кошения, S3, Tier B

> Implement a species-by-height lookup of measured thresholds. Where a cell has no published number, return the fourth verdict state from D13b.

**Поправка к предпосылке аудита, в двух частях — вердикт DLI не полностью слеп к виду сегодня, а реальная проблема имеет худшую форму, чем "добавить колонку высоты".**

**Часть 1 — вид УЖЕ учитывается ветвлением, в трёх отдельных, несинхронизированных копиях одной и той же таблицы.** `assets/shade-engine-pure.js:108-133` определяет `SPECIES_DLI` (согласно собственному комментарию заголовка, это *каноническая* копия: ryegrass `{min:9, target:16, optimal:24}`, creepingBentgrass `{min:11, target:20, optimal:27}` и т.д.), и её классификация статуса (`shade-engine-pure.js:1116-1132`) реально разрешает пороги по виду, а не одно слепое число. `assets/dli-recovery-bridge.js:45-66` несёт **побайтовый дубликат** той же таблицы. `assets/ambient-dli-engine.js:44-57` несёт *третью*, структурно другую копию (`SPECIES_DLI_RANGES`, 5-уровневая шкала `critical/minimum/adequate/optimal/abundant` с другими числами снова: ryegrass optimal 22 против optimal 24 канонической копии) — и её собственный комментарий заголовка самодокументирует проблему: "Copies exist in dli-recovery-bridge.js, turf-profile-controller.js, ambient-dli-engine.js. Keep in sync or consolidate." Ни одна из чисел этих трёх таблиц не совпадает с собственным брифом клиента по DLI Gilba (порог выносливости к износу для спортивного поля ryegrass 9.2, диапазон восстановления ~20; bentgrass green 3.2мм: 30) — все три являются более старой внутренней оценкой, не взятой из цитируемой первичной литературы брифа (Abelard & Galbrun 2022, Cockerham et al. 2002, Russell/Karcher/Richardson 2019).

**Часть 2 — инфраструктура, учитывающая высоту, уже существует в кодовой базе, но подключена не к тому пути.** `assets/environmental-utilisation-engine.js:1437`, `getDLIForHOC(speciesKey, hocMM, goal)` — настоящая, рабочая функция цели DLI с учётом высоты кошения (HOC) — но она потребляется только опциональной веткой LED-досветки (`assets/shade-engine.js:611-625`, зависит от `hocConfig && hocConfig.hocMM && typeof GSSH_EUE !== 'undefined'`), никогда основным путём вердикта, который реально производит панель дашборда "LIGHT (DLI) ... Optimal" (`assets/growth-light-analysis.js:637,1227-1240`, которая читает `shade.effectiveStatus` — вычисляется слепой-к-высоте классификацией только-по-виду `shade-engine-pure.js` на строках 1116-1132). Так что реальное вычисление с учётом высоты существует и достижимо в некоторых контекстах, просто отключено от единственной поверхности, о которой идёт речь в доказательствах аудита. Именно поэтому буквальный заголовок аудита "не ветвится по виду" не совсем точен (он ветвится), но его практический вывод (грин bentgrass и фервей bentgrass сейчас получают идентичные вердикты) полностью подтверждён независимо. Никакой структуры двух порогов удержание-против-восстановления не существует ни в одной из трёх таблиц, и никакой поправки на сезонность или буфер поля нигде не существует.

**Предложенное решение — это решение по модели данных, которое разделяют D13b/D24/D48 (Решение 5), плюс подлинный агрономический вопрос об источниках, на который клиент уже частично ответил, плюс консолидация по хозяйству, которую выявил этот проход проверки.** (1) **Консолидировать три дублирующие таблицы вид-DLI в один канонический источник** (копия `shade-engine-pure.js` — та, которую собственные комментарии называют канонической) прежде чем что-либо расширять — добавление измерения высоты к трём независимо расходящимся таблицам утроило бы работу и риск расхождения. (2) Заменить эту единую консолидированную таблицу структурой поиска вид-по-высоте, которую указывает клиент, взятой из уже предоставленного им брифа по DLI Gilba — это не изобретение чисел, клиент предоставил цитируемый источник (P3, Spencer 2026) с заявленными уровнями доверия по каждому порогу (среднее по цифрам высоты спортивного поля, среднее по цифре гринов bentgrass, опирающейся на одно рецензируемое исследование, **низкое конкретно на ветке высоты**, поскольку "не существует ни одного холодносезонного факториала DLI-по-высоте-кошения нигде", согласно собственному брифу клиента). (3) **Подключить существующее значение `getDLIForHOC`/HOC в основной путь классификации `effectiveStatus`** (`shade-engine-pure.js:1116-1132`), а не оставлять его изолированным в ветке LED-досветки — это большая часть "добавить высоту" бесплатно, поскольку вычисление с учётом высоты уже существует, его просто нужно перепроложить к поверхности, на которую смотрит клиент. (4) Пустые ячейки (любая комбинация вид/высота, которую бриф не покрывает — клиент называет холодносезонный fairway/tee высотой 10-15мм как один такой пробел для каждого вида) возвращают четвёртое состояние вердикта D13b вместо интерполированной догадки. (5) Структура двух порогов удержание-против-восстановления и поправка на сезонность — реальные расширения области сверх работы по таблице/подключению выше — подтвердить с клиентом, нужны ли оба в этом проходе (Решение 6).

**Трудоёмкость и радиус поражения:** больше, чем "добавить колонку в одну таблицу", как только учтены находки о дублировании и отключённом пути HOC, но также дешевле в одном отношении — математику с учётом высоты (`getDLIForHOC`) не нужно писать, только перепроложить, а консолидация реально уменьшает площадь поверхности (три таблицы становятся одной). Затрагивает `shade-engine-pure.js` (каноническая таблица + классификация), `dli-recovery-bridge.js` и `ambient-dli-engine.js` (удалить их дублирующие таблицы, читать консолидированную вместо них — проверить также `turf-profile-controller.js`, названный в собственном комментарии `ambient-dli-engine.js` как возможная четвёртая копия, ещё не подтверждена независимо), и `shade-engine.js` (прокинуть HOC в основной путь, не только ветку LED). Это разделяет контракт четвёртого состояния вердикта с D13b/D24 — построить этот контракт один раз (Решение 5), и этот его потребляет. **Зависимость от цитаты, которую флагирует сам клиент**: цифра bentgrass 30 моль/м²/день (Russell, Karcher & Richardson 2019, *Crop Science*) подтверждена собственным тезисом авторов 2017 года, но сама первичная статья не получена/за платным доступом — подтвердить доступ до того, как это будет выпущено как цитируемое, готовое к отгрузке число, согласно собственному замечанию клиента, что это "переходит из пункта наведения порядка в архиве в зависимость сборки" после написания кода.

**Тест:** утверждение 28 точно (25.4 моль/м²/день возвращает Optimal на спортивном поле ryegrass, флаг ниже-порога на грине creeping bentgrass при 3.2мм).

### D32 — реаппликация PGR срабатывает при критически низком GP; прогресс ограничен без раскрытия, S2, Tier A

> Two defects in one panel: a reapplication trigger that does not gate on GP, and a progress card capped at 100% that hides the extent of the overrun.

**Что код реально делает сегодня.** Триггер реаппликации: `assets/gilba-pgr-module-v3.js:1685` и `:1794`, `reapplicationStatus: progress >= 0.75 ? 'due' : progress >= 0.60 ? 'approaching' : 'active'` — чисто функция от отношения накопления GDD, никакого члена GP нигде ни в одном тернарном операторе, ни в окружающей области видимости функции. **Поправка к формулировке аудита по ограничению прогресса**: потолок в 100% — это **намеренный, прокомментированный дизайнерский выбор**, а не недосмотр — `assets/plan-ui.js:802-807`, `// Cap at 100 — same as renderPGR; "Reapply now" badge communicates overdue state`, `clamp(pgrPct, 0, 100)`. И критически, **сырые неограниченные цифры GDD уже печатаются на той же карточке**, без ограничения, прямо рядом с ограниченным процентом: `plan-ui.js:809-810,816`, `pgrAccum + ' / ' + pgrThresh + ' GDD'` (541/350, совпадает с собственной скорректированной цифрой аудита точно) — так что исходные данные для вычисления перерасхода 155%/191-GDD уже видны на карточке, просто не предвычислены в заголовочный процент.

**Предложенные решения.**
1. **GP-gate (согласованная форма фикса, агрономический параметр уже установлен — тот же порог 50%, что D24).** Добавить тот же gate `CULTIVATION_GP_FLOOR`, который D24 вводит, в тернарный оператор статуса реаппликации, точнее — подавлять состояние бейджа "apply now"/"overdue" конкретно (не лежащее в основе отслеживание GDD, которое должно продолжать работать), когда GP ниже порога, соответствуя форме фикса клиента ("гейтовать реаппликацию PGR на том же пороге GP, что D24"). Переиспользовать именованную константу D24 вместо дублирования.
2. **Отображение прогресса, инженерное решение (не агрономическое).** Учитывая, что сырые цифры GDD уже видны, есть два реальных варианта: (a) снять ограничение с самого процента (показывать "155%"), что меняет визуальный вес пары номер/бейдж, вокруг которой был спроектирован намеренный потолок (собственное обоснование комментария кода — "бейдж сообщает состояние просрочки" — нужно было бы переоценить, если процент тоже снимет ограничение, поскольку тогда и число, и бейдж говорят одно и то же, что может быть желательным или может быть избыточным); либо (b) держать процент ограниченным точно как спроектировано, но добавить явную строку "191 GDD over" или "55% over threshold" рядом с ним, удовлетворяя реальную претензию клиента (скрывает степень перерасхода) без изменения существующего дизайна ограниченный-процент-плюс-бейдж. Рекомендуется (b) — это аддитивно, сохраняет рабочий, намеренный дизайнерский выбор, и напрямую отвечает на то, что клиент реально просит (видимость масштаба перерасхода, не обязательно неограниченное число).

**Трудоёмкость и радиус поражения:** (1) небольшая, разделяет константу с D24 (оформить последовательно или сразу после). (2b) небольшое добавление к той же карточке. Низкий риск регрессии для обоих — (1) только подавляет состояние бейджа при новом условии, (2b) только добавляет текст.

**Тест:** утверждение 24 точно (реаппликация PGR не срабатывает ниже порога GP 50%).

---

### D50 — альтернативы в FRAC-группе, когда препарата нет на складе, S4, долгий ящик

   **Сначала решение, чтобы его не переоткрывали. Ротация показывает один препарат на FRAC-группу и не больше четырёх шагов — это намеренно, подтверждено владельцем 16.09.2026.** Довод агрономический: для управления устойчивостью важна смена **группы**, а не торгового названия, поэтому второй препарат той же группы ротации ничего не добавляет. История это подтверждает: правило стоит с первого коммита и ни разу не менялось — менялись только классификация и проверка последовательностей (GH-188, GH-197, GH-200).

   **Почему запись вообще появилась.** При работе над вопросом 26 выяснилось, что три препарата — Taratek 5F, Clarity, Fostonic — не встречаются ни в одной из восемнадцати ротаций, и четыре теста про них не могли упасть. Разбор показал, что дело не в данных о продукте и не в регистрации: например, Fostonic — законный кандидат на питиум, но группу P07 первым занимает Phosgard. Недостижимость создаёт правило отбора, а не свойства препарата. Тесты с тех пор **утверждают** эту недостижимость строкой, а не описывают комментарием: они упадут в день, когда препарат войдёт в ротацию, и это сигнал перечитать их вторую половину.

   **Что отложено в долгий ящик.** Когда у агронома не окажется препарата на складе и он запросит альтернативу — показать других кандидатов той же группы. **По запросу агронома, а не сейчас**: сегодня молчаливое отбрасывание остальных кандидатов признано верным поведением.

   **Чего здесь нет:** это не про молчаливость отбора как дефект. Отбор молчит намеренно; вопрос был только в том, знает ли об этом читатель ротации, и ответ — ему достаточно знать группу.

### D49 — пространство `gssh` (стадион) осталось на старом механизме записи конфига целиком, S3, долгий ящик

   Заведено как Вопрос 25 16.09.2026, перенесено сюда 16.09.2026 решением владельца: стадион сейчас не нужен, держать его в открытых вопросах незачем.

   Решения по GH-439 переводят конфигурацию сайта на запись намерений: сервер принимает изменённые поля, а не объект целиком, и браузерная копия перестаёт быть источником записи. Область этой работы — namespace `gaip`. Для `gssh` маршрут `PUT config/gssh` остаётся прежним, то есть тем самым механизмом, который на `gaip` привёл к сбросу конфигурации живого сайта в дефолты. **Почему отложено:** стадионная часть пишется другими файлами (`site-setup-wizard`, `gssh-*`), клиентской базы на ней сейчас нет, и втягивание её в план GH-439 растянуло бы работу без выгоды для тестирования у клиента. **Что делать:** отдельный, меньший план после GH-439 — по образцу принятого, но с проверкой, действительно ли `gssh` пишется тем же способом. Аналитик отметила это как рекомендацию (решение 7 в `PLAN-GH439-site-config-db-only-RU.md`).

   **Что изменилось после закрытия GH-439.** Namespace `gaip` переведён на запись изменений через хелпер с блокировкой строки; `gssh` остался на замене колонки целиком и в область работы не входил. Это не та же уязвимость, что чинил GH-439 (патч там не теряется, потому что слияния нет), но и не защищено от устаревания: выигрывает последний писатель, и более старый расчёт может лечь поверх нового. Условие безопасности — писатель шлёт полный объект.

   **Когда браться:** когда стадион понадобится. Отдельный, меньший план по образцу принятого; первым делом проверить, действительно ли `gssh` пишется тем же способом, что писался `gaip`.

   **Ещё два расхождения той же страницы, найдены 18.09.2026. Держим здесь, чтобы стадионное лежало в одном месте.**

   **Первое — единственная страница, где виден неверный текст.** `stadium.blade.php:141` подключает партиал старого хаба прямо в `main class="content content-wide"`, без скрывающей обёртки; на трёх страницах отчётов тот же партиал лежит внутри `div id="rp-hub-runner"` со `display: none !important`. Поэтому блок «Enter monthly N rate in the Climate & Growth section» (`hub-tissue-v3.js:5705`) виден только здесь, вместе с эмодзи. Раздела Climate & Growth в новом хабе нет. Разбор самого поля — Вопрос 30, отложен решением владельца.

   **Второе — здесь работает другая копия расчёта ростового потенциала.** `mlsn-progressive-disclosure.js` на `stadium.blade.php` не подключён вовсе (проверено: ноль вхождений), поэтому имя `calcMixedGrowthPotential` достаётся версии из `hub-tissue-v3.js`, а на остальных четырёх страницах партиала — версии из `mlsn-progressive-disclosure.js`, объявленной позже и выигрывающей глобальное имя. Возвращают они поля под разными именами. Замерено чтением разметки и порядка загрузки, страницы не открывались.

   **Оба — про одну страницу и оба не про запись конфига.** Когда стадион понадобится, они входят в ту же работу.

## Список решений

Ссылаться на них так: **«Решение 2»**. Нумерация своя, с «Вопросами» ниже не связана.

Каждый вопрос ниже нуждается в решении клиента (или назначенного им агронома), их можно задать одним раундом. Это вопросы **к клиенту**; решения, принятые самим пользователем по ходу работы, живут не здесь, а в списке работ ниже и в `PLAN-D31-unify-engines.md` (раздел 9) — там же записаны последствия каждого в цифрах.

2. **D25 — Base Saturation для образцов методологии AA.** Текущее подавление (b35fix438/439) основано на цитируемой литературе (Kopittke & Menzies 2007), аргументирующей, что %BS/BCSR не валидирован для газонной травы — тот же самый аргумент, который сам клиент использует, чтобы убрать собственную цитату Carrow из D07 в v5.2. Хочет ли клиент восстановить Base Saturation для образцов AA (как буквально просит его решение D25), или он согласен с более поздним, цитируемым решением кодовой базы упразднить его конкретно для AA, учитывая его собственный заявленный скепсис к BCSR в другом месте этого документа? (Sodium следует добавить в простую панель почвы независимо от этого ответа — эта половина не под вопросом.)
3. **Порог накопления OM в D17.** Какой %OM (на образце, классифицированном как песок) должен запускать предупредительное повествование о поверхностном запечатывании/инфильтрации/чёрном слое? Числа изобретать не следует — нужна цифра клиента.
4. **Год цитаты MLSN в D21.** Woods, Stowell & Gelernter опубликовали и руководство PACE Turf 2014, и статью PeerJ Preprints 2016. Является ли одна из них канонической цитатой для реестра, или обе легитимно цитируемы для разных утверждений (например, 2014 для исходного руководства, 2016 для пересмотренной/препринтной версии)?
5. **Общий контракт модели вердикта D13b/D24/D48.** Этим трём нужно четвёртое состояние классификации ("измерено, недостаточная калибровка для оценки" / "ниже порога восстановления" / и т.д.), добавленное в общую схему диапазона/вердикта. Подтвердить, что клиент хочет, чтобы это было построено как одно общее изменение контракта (рекомендовано, и соответствует его собственной инструкции оформить их вместе), а не три отдельных специальных добавления.
6. **Область D48 для этого прохода.** Выпустить поиск DLI вид-по-высоте отдельно первым (закрывает продемонстрированный случай провала на грине bentgrass), или включить структуру двух порогов удержание-против-восстановления и поправку на сезонность в ту же сборку? Сам бриф оценивает ветку высоты как "низкое доверие" — стоит подтвердить аппетит к выпуску числа низкого доверия сейчас против ожидания.
7. **Зависимость от цитаты D48.** Russell, Karcher & Richardson (2019) недоступна/за платным доступом, подтверждена только собственным тезисом авторов от конференции 2017 года. Подтвердить доступ к первичной статье до того, как цифра bentgrass-30 выйдет как цитируемое число, или согласиться на выпуск на уровне подтверждения тезисом, с указанием этой оговорки в экспорте.
8. **Режим отказа D23.** Если версию хаба не удаётся разрешить во время экспорта, должен ли экспорт полностью прерываться (буквальное требование клиента) или печатать честную строку "недоступно" (рекомендованная альтернатива, избегает превращения пробела в метаданных в клиентский сбой экспорта)?
9. **Метка болезни D29.** "Crown Rust" (с указанием вида, соответствует собственному предложенному фиксу аудита) или "Rust" (соответствует конвенции существующего рендерера чипов на экране для того же ключа)? Низкие ставки, нужен единый выбор, чтобы обе поверхности были согласованы.
10. **Подтверждение области D33.** Подтвердить, что рекомендованный переход от бинарного флага `release: 'slow'/'standard'` к проценту `slowReleaseFraction` приемлем — это изменение модели данных, не только изменение заполнения данных, и клиент должен подтвердить его до того, как оно будет считаться "уже согласованным", учитывая, что оно выходит немного за пределы его буквальной формулировки формы фикса ("добавить атрибут характеристики высвобождения").


**Закрытые решения — след, в работу не берутся.** Номера сохранены, потому что на «Решение 1» ссылаются таблица закрытых тикетов и разбор D07/D07a выше.

1. ✅ **РЕШЕНО — реализовано в GH-369. Направление tissue gate (остаток D07a).** На сайте, где ткань K находится *ниже* достаточности, текущий gate понижает вынос K (110→46 kg/ha при N=200, на собственной фикстуре Test5-NZ этого репозитория). Отчёт отдельно печатает "Tissue K critically low... apply foliar potassium immediately". У движка есть член коррекции с почвенной стороны, но вообще нет члена коррекции со стороны ткани. Должно ли показание ткани ниже достаточности добавлять коррекцию *подъёма* (зеркалируя логику ниже-floor с почвенной стороны), а не только управлять соотношением выноса? Это единственный открытый пункт, перенесённый из в остальном закрытой работы D07a.

11. ✅ **РЕШЕНО 09.09.2026 — выбран Вариант 1, реализовано в GH-383…GH-389. Структурное решение D31.** Вопрос стоял так: упразднить независимую арифметику `nutrition-requirement-engine.js` и провести обе поверхности через одно вычисление (Вариант 1), или согласовать константы двух движков, оставив обе реализации отдельными (Вариант 2). Выбран Вариант 1. Попутно выяснилось, что Вариант 2 закрыл бы меньше половины проблемы: из пяти найденных расхождений только два были дублированной арифметикой (база азота, pH-лестница), а три — разными **входными** данными при одинаковой формуле (регистр методологии, флаг клиппинга, источник годового азота). Поэтому вместе с общим ядром сделан и общий сборщик входов `nutrition-program-inputs.js` — без него объединение формулы ничего бы не гарантировало. Сопутствующие решения по агрономии (подъём до минимума MLSN, таблица выноса по видам, pH-лестница для MLSN, поведение на потолке) перечислены в `PLAN-D31-unify-engines.md`, раздел 9, вместе с последствиями каждого в цифрах.

---

## Технические открытые вопросы

Ссылаться на них так: **«Вопрос 7»**. Это отдельная нумерация — не путать со «Списком решений» выше, где свои номера («Решение 2»), и с «Порядком сборки» ниже, где номера означают очередь работ, а не тему. Три списка, три независимые нумерации: совпадение номеров ничего не значит.

Не из аудита клиента — найдены нами по ходу работы. Это единственный список: отдельный файл `REVIEW-GH349-onward.md`, где они жили раньше, удалён, а его содержательная часть перенесена сюда.

**Вопрос 1. Синхронизация удаляет лабораторные образцы при обычном добавлении пробы — ВОСПРОИЗВЕДЕНО ВЖИВУЮ 10.09.2026, самое серьёзное на проекте.** Прежняя формулировка («устаревшая вкладка», нужны два устройства) **оказалась неверной по триггеру**: механизм описан правильно, но условия куда шире. `POST /api/samples/sync` работает как «вот моя полная картина, приведи базу в соответствие», и `reconcileMissingSnapshotSamples()` (`SampleController.php:427-457`) мягко удаляет всё, чего нет в присланном снимке. Ключевое, чего мы не знали: **снимок из браузера — это не то, что во вкладке**. Образцы, восстановленные с сервера, клиент собирает с полем `values` (`sample-persistence.js:429-450`), а `sync()` читает только `rawData` (`SampleController.php:171-174`) — такие образцы в keep-list не попадают вовсе. **Живая репродукция на dev-стенде: одна вкладка, один пользователь, страница `/analysis`, добавить один образец на сайте, где уже есть образцы → сервер ответил `synced: 1, deleted: 14`.** Тринадцать существующих образцов сайта «New test - location» удалены одним запросом. Второго устройства не нужно, устаревшей вкладки не нужно — достаточно обычного действия. То же самое даёт любое изменение образца, импорт из лаборатории и Capture. **След уже есть в базе:** 86 из 144 строк помечены удалёнными, из них 66 по четырём сайтам с одинаковой отметкой времени и без единого upsert — почерк того же механизма. **Направление «мержить по отметкам времени», записанное раньше, отвергнуто планировщиком:** это по-прежнему удаление по отсутствию и не переживает ни постраничное восстановление, ни ограничение в 200 записей. Правильно — сервер источник истины, клиент никогда не утверждает отсутствие, а удаление идёт явными записями об удалении с версией. Стадия 0 (убрать reconcile) закрывает потерю данных за день. Полный разбор, стадии и семь решений — в `PLAN-samples-sync-FINAL.md`.

   **Объём и частота, замерено 18.09.2026 при сдаче пяти починок; в этом документе числа
   прогонов за обход не было.** `POST /api/samples/sync` шлёт весь клиентский срез проб —
   **23 КБ, трижды за обход трёх страниц**. Это замер: три страницы — три полных среза.
   Механизм записан выше, план по нему есть; новое здесь одно — сколько раз он срабатывает
   за обычный обход.

   **ЧЕМ ВЫЗВАН КАЖДЫЙ ИЗ ТРЁХ ПУШЕЙ — НЕ ИЗМЕРЕНО.** Загрузкой страницы или событием, и
   каким именно. Координатор написала здесь вывод «частота привязана к загрузке страницы, а
   не к действию пользователя» — вывод снят 18.09.2026 как не следующий из замера: возможен
   и другой ход, при котором каждая загрузка порождает событие смены площадки, и тогда
   частота привязана к событию, а страница лишь его повод.

   **Разница меняет место починки:** привязано к загрузке — чинить инициализацию; привязано
   к событию — чинить список событий.

   **Чем разрешается:** журналом с причиной у каждого вызова. Причина туда уже передаётся —
   `sample-persistence.js:247`, `function scheduleSave(reason)`, и на `:700` вызов идёт как
   `scheduleSave(MUTATION_EVENTS[eventName])`, то есть имя события у причины есть. Нужен
   прогон, печатающий `reason` на каждом срабатывании.
   ✅ **РЕШЕНО 10.09.2026: удаляем кэш, а не чиним синхронизацию. Офлайн-работа не нужна и не планируется.** Локальное хранилище — не спроектированный кэш, а исходное хранилище плагинной эпохи, когда базы данных не было вообще; синхронизацию с MySQL добавили поверх него, а не вместо. Аргументов за него не осталось ни одного, проверено: офлайн не поддерживается и никогда не поддерживался (любое сохранение обязано дойти до сервера, иначе поднимается ошибка), загрузку кэш не ускоряет (страницы и так ждут API), и в новом хабе его не читает никто — ни `/data`, ни `/plan`, ни `/dashboard`, ни `/settings`. Остались только legacy-места.
   **Что это меняет в объёме работ — она уменьшается.** Стадия 0 (убрать удаление по отсутствию) остаётся первой и самостоятельной. Дальше вместо переделки `samples/sync` под версии и явные удаления страницы хаба переводятся на **уже существующие поштучные маршруты** страницы Data (`POST` / `PATCH` / `DELETE`); не хватает только `DELETE /api/samples/{id}` — сегодня удаление в хабе идёт исключительно через снимок. Сам `sync` остаётся **только для импорта в настройках**. Версии и разрешение конфликтов становятся необязательными — откладываются до появления второго одновременного пользователя. Кэш удаляется целиком: восстановление из localStorage, запись `gilba_samples`, ветка образцов в `hub-persistence.js`, штамп текущего сайта в `dashboard-ui.js` (заменяется чтением из конфига) и чистка при импорте в настройках.
   ✅ **РЕШЕНО 10.09.2026: журналы при импорте удаляются насовсем, обратимость не делаем (отменяет D-3 плана).** Импорт — это односторонний перенос из старого хаба, где выгрузка была; в новом хабе выгрузки нет и не планировалось. Импорт заменяет сайт целиком, «создаёт его заново», поэтому стирание журнала опрыскиваний и полевого — задуманное поведение, а не потеря. Круговорота «выгрузил-загрузил» не существует, и журналов в пакете нет потому, что в старой системе их не было. Это снимает пятикратный рост объёма, который ревьюер насчитал на реализацию мягкого удаления (моделей для этих двух таблиц нет вовсе, шестнадцать мест с прямыми запросами, а у полевого журнала жёсткое ограничение уникальности — мягко удалённая строка заблокировала бы повторную запись того же ключа). Остаточная мелочь, отдельным пунктом не заводим: импорт идёт в **активный** сайт, и если запустить его на сайте, где уже ведут журналы, они исчезнут без замены; интерфейс показывает, куда импортирует и что придёт, но не что сотрётся.
   ✅ **РЕШЕНО 10.09.2026: сервер не воскрешает удалённые записи.** `saveSampleRecord()` (`SampleController.php:358-368`) ищет запись **включая удалённые** (`withTrashed()->firstOrNew()`) и, найдя мягко удалённую, вызывает `restore()`. Это тот же плагинный след: в старой модели появление записи в присланном снимке означало «она существует», клиент был источником истины. При базе как источнике истины входящая запись — это создание или обновление живой строки; удалённая остаётся удалённой. Препятствий нет: на `samples` обычный индекс `samples_site_type_client_uid_idx`, а не уникальный, так что новая строка с тем же ключом создаётся свободно. Класть **не в стадию 0** — та должна остаться в один метод, чтобы откатываться одним движением, — а в стадию 1, вместе с появлением настоящего маршрута удаления: там собирается вся семантика («удалять по явной команде, не удалять по отсутствию, не воскрешать по совпадению»). Попутно снимает вопрос, который планировщик оставлял открытым на будущее: после появления «Недавно удалённых» повторная отправка не вернёт то, что пользователь удалил.
   **Поведение при сбое:** если запрос к серверу не прошёл — показываем сообщение, что пробы не загрузились, и предлагаем обновить. Никогда не подставляем старое из браузера. Это то же правило, что уже действует в проекте: лучше сказать, что данных нет, чем посчитать на заглушке.
   **Отдельно, не путать со стадией 0:** на `/reports/export`, `/reports/scenarios` и `/reports/forensic` (и на `/hub`, но он не в счёт) старая персистенция через 200 мс достаёт образцы из localStorage, не дожидаясь ответа сервера, и при совпадении ключей **оставляет локальную версию**. То есть Word-отчёт может быть молча построен по устаревшей браузерной копии проб. Это **другой** дефект — про устаревшие данные, а не про их потерю, — и удаление кэша закрывает его попутно. Успевает ли сервер за 200 мс, не измерено: на локальной машине скорее да, через реальную сеть скорее нет.

**Вопрос 2. Программа не сбрасывается при смене расчётных входов, кроме координат и вида травы.** GH-371 сравнивает координаты, GH-377 — вид травы и методологию. Но `computeProgram()` зависит и от других входов, изменение которых сохранённую программу не трогает. **С GH-394 появился конкретный пример, и он больше не гипотетический: расписание трафика.** Оно намеренно не считается программой (иначе дрейф координат сносил бы его вместе с программами), поэтому его изменение сохранённую программу не сбрасывает. Пользователь меняет расписание с «1 матч» на «4 матча», ожидает роста нормы азота на 30% — и продолжает видеть старые цифры, пока не нажмёт Generate. Это уже не «риск», а воспроизводимое поведение, и решать его надо вместе с общим вопросом: какие входы должны инвалидировать программу и как показывать, что она устарела.

   **Второй повод устареть — наш собственный деплой. Добавлено 10.09.2026 по итогам GH-403.** Тот же механизм, другой источник: программа устаревает не от действий пользователя, а от того, что мы поменяли движок. Проверено по базе после GH-403: калий в сохранённых программах лежит целыми числами — Canberra и Russley 110, test4 - USA 119, Test6 - UK 107, Westview 125, Test1 - Sports 67. Это значения, посчитанные ещё тем округлением до целого килограмма, которое GH-403 убрал; сегодняшний движок для Canberra даёт **106.7** против лежащих **110**. Пересчиталась только Test5 - NZ (136.1), потому что её программу перегенерировали при проверке. Страница показывает сохранённую программу, а сбрасывается она сравнением координат, вида травы и методологии — версия движка в это сравнение не входит. **Эта половина опаснее пользовательской:** про смену расписания пользователь хотя бы знает, что что-то поменял, а про наш деплой он не знает ничего и повода нажать Generate у него нет. Пока живых клиентов нет, цена нулевая — данные вносятся заново. Но правило устаревания нужно завести до первого клиента, иначе каждое наше исправление расчёта будет доезжать не до всех. **Решать вместе с первой половиной, одним правилом:** что именно делает программу устаревшей — список входов плюс версия движка, — и что видит пользователь, когда она устарела.

**Вопрос 5. На Burns не печатается раздел сверки калия.** После GH-387 программа на этом поле калий не даёт, механизм видит ноль доставки и убирает раздел целиком. По своей логике верно, но клиент не видит ни слова про калий на поле, где калия в почве 195 ppm при потолке 55.5. Нужно продуктовое решение: показывать раздел с нулём и объяснением или оставить как есть. **Вынесено за этот релиз, решение пользователя 10.09.2026** — в релизный список не входит, вернуться после сдачи. Причина отсрочки: это не неверная цифра и не потеря данных, а вопрос полноты изложения, и решать его лучше, когда клиент увидит документ целиком и скажет, чего ему в нём не хватает.

**Вопрос 6. Все три формы Settings перезаписывают конфиг сайта целиком из возможно устаревшей копии.** Формы Site, Turf и (с GH-394) Traffic делают PUT всего объекта из `D.gaipConfig`, который мог быть прочитан давно. Программные ключи защищены на сервере, а вот `appliedMonthlyN`, `maxNPerMonth`, `nzDistributor` — нет: при двух открытых вкладках их можно затереть. Не внесено GH-394, это давняя семантика всех трёх форм; GH-394 лишь добавил третью форму с той же экспозицией. Требует решения о семантике PUT, а не точечной правки. **Поглощён работой по GH-439, решение владельца 16.09.2026: конфигурация переезжает в базу, браузер копию не пишет.** Принят полный план (`PLAN-GH439-site-config-db-only-RU.md`), пять стадий, 11–13 дней плюс живые проверки; отвергнут вариант минимальной починки (`PLAN-GH439-minimal-guard-RU.md`), оба лежат рядом. **Стадия 0 сдана и проверена 16.09.2026:** сервер принимает изменение через `PATCH …/config/gaip`, на старом PUT стоит гвард — отсутствующие ключи переносятся из базы, обнулённые identity-поля восстанавливаются, попытка пишется в журнал; `POST /api/sites/sync` больше не пишет имя и не создаёт сайты; таймзона выводится из координат. PHPUnit 166, Jest 2584, живой прогон 12/12. **Чего стадия 0 не закрывает:** снимок страницы несёт непустые значения (`turf.nProgram`, методология из устаревшей вкладки, дефолты формы PGR), и сервер не отличает их от намеренной правки — три сценария живого теста остаются красными намеренно, до стадий 1–2. Базовый срез базы до начала работы — `GH-439-baseline-dev-2026-09-16-RU.md`. **РАБОТА ЗАКРЫТА 16.09.2026: все шесть стадий сделаны и проверены.** Стадия 0 — сервер принимает изменение через `PATCH`, на старом `PUT` гвард; стадия 1 — писатели нового хаба шлют изменение, а не состояние; стадия 2 — браузер перестал писать вовсе, фоновые записи и снимки DOM удалены; стадия 3 — локальная копия удалена, старые маршруты закрыты; 4a — автоматический ремонт испорченных строк; 4b — мастер решает только по базе. Обход девяти клиентских страниц обычной загрузкой: мастера на настроенном сайте нет, пустых экранов без объяснения нет, расчёта на умолчаниях нет, чужих значений нет, за весь обход ни одной записи конфига. На dev починено 12 записей мастера и 6 таймзон, сверено со срезом двумя независимыми проверками. **Осталось от этой формы одно:** `POST /api/samples/sync` с клиентской коллекцией на четырёх страницах из девяти — это Вопрос 1, план по пробам, и он закрывается там. **Два дефекта, внесённых самой работой и закрытых в ней же:** гонка при записи патчами (Вопрос 27) и фолбэк штампа координат, открывший дыру в защите GH-371. **Одна работа оказалась лишней:** половина GH-441 с инжектом `GAIP_WIZARD_CONFIG` на db-shell-страницы закрывала несуществующий дефект — мастер `/hub` на этих страницах не инициализируется вовсе, а у db-shell свой мастер, решающий по виду травы из базы; источник ошибки — предпосылка плана, аналитик назвала его своим. Половина с конфигом сайта нужна и осталась.

**Замерено ревью 16.09.2026, шире, чем считалось: порча переходит между сайтами.** В теле фонового PUT по активному сайту пришли вид травы и методология **другого** сайта — значения копии, которая была ключом на него; сайт был засеян `mlsn` / Creeping Bentgrass, а получил `slan` / Perennial Ryegrass. Гвард стадии 0 этого не останавливает: поля непустые, и от намеренной правки они неотличимы. Достаточно одной загрузки hub-страницы. Закрывается стадией 2, когда страницы перестают слать снимки. **Отдельно проверено:** держанная копия заменяется ответом сервера только при более позднем `savedAt` у сервера; копия с более поздней отметкой проходит мимо этого условия, а пуш отложен на 2 секунды — закрытая вкладка или упавшая сеть оставляют местную копию впереди.

**Вопрос 7. Три разных правила разрешения методологии сосуществуют.** Календарь считает голое `'mlsn'` заглушкой хранилища и пропускает его; общий сборщик входов принимает его как осознанный выбор; `getMethodology()` для новозеландских сайтов сворачивает всё, кроме SLAN, в ammonium acetate. Не выровнено сознательно: оба очевидных выравнивания неверны, а настоящий блокер в том, что система не отличает выбранное значение от умолчания. GH-393 поставил тесты-ловушки на все три правила, чтобы расхождение нельзя было переставить незаметно. GH-395 снял единственное живое последствие, убрав MLSN из списка на NZ-сайтах.

**Вопрос 8. Методология хранится в двух местах — разбор отложен.** Колонка `sites.methodology_override` не заполняется интерфейсом (пуста у всех сайтов), но её читает создание пробы, поэтому все пробы штампуются умолчанием аккаунта. Сегодня латентно: экспорт берёт методологию из конфига сайта, а единственный другой читатель штампа ставит его позади живого значения. Полный разбор и план работ — в `PLAN-methodology-storage.md`, чтобы не выводить заново. Пользователь отложила переработку 10.09.2026: реальных клиентов нет, а на цифры это не влияет.

**Вопрос 10. Британская интеграция утраивает количество удобрения при медленном высвобождении — пока не чиним, решение пользователя 10.09.2026.** Продукт, работающий три месяца, кладётся строкой в каждый покрытый месяц **с полной нормой** (`nutrition-uk-fertiliser-integration.js:485-515`: `rateKgHa: rate`), а делится только объявленное питание (`rate × процент / число месяцев`). Поэтому питание считается верно — Test6 - UK 199.8 кг азота, — **а количество продукта утроено**: три строки по 40 кг/га там, где внесли один мешок. Таблица закупки закажет втрое больше удобрения, и это заметнее любой расчётной погрешности, потому что за это платят. **Два других региона это уже решили, разными способами:** Новая Зеландия кладёт в покрытый месяц строку-пометку «Covered by …» **без продукта и без нормы** (`prebbles-products.js:1745`); Австралия просто не добавляет гранулированный продукт, если медленное высвобождение уже покрывает больше половины потребности месяца (`au-fertiliser-products.js:6111`). В британском файле понятия «покрыто» нет вообще — grep не находит ничего. То есть решение принималось дважды и до третьего региона не дошло. **Чинить не делением, а как в Новой Зеландии** — не класть продукт в покрытый месяц. Отложено сознательно: возможное направление — объединить алгоритмы подбора для всех трёх регионов разом, а не латать третий по образцу первых двух; решать это, когда дойдут руки.

**Вопрос 11. Тёрф-профиль на пробе — вторая модель расчёта, обезврежена, судьба не решена.** У пробы может быть свой профиль: тип покрытия, вид травы, сорт, подсеянный вид. Когда режим «несколько тёрфов» включён, выбор такой пробы **подменяет настройки сайта** её значениями, и дальше всё считается по этой траве — ставки выноса, потенциал роста, подбор продуктов. Задача, ради которой это делали («грин на полевице, фервей на райграсе, а сайт один»), **уже решена другим способом** — отдельным сайтом на каждую поверхность, со своими настройками. То есть это второй ответ на вопрос, на который уже ответили. Механизм спит: ни на одном сайте режим не включён, профилей в базе ноль, тумблер подключён только к `/hub` и трём страницам отчётов, а в `/settings` нового хаба его нет вовсе — то есть увидеть или выключить режим из рабочих страниц невозможно. **Опасность была не в функции, а в тихом включении:** импорт CSV с колонкой вида травы включал режим сам, без спроса (b35fix371). Клиент, загрузивший лабораторный файл с такой колонкой, получил бы расчёт, идущий по пробе вместо его настроек, и никакого способа это заметить. Наши собственные шаблоны импорта колонки вида не содержат, так что достижимо это было только вручную дописанным файлом. **Автовключение убрано в GH-402**, профиль на пробу по-прежнему записывается (инертен, пока режим выключен, и данные стоит сохранить, если функцию оставим). **Решить: оставить модель — тогда нужен тумблер в настройках нового хаба, модалки на рабочих страницах и перенос профиля на сервер; или удалить её вместе с профилями.** Пока не решено, механизм безопасен, но и не работает.

**Вопрос 12. Мёртвые функции подбора в австралийской интеграции — удалить попутно, чтобы не забыть.** `calculateLiquidApplication()` (`au-fertiliser-products.js:5815`) и `selectGreensSpoonfeed()` (`:5697`) не вызываются ниоткуда. Первая опасна тем, что выглядит живой: я сама сослалась на неё в задании к GH-400 как на путь подбора жидких, и разработчику пришлось меня поправлять — настоящий путь называется `selectFoliarNitrogen()`. То есть мёртвый код уже один раз увёл работу не туда. Отдельного тикета не заводим: удалить при следующей содержательной правке этого файла — ближайшая будет по перебору фосфора (пункт выше), там и убрать.

**Сюда же переведён бывший пункт 4 — ветка одиночного экспорта в `word-export.js`.** Из нового хаба недостижима: `/reports/export` всегда идёт через комбинированный конвейер. Тот же почерк, что и у австралийских функций, — код выглядит живым и уже один раз ввёл в заблуждение сам план, где числился утраченной возможностью продукта. Ничего восстанавливать не нужно, экспорт у продукта один и работает; нужно снять ветку и перестать грузить файл там, где он больше ни для чего не нужен. Проверить при удалении, что `word-export.js` не держит ничего ещё используемого: он подключён на `plan.blade.php`, `hub.blade.php` и трёх страницах `reports/`.

**Вопрос 13. Четырнадцать мест молча подставляют широту Сиднея, когда координат нет. Найдено 10.09.2026 при разборе вопроса 3.** Когда широта не разрешается, расчёт не останавливается — он берёт зашитое число: `climate-module-v2.js:682` и `disease-integration.js:376` и `hub-orchestrator.js:1455` берут `-33.87`, `irrigation-scheduler.js:263` берёт `-33`, `hub-tissue-v3.js:385/2294` — `-35` и `-33`, плюс `gaip-scenario-engine.js:762`, `gssh-scenario-engine.js:934`, `gaip-whatif-ui.js:754`, `gssh-whatif-ui.js:750`, `event-planner-engine.js:459/460/571`. Четыре из этих файлов грузятся на `export`, `scenarios` и `forensic` — живых страницах нового хаба, не только на `/hub`. Последствие: сайт без координат получает полный уверенный отчёт — климат, риск болезней, потенциал роста, полив — посчитанный для другого места. Для британского или новозеландского поля это другое полушарие и другой сезон, и в документе об этом ни слова. Тот же класс, что D03 («никакого тихого фолбэка»), и прямое нарушение правила «сообщение вместо фолбэка».

**GH-404 закрыл вход, а не сами подстановки** — сохранить сайт без координат из Settings больше нельзя, но фолбэки на месте и сработают, если широта не разрешится по другой причине. Убирать их — отдельная работа, сознательно не начатая: правка затрагивает одиннадцать файлов и требует решения, что именно показывать вместо расчёта на каждой поверхности. В GH-404 оставлен тест, который падает, если подстановки исчезнут, — чтобы обоснование не осталось висеть, когда причина уйдёт.


**Временный блок выверки расчёта на странице плана — GH-425, для периода сверки с клиентом.**

   Под сводкой доставки на `/plan` показывается, **как посчитано каждое число**: по N, P, K пошаговая выкладка с подставленными значениями — какая ветка требования отработала, как считался подъём и на сколько лет растянут, построчный реестр доставки по продуктам с указанием, объявлено это рекоммендером или посчитано по составу, и как из всего этого вышли баланс и статус. Плюс один раз на блок — входы, которых на странице нигде не было: методология, годовой азот до и после трафика, коэффициент скашивания, потолок и режим раскладки, текстура, pH, CEC, плотность и глубина с полученным множителем ppm→kg/ha, тканевой анализ и сработал ли gate, лет на исправление, двенадцать месячных норм с поставщиком и окном климатологии, серия потенциала роста, температура почвы — каждый со своим источником.

   **Зачем.** Клиент задаёт агрономические правила, которые продукт реализует, и часть вопросов ждёт его ответа. Он не может судить о числе, не видя, как оно сложилось. Блок даёт ему указать на конкретную строку.

   **Панель ничего не пересчитывает** — читает то, что выдали движки, и сама себя сверяет: все 21 показатель сравниваются с ячейками таблицы выше, при расхождении печатается предупреждение «не совпадает, верьте таблице». Значения, которых движки не отдавали, выведены наружу аддитивно и остаются после удаления панели.

   **Блок помечен временным на экране и в changelog, удаляется двумя шагами.** В отчёт не добавлен, и тест это стережёт.

   **Что он сразу вскрыл, записано здесь, чтобы не потерялось:** методология, вид травы и тип поверхности на плане всегда читаются как «из пробы», потому что страница разрешает их до общего резолвера; источник диапазона под MLSN и SLAN помечен как «сертификат» — намеренно, чтобы не срабатывал значок «общие данные». Источник температуры почвы вывести не удалось — рекоммендер публикует только значение, блок пишет «источник недоступен».

   **Не проверено вживую:** ammonium acetate — все AA-сайты новозеландские, а открывать их было нельзя, пока шла работа по температуре; закрыто юнит-тестами на реальных фикстурах.

**Вопрос 24. Внутри диапазона достаточности требование возмещает вынос и не смотрит, где почва окажется через год. Решение пользователя 11.09.2026: оставляем, меняем только если скажет клиент.**

   Найдено пользователем на `Test - GC - NZ - delivery`, фосфор: в почве 18 ppm (25.2 кг/га) при полосе 12–28 ppm (16.8–39.2 кг/га), вынос 25. Требование напечатано **25.0** — ровно вынос, потому что почва внутри полосы. К концу года в почве останется **0.2 при нижней границе 16.8**: провалится ниже полосы, а требование этому не препятствовало. Статус «Deficit (−36%)» при этом честный, он считается от баланса.

   **Это не дефект, а граница принятого решения.** Формула «удержи пол после выноса» (GH-415, вариант B1) применяется только выше потолка. Вариант B2 — применять её и внутри полосы — замерен и отклонён: он двигает 34 строки из 91, например Russley по калию 119 → 9.5.

   **Что говорят источники, проверено 11.09.2026.** SLAN исходит из того, что у элемента есть **диапазон, внутри которого держат**; MLSN говорит только «не дай упасть ниже X» — это разные конструкции, а не два названия одного. MLSN несёт **учёт потребления за период** с самого начала, «в отличие от прежних диапазонов достаточности, у которых явного учёта периода нет» (Asian Turfgrass Center). То есть наша формула «удержи пол после годового выноса» — это метод MLSN, и переносить его на SLAN и AA значит применять к ним чужое правило.

   **И обратное тоже верно.** SLAN считает дефицит как разницу между фактическим уровнем и **оптимальным**, а не нижней границей — то есть тянет почву к середине полосы, а не к её дну. Наше нынешнее «внутри полосы возмещай только вынос» ближе к MLSN, чем к SLAN. На случае выше SLAN дал бы рекомендацию, а мы её не даём.

   **Вопрос к Спенсеру, если дойдёт:** для SLAN и AA считать дефицит от оптимума полосы, как предписывает методология, или оставить возмещение выноса. Меняет цифры на всех сайтах с этими методологиями.

   Источники: `asianturfgrass.com/post/the-time-component-of-soil-test-interpretation/`, USGA Green Section Record 63(11) о MLSN против SLAN, `nuturf.com.au` о выводе дефицита из оптимума.

**Вопрос 28. Полоса стресса сравнивает потенциал роста на тех же порогах, но отвечает на другой вопрос. Заведено 16.09.2026, решение отложено.**

   Полоса стресса сравнивает потенциал роста на тех же порогах **70 и 40**, но отвечает на другой вопрос и красится иначе: при высоком потенциале роста она подписана `Low` и использует свой зелёный, не канонический.

   **Решить:** это другая величина со своей шкалой, случайно совпавшая числами, или вторая палитра, запрещённая правилом «цветовая шкала потенциала роста только через общий модуль». От ответа зависит, входит ли она в область сторожа `gh365`.

   **Почему это не праздный вопрос.** Совпадение чисел сегодня ничего не гарантирует: правило написано ровно потому, что завтра числа могут разойтись, и тогда две шкалы на одном экране начнут противоречить друг другу. Если это законно другая величина — она должна быть явно выведена из области сторожа с причиной; если та же — её пороги обязаны идти из модуля.

   **Мнение ревьюера, 16.09.2026 — нарушением правила не считает.** Палитра там другая, а не копия канонической, и отвечает функция на другой вопрос: при потенциале роста 70 она возвращает `Low`, что как подпись самого потенциала было бы прямо неверно. Правило запрещает свою палитру **для потенциала роста**, а здесь окрашивается стресс, и инвертированные шкалы правило само выводит за скобки.
   **Но связь есть и не оформлена:** пороги 70 и 40 выписаны заново, тогда как модуль их экспортирует. Смысл функции в том, что её границы обязаны следовать границам потенциала роста, — значит при сдвиге порогов производная шкала разойдётся с основанием **молча**. Его рекомендация: палитру оставить, пороги брать из экспортируемых констант модуля (проверено, отдают 70 и 40). Таких функций в файле две, с одинаковыми порогами и цветами.

   **Найдено** при работе над вопросом 26: сканер `gh365` научился читать фолбэки по переменной, и эта цепочка оказалась на границе его области. Разработчик её назвал и решать не стал — правильно, это доменный вопрос.

**Вопрос 29. Чужой климат в отчёте — экспорт брал входы расчёта из состояния страницы. Найдено владельцем 16.09.2026, GH-459.**

   **Что она увидела.** Выгрузила отчёт по Test5 со страницы отчётов и получила чужой климат: помесячный потенциал роста в документе посчитан по правильной кривой, но на температурах другого сайта. Совпадение точное: ревьюер применил ту же кривую к ряду крайстчёрчского сайта и получил ряд документа с расхождением в единицу на двух месяцах.

   **Цепочка одна на все четыре расхождения:** другой климат → другой помесячный GP → другое распределение азота → другой подбор продуктов → другая доставка и баланс.

   **Почему этого не поймал ни один тест.** Парити-харнесс ходит тем же путём, по тому же сайту и сравнивает продукты — но не переносит в сравнение помесячный массив вовсе. А годовая сумма азота нормируется к цели независимо от формы распределения, поэтому подмена ВСЕГО климатического ряда оставляет все годовые числа на месте. Харнесс сравнивал ровно те величины, которые к этому дефекту невосприимчивы.

   **С какого времени.** Чтения существуют **с первого коммита репозитория, 28.04.2026** — проверено поиском по истории. Сегодня изменилось не чтение, а его надёжность: стадия 2 GH-439 убрала раннего писателя этих полей и сделала единственного оставшегося зависящим от сети. Замер: координаты пишутся на 327-й миллисекунде при паузе в цикле 300.

   **Чем закрыто:** экспорт берёт все входы расчёта из конфига сайта пробы, страничные фолбэки удалены, а не понижены в приоритете. Харнесс сравнивает помесячный массив — GP, температуру и азот по месяцам.

   **Правило, введённое этим случаем:** база единственный источник **и для записи, и для чтения** — то, что печатается, берётся из данных того объекта, о котором документ, а не из состояния страницы. Записано в `CLAUDE.md`. **Где сломался процесс:** правило «база единственный источник» весь GH-439 применялось к записи; чтение осталось за скобками не по решению, а потому что вопрос так не ставился. Инвентарь покрыл читателей хранилища конфига и не покрыл читателей состояния страницы.

**Вопрос 27. Гонка при записи конфига патчами — первый дефект, который мы внесли сами и который терял данные пользователя. Внесён стадией 1 GH-439, найден ревью 16.09.2026.**

   **Что видел бы пользователь.** Одно нажатие Generate: программа на экране есть, календаря в базе нет. Все запросы отвечают `200`, на странице ни слова. Человек уверен, что программа сохранена, а её нет.

   **Частота на замере:** три отказа из четырёх прогонов у ревьюера, два из четырёх у разработчика. То есть не редкий крайний случай, а больше половины попыток.

   **Механизм.** Гонка чтение-слияние-запись в `patchConfig()`: метод читает конфиг, сливает патч в PHP и пишет колонку целиком через `updateOrCreate` — без транзакции, без `lockForUpdate`, без сверки версии. Программа приходит цепочкой из трёх патчей подряд, все три обрабатываются в одну секунду, и тот, что успел первым, затирается вторым.

   **Три доказательства, которыми это отделено от похожих версий.** (1) В упавших прогонах в базе нет не только календаря, но и `maxNPerMonth`, пришедшего тем же запросом, — потерян **весь патч целиком**, а версия «штамп координат не совпал» убрала бы только программные ключи. (2) Порядок запросов в `laravel.log` совпадает с исходом: календарь идёт первым — потерян, идёт вторым — уцелел. (3) В коде отсутствие блокировки видно прямо.

   **Почему это наш дефект.** До стадии 1 конфиг уходил на сервер одним `PUT`, целым объектом, и слияния между запросами не было вовсе. Переход на патчи — правильное направление и остаётся в силе; ошибка в том, что запись разбили на несколько параллельных, а строку не защитили.

   **Чем закрыт:** транзакция с `lockForUpdate` на строку `site_configs`. Объединение цепочки в один патч рассматривалось отдельно и гонку само по себе не закрывает. Тест `S8`, поймавший дефект, ослаблять запрещено — он остаётся в том виде, в каком поймал.

   **Что проверено сверх самой починки, требование владельца 16.09.2026:** гонка возможна везде, где после стадий 1 и 2 в одну строку конфига может прилететь больше одного патча подряд или одновременно, а не только на пути Generate. Разработчику поручено пройти по всем таким местам списком и ответить по каждому, защищено оно той же блокировкой или нет; если защита ставится не в одном месте на всех, это отдельный риск.

   **Отдельно, оговорка ревьюера 16.09.2026 — верна ровно пока выполняется её условие.** Остальные шесть мутаций конфига заменяют колонку целиком, и это безопаснее слияния: замена всегда оставляет чей-то целый объект, тогда как слияние оставляло состояние, которого не хотел никто. Но у безопасности есть условие: **писатель должен слать полный объект.** Там, где выигрывает последний, более старый расчёт может лечь поверх нового — это устаревание, а не порча, и с потерей патча его путать не надо. Условие «полный объект» однажды перестанет выполняться, и тогда эти шесть мест переходят в тот же класс, что и две записи под блокировкой.

   **Закрепление правила конструкцией, решение владельца 16.09.2026.** Дисциплины признано недостаточно: два места нашлись за один день, причём второе — только потому, что искали специально. Делается и то и другое, в таком порядке: сначала общий хелпер, который сам берёт строку под блокировкой и становится **единственным** способом менять конфиг, затем структурный тест, утверждающий именно это — не «все мутации под блокировкой», а «мутация одна». Каждое исключение в списке несёт причину текстом: исключение без причины через полгода неотличимо от забытого места. По самому сторожу — красная проверка: добавить запись в обход хелпера, убедиться, что тест упал, убрать (см. Вопрос 26 — шесть случаев тестов, зелёных не по своей причине).

   **Зачем записано здесь, а не строкой в changelog:** чтобы через полгода было видно, почему в этом месте стоит блокировка, и чтобы её не сняли как лишнюю.

**Вопрос 26. Тесты, которые не сторожат то, ради чего написаны, — нужен сплошной проход. Заведено 16.09.2026, после четвёртого случая.**

   Это уже не случайность, а способ писать тесты, который даёт зелёную сборку при снятой защите. Четыре случая подряд, все найдены ревью, ни одного — самими тестами:

   1. **GH-436, восемь штук разом.** Среди них тест, патчивший функцию, удалённую в GH-427, и сам же строкой ниже проверявший, что её нет: три «варианта» были одним и тем же baseline, и именно их цифры цитировал план. Тест с литеральным индексом, проверявший азот трижды и не проверявший фосфор и калий. Тест, чей комментарий обещал вызов функции, которой он не вызывал.
   2. **GH-439, стадия 0: `test_an_identity_field_cannot_be_emptied`** — зелёный по чужой ветке. Пустая строка становилась `null` и отклонялась проверкой на `null` раньше, чем выполнение доходило до проверки identity. Тест остался бы зелёным после удаления всей защиты identity целиком.
   3. **GH-439, стадия 0: сценарии `H2a` и `H2b`** — зелёные при нулевом числе запросов. Сценарий не воспроизводился, и оба подтверждали отсутствие записи, а не отказ сервера.
   4. **GH-440, стадия 1: `sends no PUT to config/gaip`** — сторожил два файла из шести. Метод и адрес стояли на разных строках, и проверка их не связывала.
   5. **GH-441, стадия 2: сценарий `S2`** — утверждал ноль записей на пяти страницах и был зелёным только за счёт собственной семисекундной задержки: цепочка программы в её окно не укладывалась. На обычной загрузке `/reports/export` слал два `PATCH`, `/plan` — один. **Этот случай прошёл через координатора в отчёт владельцу как доказанный** — отсюда правило мерить на обычной загрузке (раздел 8 плана GH-439).
   6. **GH-441, стадия 2: пять живых сценариев** (`H1a`, `H1b`, `H1c`, `H2`, `S2`) оставались зелёными, если страница отдавала 500: ни один не утверждал, что страница вообще догрузилась.
   7. **GH-446, стадия 4a: `test_a_dry_run_changes_nothing_and_reports_the_same_thing`** — вторую половину своего имени не проверял: отчёт сухого прогона ни с чем не сравнивался. Именно на это свойство опирается решение запускать ремонт на проде.

   **Тот же корень, на данных: фикстура закрепляет момент времени вместо заявленного сценария. Установлено 16.09.2026 разбором тканевого калия.**
   Проба `144` за три недели побывала в трёх состояниях: сначала достаточное значение, затем `1.05` — чтобы проверить **нехватку**, затем `2` — чтобы проверить **нормальное состояние**. Каждое снятие фикстуры записывало то, что стояло в базе в тот день, и разные тесты зашили в себя разные моменты. Ни одна фикстура при этом не говорит, какой сценарий проверяет; из шести фикстур и десяти тестов заявленный сценарий несёт **один** (`gh369` — нехватка). Случайное состояние несут парити-фикстура (ткань `2`), `belowfloor`-фикстура и четыре теста на ней (`1.05` зашито в ожидаемые выносы, но нигде не объявлено) и `gh361` (источник числа описан неверно).
   Проявилось это красной строкой парити-харнесса, которая держалась три недели и оказалась **дефектом конструкции проверки**: две тканевые строки сравнивали фикстуру с экспортом, то есть проверяли, не изменилось ли содержимое базы с дня съёмки, тогда как соседние почвенные строки сравнивают Plan с экспортом, как и задумано. Ни данные, ни расчёт ни при чём: проба и числа в фикстурах правки не требуют.
   Почему это одна запись с тестами выше, а не отдельная: корень общий — **проверка выглядит осмысленной, а на деле закрепила момент времени.** У тестов это зелёный не по своей причине, у фикстур — состояние не по своему сценарию. По отдельности их чинили бы дважды.

   **Что нужно:** сплошной проход по структурным тестам после завершения GH-439 — какие из них могут упасть, а какие нет. Способ проверки один и уже отработан: снять защиту, которую тест обязан ловить, и убедиться, что он краснеет по своей причине, а не по соседней ветке. Перечень выше — отправная точка, выводить заново не нужно.

   **Что часть 0 нашла попутно, 16.09.2026 — GH-458.** Сделав сторож `gh365` зрячим, обнаружили **три места с зашитой палитрой потенциала роста**: на дашборде, в документе и в анализе. Все три совпадали с каноном, и нарушения правила это не отменяет — правило написано ровно на случай расхождения. Переведены на общий модуль; ни порогов, ни цветов в этих местах не осталось, поэтому дрейф там теперь нечем выразить. Сам сторож за день прошёл четыре версии: сначала не видел заглавных букв, потом цветовых форм, потом многострочных, потом связывал субъект по объекту вместо поля. Итоговый принцип — **фолбэк помечает переменная, а не место**: пятнадцать пар в девяти файлах, из них три не сторожились никогда.
   **Оставлено незакрытым:** живой проверки цвета в документе нет — цвета там сверялись разово при сдаче, постоянной проверки не существует. Предложение ревьюера: добавить сверку ячейки в парити-харнесс, он и так собирает документ.

   **План написан, объём урезан решением владельца 16.09.2026.** Полный разбор — `PLAN-Q26-tests-that-cannot-fail-RU.md`: восемь случаев обобщены в шесть способов, по каждому механический сигнал и своя мутация; протокол красной проверки, где строка без вставленного вывода падения не принимается. Оценка полного прохода — около двадцати одного дня плюс полтора на ревью.
   **Берём сейчас только часть 0** — глобальное требование, что у теста есть хотя бы одно утверждение. Она закрывает класс «ноль утверждений» механически, одной настройкой, и прохода по тестам не требует. Остальное — после работы по пробам: там живая потеря клиентских данных, а сплошной проход по тестам это гигиена, и за три недели прохода пробы продолжили бы терять данные. План под сокращённый объём не переписывается, к нему возвращаемся после проб.
   **Введено немедленно и бесплатно:** мутацию для красной проверки выбирает **ревьюер, а не автор теста**. Повод — два случая за один день, когда автор показывал красную проверку, а ревьюер находил, что тест краснеет не на том. Это не небрежность, а естественная слепота: человек ломает то, о чём думал, когда писал.

   **Почему не сейчас, решение владельца 16.09.2026:** порядок работ по GH-439 не меняется, проход идёт после него.

**Финальное ревью перед передачей клиенту, 11–12.09.2026. Находки закрыты в GH-431…GH-438.**

   Ревью смотрело весь незакоммиченный диф под одним вопросом: **введут ли клиента в заблуждение цифры и переживут ли его данные.** Нашлось четыре места, где клиент прочитал бы неправду, и одно, где терялись данные.

   **GH-431 — проба, удалённая на странице Data, возвращалась при следующей синхронизации.** Кнопка обещала «This cannot be undone». Сохранение искало строку вместе с удалёнными и вызывало `restore()`. Под ударом были 46 живых проб. Доказано живьём: `DELETE → deleted_at 07:01:02`, затем обычный push → `deleted_at: -`. После правки push отвечает `skipped: 1` и метка не меняется. **Запись GH-430 утверждала, что удаление «инертно, пока не появится настоящий эндпоинт удаления» — он уже был**, на странице Data нового хаба, поэтому принятый компромисс описывал не то, что происходило. Исполнитель поймал и собственную первую версию правки: она тихо теряла бы пробу, удалённую раньше и присутствующую в файле импорта; импорт — полная замена, значит там истина в файле.

   **GH-432 — `POST /api/samples/sync` был единственным эндпоинтом данных без проверки роли.** Пользователь с ролью viewer мог делать upsert и с `clearSiteData` жёстко удалять журналы опрыскиваний и полевой.

   **GH-434 — Monthly Schedule в документе печатал единицу в десять раз больше.** На гринах план показывал `20.0 g/m²`, документ — `200 kg/ha`, причём в том же документе таблица продуктов уже была в граммах: GH-409 перевёл две таблицы из трёх. **Попутно вскрылось, что план противоречил сам себе:** растворимый продукт печатался на одной странице как `20 kg/ha` в месячной таблице и `2 g/m²` в таблице продуктов. Растворимое — масса, значит на тонком тёрфе граммы на метр; на плане март стал `2 g/m²`. Единственная цифра на экране, которая сдвинулась, величина та же.

   **GH-433 — подсказка ⓘ показывала старый текст на всех страницах, включая новозеландские.** Две панели регистрировали один ключ, австралийская грузилась позже и затирала. Текст говорил «0 once soil ≥ ceiling» — то самое, что изменил GH-415. Запись перенесена в модуль, который эти величины и считает; близнеца больше нет. Два пина GH-312, которые должны были это ловить, проверяли **каждый свою копию** — то есть фиксировали расхождение вместо того, чтобы его обнаружить.

   **GH-435 — австралийская заметка о фосфоре** называла обрезанную дозу годовым требованием: `4.1 kg/ha (annual requirement)` при потребности 14.0. Стало `4.1 kg P/ha (of 14.0 requested)`. Ни один продукт и ни одна доза не сдвинулись, доказано прогоном прежнего исходника.

   **GH-436 — восемь тестов, которые не могли упасть.** Каждый переписан и **показан красным** на том дефекте, который обязан ловить. Среди них: тест, патчивший функцию, удалённую в GH-427, и сам же строкой ниже проверявший, что её нет — три «варианта» были одним и тем же baseline, и именно их цифры цитировал план; тест с литеральным индексом, проверявший азот трижды и не проверявший фосфор и калий; тест, чей комментарий обещал вызов функции, которой он не вызывал. Один тест выброшен как шумный.

   **GH-437 / GH-438** — результаты аудита убраны из корня репозитория в игнорируемую папку; в блоке выверки оговорено, что его самопроверка сравнивает напечатанное с напечатанным, и починена строка плотности с глубиной, печатавшая пустое значение.

   **Прогоны после всего:** Jest 2584 в 198 наборах, PHPUnit 127, весь живой набор 230 тестов в 23 наборах. Красного нет.

**Вопрос 30. Поле месячной нормы азота заполняемо, а потребителя у него почти нет. Найдено владельцем 18.09.2026 по её знанию продукта, после нашей неверной записи, что поля нет вовсе.**

**Поле есть.** Plan → вкладка Nutrition, «Current Monthly N Rate», `plan.blade.php:950-955`, `id="plan-nut-monthly-n"`. Подсказка помечает его необязательным (`dashboard-init.js:55`). Введённое уходит в конфиг площадки ключом `appliedMonthlyN` (`nutrition-calendar.js:570`) и восстанавливается при загрузке (`:3207`). На стенде пусто у всех двенадцати площадок.

**Модуль потребности в питании его не видит.** Число он берёт из `hub-tissue-v3.js:1245`, а это место есть только в разметке старого хаба (`legacy-hub-markup.blade.php:385`) и всегда пусто. Поэтому `annualNRate` равен нулю, условие `hasNProgramme` (`hub-tissue-v3.js:2747`) закрыто всегда, и **восемь участков расчёта и вывода под ним не исполняются ни разу** (`:2889`, `:2920`, `:2933`, `:2975`, `:3038`, `:3103`, `:3358`, `:3379` — счёт проверен: девять вхождений имени, одно объявление, восемь использований).

**Выгрузка в Word то же самое у себя закрыла** (`word-export.js:9837-9852`): при пустом скрытом поле она читает `appliedMonthlyN` из конфига. Решение принималось однажды и до второго потребителя не дошло.

**Вместо расчёта рисуется блок** «Enter monthly N rate in the Climate & Growth section» (`hub-tissue-v3.js:5705`). Раздела Climate & Growth в новом хабе нет. Тот же код принудительно раскрывает раздел N-Linked Nutrient Demand (`Le.style.display = "block"`, контейнер `legacy-hub-markup.blade.php:1121`). В блоке стоит эмодзи, у владельца на эмодзи прямой запрет; **эмодзи откладывается вместе с пунктом, отдельно не чиним**.

**Куда этот текст доходит — ЗАМЕРЕНО 18.09.2026 чтением разметки и стилей; страницы не открывались.** Контейнер `.gaip-nutrient-demand-body` объявлен один раз, `legacy-hub-markup.blade.php:1123`, внутри блока `data-section="nutrient-demand"` на `:1121`. Партиал подключён на пяти страницах:

- `reports/export.blade.php:204`, `reports/scenarios.blade.php:77`, `reports/forensic.blade.php:59` — внутри `div id="rp-hub-runner"`, а тот закрыт стилем `display: none !important; position: absolute; left: -9999px; width: 1px; height: 1px; overflow: hidden; pointer-events: none` (`export:51`, `scenarios:20`, `forensic:20`). Сообщение рисуется в невидимый узел и до глаз клиента не доходит.
- `hub.blade.php:281` — старый хаб, по правилу проекта не страница.
- `stadium.blade.php:141` — партиал подключён ПРЯМО в `main class="content content-wide"`, без скрывающей обёртки и без `rp-hub-runner`.

**Вывод: текст неверен везде, а виден ровно на одной странице — `/stadium`, и эмодзи вместе с ним.** Это та страница, которую владелец отложила и которой никто не пользуется. Второе расхождение той же страницы — в D49.

**Не путать с соседним полем.** «Max N per month» (`plan.blade.php:918-922`, ключ `maxNPerMonth`) заполнено у всех двенадцати площадок и живое — его читает распределение (`nutrition-requirement-engine.js:494-497`).

**РЕШЕНИЕ ВЛАДЕЛЬЦА 18.09.2026: отложено, в работу не берём, вернуться позже.** Причина её словами: клиент туда цифру никакую не ставит.

**Её же доменное суждение, записано рядом как вход для будущего плана, а не как указание способа:** поле необязательное, поэтому при пустом значении сообщение можно не выводить вовсе.

**Вопрос 31. Открытие страницы заменило полный кэш анализа почти пустым. Найдено координатором 18.09.2026 по полным числам замера.**

**Что замерено.** После обхода страниц при проверке интерфейса две строки `site_configs` namespace `analysis_cache` сменили не содержимое, а порядок величины:

- Russley: **174 093 → 1 325 байт** (`c4b5b213…` → `a96c9f6a…`), метка `2026-09-18 01:26:24`;
- Federal Golf: **1 716 → 783 байта** (`c5254565…` → `3f7496a7…`), метка `2026-09-18 01:26:16`.

У Russley это 172 768 байт разницы. Разработчик в отчёте назвал эти строки «записи страниц, которые я открывал» — верно по факту; величина видна только в полных числах, в усечённых строка выглядит просто изменившейся.

~~**Почему так вышло.** На `/analysis/disease` для Russley страница показала «No analysis data found». Запись всё равно ушла…~~

**ЭТА СТРОКА НЕВЕРНА И ОПРОВЕРГНУТА ЗАМЕРОМ 19.09.2026. `/analysis` НЕ ПИШЕТ ВООБЩЕ.** Обход
страниц дал: `/analysis` — 0 запросов на `analysis-cache`, `/data` — 0, `/hub` — 1. Пишет
только `/hub`.

**Цена этой строки измерима, а не гипотетична:** писателем считали `/analysis` оба координатора
и сам документ, и **первый прогон разработчика вернул ноль именно потому, что он искал
писателя там, куда указывал документ.** Заход потрачен на то, что было записано неверно.

Что «No analysis data found» на экране Russley — наблюдение верное; неверна была связь между
этим экраном и записью. Экран и запись оказались разными событиями.

**Как это выглядит на самом деле:** `hub-persistence.js`, **якорь `syncToServer`** (зовётся из `save`, аргумент приходит из `cacheAnalysisResults`), шлёт `POST` на `analysis-cache` с тем, что насчитала страница, беря площадку из `window.GAIP_HUB_CONFIG.activeSiteId`. Маршрут — `app/routes/web.php`, `analysis-cache.store` → `AnalysisCacheController@store`.

**АДРЕС ЗДЕСЬ БЫЛ НОМЕРОМ И УСТАРЕЛ — исправлено 19.09.2026.** Стояло `hub-persistence.js:2208`. Файл двигала стадия 3, и сегодня на `:2208` лежит `if (global.GILBA_REPORTS_EXPORT) return;` — защита страницы экспорта от записи. **Номер без якоря стареет молча: читатель, пришедший по нему, попал бы на защиту экспорта, а не на точку записи, и увидел бы там осмысленный код — то есть не заметил бы подмены.** Поэтому номера здесь сняты, оставлены якоря. Сервер по замыслу ЗАМЕНЯЕТ, а не сливает — `AnalysisCacheController.php:26-29`, комментарий GH-447: «an analysis result is whole or it is nothing». Замена сделана намеренно; проблема в том, что страница прислала «ничего» и это «ничего» оказалось целым результатом.

**Класс.** Тот самый, ради которого делался весь GH-439: страница записала СОСТОЯНИЕ, а состояние было «у меня ничего нет». Правило проекта — браузер шлёт изменение, а не копию состояния — здесь не соблюдено.

**ЧЕГО МЫ НЕ ИЗМЕРИЛИ, и это важно для оценки:**
- восстанавливается ли кэш обычным пересчётом — то есть вернёт ли Re-run или следующий полный прогон те же 174 КБ;
- что видит клиент на других страницах, пока кэш пустой;
- ~~на каких ещё страницах срабатывает тот же путь записи~~ — **ИЗМЕРЕНО 19.09.2026, см. ниже.**

  **Поправка к поправке, и вторая ошибка здесь моя.** 19.09 я написала, что номера `:135` и `:206` «как перечень не годятся», потому что на них стоит `method: 'POST'` — строки внутри других запросов. Это неверно: они относятся К ТЕМ ЖЕ запросам, просто номер был смещён на строку. Настоящие вызовы — `:134` и `:205`, где стоит `fetch(restUrl + 'analysis-cache', {`, а `method: 'POST'` — их следующая строка. Прежний перечень был по существу верен, а я его отвергла, посмотрев на строку и не посмотрев, частью чего она является.

  Это тот же класс, что и остальные наши промахи этого дня: **строка открыта, но не открыто, к чему она принадлежит.**

**ТРЕТИЙ СЛУЧАЙ ЗА СУТКИ — Canberra, 18.09.2026, 09:44 UTC.** Открытие страницы при замере пути
ввода проб переписало `analysis_cache` Canberra: **168 087 → 167 807 байт**,
`fd90871f…` → `504a23ab…`, метка `2026-09-18 09:45:20`. Разница здесь мала — 280 байт, — и
именно поэтому случай стоит рядом с двумя первыми: **тот же путь записи, тот же повод (просто
открыли страницу), а величина потери зависит от того, что страница в этот момент смогла
посчитать.** Строка возвращена координатором побайтово из бэкапа 09:20; возврат проверен из базы.

**Итого за сутки три строки: Russley, Federal Golf, Canberra.** Первые две — 18.09 в 01:26 UTC,
третья — в 09:44.

**ПОПРАВКА К СЧЁТУ, чтобы его не завели заново.** Эти три — ВСЕ известные случаи, а не «ещё два
сверх прежних»: Russley и Federal Golf записаны здесь с самого начала, и когда разработчик
18.09 вечером сказал «за день две строки `analysis_cache` схлопнулись с ~228 КБ до 1768 и 1044
байт», он говорил о НИХ ЖЕ, а не о новых. Проверено сверкой размеров и меток.

**ЗАМЕР СДЕЛАН 19.09.2026. МЕХАНИЗМ УСТАНОВЛЕН, ПРИЧИНА — НЕТ, И ЭТО РАЗНЫЕ ВЕЩИ.**

**Писателей у строки ТРИ, все в одном файле, и вне его маршрут не называет никто.**
`hub-persistence.js:134` и `:205` — оба на потоке Re-run; `:2217` — внутри `syncToServer`.
Проверено координатором проекта: `grep` по `analysis-cache` во всём `assets/` даёт только эти
три `fetch` и две строки логов рядом с первым. **Сервер prежнего значения не читает:**
`AnalysisCacheController@store` кладёт присланное вместо прежнего, размер строки равен размеру
присланного.

**ОБХОД СТРАНИЦ, замер разработчика:** `/analysis` — 0 запросов на `analysis-cache`, `/data` —
0, **`/hub` — 1. Пишет только `/hub`.**

**И это опровергает то, что стояло в этой же записи выше.** Здесь было написано, что запись
ушла «на `/analysis/disease` для Russley». Открытие `/analysis` не присылает ничего; писателем
считали её и мы, и документ. Разработчик говорит об этом прямо: первый его прогон вернул ноль
именно потому, что он, как и документ, искал писателя на `/analysis`.

**ЧТО ИМЕННО ПРИХОДИТ — измерено на площадке с полными входными данными.** Payload **не пустой**:
153 144 байта, `metrics` 13 ключей из 13, `computed` 17 из 17, без значения только `soilTemp`.
Против 171 798 сохранённых — меньше на 18 КБ **при той же форме**. Это в точности картина
Canberra: 168 087 → 167 807, усадка, а не обвал.

**МЕХАНИЗМ, как он сложился из замеров.** `/hub` — скрытый счётный движок, и открытие его само
по себе есть полный прогон расчёта. Что прогон посчитал, то и уходит; сервер кладёт это вместо
прежнего. Где расчёт дал полный результат — строка меняется чуть. Где расчёт дал почти ничего —
уходит почти ничего.

**Тонкие строки в базе именно такие, сверено координатором проекта:** `Federal Golf` — 783
байта, `metrics` 5, `computed` 11; `Russley` — 1325 байт, `metrics` 5, `computed` 12. Для
сравнения `Canberra` — `metrics` 13, `computed` 16; `Test - GC - NZ - delivery` — 13 и 17.
**Тот же писатель, тот же маршрут, то же замещение** — разный вход.

**ЧЕГО ЗАМЕР НЕ УСТАНОВИЛ, и это теперь единственное неизвестное: ПОЧЕМУ расчёт даёт тонкий
результат.** Границы названы разработчиком: страницу болезней он не открывал (случай Russley
был на ней), пути `:134` и `:205` не проверял — они на потоке Re-run, который он не запускал,
обход ограничен тремя страницами того же следа, что сдвинул Canberra.

**Его собственная поправка:** одно его утверждение покраснело, и неверным оказалось
утверждение — он потребовал, чтобы payload был ТОНЬШЕ сохранённого, а тот оказался той же
формы. Переписано на измеренное, причина вписана в файл.

**Тест:** `tests/e2e/gh539-empty-analysis-cache-write-live.test.js`, 4 passed. Сторож удержал
2 записи в `predictions`. Продукт не правился.

**ТРЕТИЙ ЗАМЕР 19.09.2026 — ЗАВИСИМОСТИ. И ОН МЕНЯЕТ ОСНОВАНИЕ ВОПРОСА.**

**`/hub` в скрытом окне открывают РОВНО ДВОЕ**, оба по явному действию пользователя:
`dashboard-ui.js:102` — кнопка Re-run; `settings-init.js:1604` — `runAnalysisAndRedirect()`
после импорта в Settings, с текстом «Re-running analysis… You will be redirected to the
dashboard». Проверено координатором проекта: во всём `assets/` ровно два места, открывающих
`/hub` в iframe, и это они.

**Re-run существует ИМЕННО РАДИ этого обновления — измерено нажатием, а не выведено.** Кнопка
есть, нажатие дало 2 POST, страница после нажатия перешла: `button present=true`,
`POSTs produced=2`, `page navigated=true`, payload 153 144 и 153 234 байта, по 13 ключей
`metrics`. Сигнал, освобождающий родителя, привязан к завершению запроса
(`hub-persistence.js:146` и `:150`); родитель убирает iframe и делает `location.reload()`, а
перезагруженная страница читает строку С СЕРВЕРА. **Убрать запись со стороны `/hub` — значит
сделать обе кнопки пустышками.**

**ТРИ СУТОЧНЫХ СЛУЧАЯ НИ ОДНИМ ИЗ ЭТИХ ДВУХ НЕ ОБЪЯСНЯЮТСЯ, и один из трёх — НАШ СЛЕД.**
Canberra 09:45 — это обход самого разработчика, открывший `/hub` напрямую КАК СТРАНИЦУ, а не
через iframe. Russley и Federal Golf в 11:26 — **чем открыты, не установлено**; разработчик
говорит прямо, что доказательства у него нет и достраивать он не станет.

~~**Отсюда поправка к основанию вопроса: случаев не три, а два, и оба необъяснены.**~~

**УСТАНОВЛЕНО 19.09.2026 ПО НАШИМ СОБСТВЕННЫМ ЗАПИСЯМ: ВСЕ ТРИ СЛУЧАЯ — НАШИ. ПОДТВЕРЖДЁННЫХ
СЛУЧАЕВ, ВЫЗВАННЫХ КЛИЕНТОМ, НОЛЬ.** Прогона для этого не потребовалось: ответ лежал в
scratchpad разработчика со вчера.

**Russley и Federal Golf, 18.09, 11:26:16 и 11:26:24.** В эту же минуту разработчик гонял живой
зонд `probe/gh520-pill.test.js` — файл написан в 11:26:08, его вывод `gh520-pill.txt` записан в
11:26:26. **Зонд перечисляет ровно две площадки и ровно эти:**
`{ name: 'Federal Golf', id: '019e96d7-36aa-707a-9879-68ab28726e12' }` и
`{ name: 'Russley', id: '019f35f0-d912-73e5-82bd-3de0bfd4f6ce' }`.

**И его вывод содержит ту самую строку, которую мы цитировали как наблюдение о поведении
продукта:** «G Russley AA Browntop Bent (Greens) … **Analysis: Sep 18 11:26** Re-run **No
analysis data found** — run analysis from the hub to populate». То есть текст на экране Russley,
записанный в этот вопрос как симптом, был снят нашим же зондом в момент, когда строка
схлопывалась.

**Восемь секунд между двумя площадками — это обход, а не два действия клиента.** Догадка
координатора Herdr; проверка её подтвердила.

**ЧЕГО ЭТО НЕ ОБЪЯСНЯЕТ, и это теперь главный незакрытый вопрос.** Зонд открывал по четыре
страницы на площадку: `/reports/export`, `/reports/forensic`, `/reports/scenarios`,
`/analysis/disease`. **Обход третьего замера проверял `/analysis`, `/data` и `/hub` — и не
проверял ни одной страницы `/reports/*`.** Какая из четырёх написала, не установлено.

**И рядом лежит зацепка, снятая координатором проекта.** `GILBA_REPORTS_EXPORT` — защита,
которая на страницах отчётов запрещает запись, — встречается в `hub-persistence.js` **РОВНО ОДИН
РАЗ, на `:2205`, внутри `syncToServer`.** Двух других писателей, `:134` и `:205` на потоке
Re-run, она не закрывает. **Проверено ли, что поток Re-run срабатывает на страницах отчётов, —
нет. Это следующий вопрос, и он дешёвый.**

**ЧТО ЭТО МЕНЯЕТ ДЛЯ ВЫБОРА НАПРАВЛЕНИЯ.** Выбор из четырёх направлений ниже был составлен для
дефекта, у которого предполагались пострадавшие клиенты. **Подтверждённых клиентских случаев
ноль**, а три известных вызваны нашей собственной привычкой открывать продуктовые страницы
зондами. Решение владельца на прежнем основании было бы решением о другом предмете.

**ЧЕТЫРЕ НАПРАВЛЕНИЯ ПОЧИНКИ, ПЕРЕЧИСЛЕНЫ РАЗРАБОТЧИКОМ, ВЫБОР НЕ СДЕЛАН.** Выбор за
владельцем: аналитика не поднимаем, её решение — «аналитик много ест».

- **А. Убрать запись со стороны `/hub` целиком.** Цена: Re-run и «Re-run after import»
  становятся пустышками; по правилу проекта про элементы `/hub` это означает и удаление обеих
  кнопок. Ломается: единственный способ клиента обновить кэш. Не знаем: есть ли другой путь
  обновления.
- **Б. Оставить запись, но не давать тонкому заместить полное.** Цена: трогается
  `AnalysisCacheController@store`, который сегодня заменяет намеренно — «an analysis result is
  whole or it is nothing». Нужен признак «результат полон». Не знаем: чем отличать тонкий
  результат от честно малого.
- **В. Сузить открывателей: `/hub`, открытый КАК СТРАНИЦА, писать не должен; писать должен
  только открытый как iframe.** Прецедент есть — `GILBA_REPORTS_EXPORT` закрывает один такой
  случай. Ломается: ничего из известного. Не знаем: покрывает ли это случай Russley — чем
  открыли те строки, не установлено.
- **Г. Ничего не менять в записи, закрыть на стороне повода.** Цена: Russley и Federal Golf
  остаются тонкими до следующего Re-run. Не знаем того же.

**Сторож в этом прогоне удержал 4 записи в `predictions`, а запись `analysis_cache` перехвачена
и до сервера не дошла ни разу.**

**ЧЕТВЁРТЫЙ ЗАМЕР 19.09.2026 — СТРАНИЦЫ `/reports/*`. НАЙДЕН ДЕФЕКТ, И ОН НЕ ТОТ, ЗА КОТОРЫМ ШЛИ.**

**Пишут `/reports/forensic` и `/reports/scenarios`, при обычном открытии, без единого нажатия.**
`/reports/export` — 0 запросов, `/analysis/disease` — 0. Нули поданы с перечнем осмотренного:
маршрут под наблюдением на каждой странице, ожидание 28 секунд при медленном пути в 10, строк
`[GilbaRerun]` в консоли ноль на всех четырёх.

**ПРИЧИНА — ЗАЩИТА, ПОСТАВЛЕННАЯ ОДНОЙ ВЬЮХЕ ИЗ ТРЁХ.** `window.GILBA_REPORTS_EXPORT = true`
стоит в **`app/resources/views/reports/export.blade.php:220`, и это единственное место во всех
вьюхах** — проверено координатором проекта: у `forensic` и `scenarios` ноль вхождений. Все три
страницы включают один и тот же скрытый прогонщик, а флаг, который запрещает ему писать, есть
только у одной.

**Это тот же класс, что «четвёртая поверхность» в GH-529:** правило записано один раз и
применено к одному месту из нескольких одинаковых.

**ЗАМЕР ПОПРАВИЛ ЧТЕНИЕ КОДА, И ПОПРАВИЛ ЕГО В ОБРАТНУЮ СТОРОНУ.** Координатор проекта нашла,
что флаг закрывает только `syncToServer` (`:2205`) и не закрывает двух писателей потока Re-run
(`:134`, `:205`), и предположила, что писать могли они. **Замер это опроверг:** если бы писал
поток Re-run, `/reports/export` написал бы тоже — его флаг действительно не закрывает. Export не
написал. **Значит писал `syncToServer` — единственный, кого флаг и закрывает.** Зацепка была
верна по букве, а следствие из неё оказалось обратным. Поток Re-run на открытии сам не
запускается.

**ЧЕГО ЭТОТ ЗАМЕР НЕ ОБЪЯСНЯЕТ, названо разработчиком:** ни одна из четырёх страниц не прислала
ТОНКОГО payload — все 153 144 байта, 13 ключей `metrics`, 17 `computed`. Значит **чем схлопнулись
Russley и Federal Golf, по-прежнему неизвестно.** Его зонд открывал те же четыре страницы, но на
других площадках; разница может быть в состоянии тех площадок, а не в странице. Он это не
достраивает.

**ЧТО УСТАНОВЛЕНО ТВЁРДО: на площадке с полными входными данными две страницы отчётов пишут
полный результат поверх сохранённого, без единого нажатия.**

**Страховка: копия снята и сверена ДО открытия** — md5 из базы и из копии совпали (`c768aa1b…`),
длина 171 798 в обеих. Возврат не понадобился: строка не изменилась, стенд 0 различающихся
строк.

**Почему строка не могла схлопнуться, хотя это допускали, — его довод, и он верен:** запрос
перехвачен и отвечен до сервера, а `AnalysisCacheController@store` замещает строку присланным и
прежнего не читает. Значит перехваченный payload и есть та строка, что легла бы, ключ в ключ.
**Мерить запрос стоит ноль строк; дать ему лечь стоило бы строку и сказало бы то же самое.**

Тест: `tests/e2e/gh540-reports-pages-analysis-cache-write-live.test.js`, 3 passed.

**Цена сегодня.** Наш стенд, клиентских данных не затронуто. В работу не берём — записано как класс, а не как срочный дефект.

**Вопрос 32. Сравнение двух поверхностей друг с другом молчит, когда обе уехали вместе. Найдено ревьюером 18.09.2026, подтверждено на четырёх тестах.**

**Класс.** Тест сравнивает то, что показывает страница, с тем, что печатает документ, и объявляет согласие верностью. Пока расхождение одностороннее — он его ловит. Как только обе поверхности берут одно и то же испорченное значение, сравнение их друг с другом даёт согласие, и тест зелёный.

**Где обнаружен сегодня.** На `gh409-product-table-units` — прямо: мутация сдвинула ряд, переданный программе, клиентская таблица продуктов изменилась целиком (`Ammos 22 (Balance) | 3 | 90 L/ha` → `| 1 | 30 L/ha`, 37 изменившихся строк журнала), а тест остался зелёным. Ревьюер видел тот же класс на `gh415-prebble-input-parity`, `gh398-monthly-distribution` и `gh426-soil-temp-options`.

**Почему это не остаток работы по `gh409`.** Его починка закрыла настоящую дыру — одностороннее расхождение документа со страницей. Требовать от одного теста, чтобы он в одиночку закрыл класс, живущий во всех тестах этой формы, неправильно. `gh409` принят; класс заведён здесь.

**ЛЕКАРСТВО УЖЕ ЕСТЬ В ДЕРЕВЕ, один раз применённое.** Принятая 18.09.2026 починка `gh415` формулирует утверждение так: «the CEC handed to the recommender is the sample's measured CEC, **not a number that merely matches**». Это и есть выход из класса: сравнивать не поверхности друг с другом, а КАЖДУЮ — с тем, что вне пары: с фикстурой, с исходными данными, с пробой.

**С этого и начинать** — не выдумывать приём, а взять образец `gh415` и приложить к каждому тесту, который сегодня сравнивает поверхность с поверхностью.

**Третий раз за сутки, когда ответ уже лежал в дереве.** Образец уборки за собой — `gh439-config-reset-live`; инструмент якорей — `tests/lib/anchored-slice.js`, написан 17.09 и используется пятью тестами; образец проверки против внешнего источника — `gh415`. Во всех трёх случаях мы начинали искать способ, а не место, где ответ уже есть.

**КЛАСС МОЖНО НЕ ПОМНИТЬ, А ПЕРЕЧИСЛЯТЬ — добавлено ревьюером 18.09.2026.** У него есть механический признак в исходниках тестов: утверждение, у которого ОБЕ стороны получены в этом же прогоне и ни одна не приходит извне — `expect(planX).toEqual(docX)`, где и `planX`, и `docX` сняты с только что построенных поверхностей. Третьего слагаемого нет, потому оно и молчит при общем сдвиге.

Перечисляется обходом по AST так же, как перечислялись комбинаторы (GH-515) и таймеры (GH-516): собрать утверждения, у которых нет ни одного операнда из фикстуры, из базы или из независимого пересчёта.

**Оговорка автора, и она обязательна к переносу: такой обход даёт КАНДИДАТОВ, а не находки.** Сравнение двух поверхностей бывает и единственно верным утверждением — `gh399-delivery` именно такой, его заявка и есть «одно Delivered на обеих поверхностях», и под мутацией, двигающей обе одинаково, он правомерно остаётся зелёным. Каждый кандидат проверяется мутацией отдельно.

«Ценность обхода в том, что он заменяет „вспомнить, где ещё так“ на „вот полный список, разбирайте“.»

**ТРЕТИЙ ОБХОД ПИШЕТСЯ НЕ С НУЛЯ, и это надо назвать прямо, иначе он оценится как новая работа.** У нас уже есть два обхода того же устройства: комбинаторы ростового потенциала — GH-515, `tests/gh510-not-computed-is-not-zero.test.js`; таймеры на цепочке объявления — GH-516, `tests/gh498-the-announcement-is-not-a-timer.test.js`. Оба разбирают дерево через `@babel/parser` + `@babel/traverse`, оба выводят вселенную, а не перечисляют её руками, и у обоих есть паспорт и положительный контроль. Третий обход берёт их устройство и меняет предикат.

**В работу не берётся сейчас.** Это класс, а не срочный дефект: продукт сегодня ведёт себя правильно, слепа проверка.

**Вопрос 33. Документ печатает Required и не печатает Delivered, когда почвенная запись площадки есть, но не выбрана. Найдено ревьюером 18.09.2026 при разборе красных тестов.**

**ВОПРОС ВЛАДЕЛЬЦУ, прямо: должен ли документ печатать Delivered, когда почвенная запись есть, но не выбрана.**

**Что установлено.** `calc-audit-export-parity-live` сравнивает одиннадцать пар «страница ↔ документ». Расходится одна: **Westview / Backyard, нутриент N — план «On Track», документ «—»**.

Из журнала того же прогона: `Westview / Backyard: plan req=200.0/20.0/125.0 del=224.8/21.4/154.1 | docx req=200.0/20.0/125.0 del=—/—/—`. То есть документ печатает ТО ЖЕ Required, что и страница, и не печатает Delivered ни по одному нутриенту.

**Состояние площадки.** Почвенная запись у Westview «на файле, но не выбрана» — замерено живым прогоном gh490 18.09.2026 (`Westview | 1 | on-file-not-selected`). Почвенный раздел в документе не печатается, строка реестра при этом стоит.

**УТОЧНЕНИЕ 18.09.2026, замер координатора по базе. Оно меняет постановку.**

У Westview ЕСТЬ живая почвенная проба: одна строка, `sample_type = soil`, дата пробы и лаборатории `2026-07-27`, `deleted_at` пуст. **Выбирать было из чего** — и при этом документ напечатался так, будто почвенных данных нет.

Значит «почвенная запись на файле и не выбрана» описывает СЛЕДСТВИЕ, а не причину. Причин возможно две, и мы между ними НЕ различали:

1. **Случай оператора.** Человек при выгрузке не выбрал пробу, хотя она предлагалась. Тогда вопрос только в том, должен ли документ в таком виде печататься вовсе.
2. **Дефект.** Выгрузка не смогла эту пробу взять. У строки **пустой `client_uid`** — тот самый идентификатор, которого нет у 27 строк из 147 (замер аналитика 18.09). Тогда это дефект, и он в одном кусте с работой по пробам, `PLAN-samples-sync-FINAL.md`.

**Чем различаются:** открыть выгрузку на Westview, посмотреть, предлагается ли проба к выбору, и что будет, если её выбрать. Замер короткий, но ЖИВОЙ, поэтому пока идёт работа по дереву не ставится; вопрос, делать ли его сейчас, у владельца.

**Отдельно, как след рассуждения:** связь с пустым `client_uid` не увидел никто, пока не посмотрели саму строку в базе. Формулировка «на файле и не выбрана» звучала как полное описание и таковым не была.

**Продукт сам называет этот случай.** Комментарий экспорта, `assets/word-export.js:8790-8792`: таблица Annual Nutrient Requirements в этом случае печатается всё равно — «с цифрами из собственного прогона страницы», и случай назван «view II, названный в отчёте GH-490, не этого тикета».

**Почему разбор остановлен здесь.** Слова ревьюера: «Должен ли документ в этом случае печатать Delivered — вопрос о том, что цифра значит, а не о тесте. Тест свою работу делает: он единственный, кто эту пару поверхностей сравнивает, и он её поймал.»

**Тест не трогаем — он красный по делу.** В задание разработчика по четырём починкам этот пункт не входит.

**ЗАМЕР СДЕЛАН 18.09.2026 ВЕЧЕРОМ. ВОПРОС НЕ ЗАКРЫТ, И ПОСТАНОВКА СНОВА МЕНЯЕТСЯ.**

**Ни одна из двух причин не подтвердилась.** Разработчик открыл выгрузку на Westview живым
прогоном. Проба **предлагается** к выбору: окно выбора, 9 групп площадок, флажок
`019f7d28-…::sample_118`, подписан `Backyard`, **отмечен по умолчанию**. Значит не «выгрузка не
смогла её взять». Документ с этой пробой собрался, 20 324 байта, и **Delivered несёт числа:
N 224.8, P 21.4, K 154.1** — те же три числа, что печатает страница. Значит и «оператор не
выбрал» этим прогоном не подтверждается: с выбранной пробой заявленного прочерка нет.

**Прочерки в документе есть, но в ДРУГОЙ колонке:** `Current (kg/ha, ppm)` — три штуки — и
`Range` — три штуки. Это колонка лабораторного показания, а не доставки; статус по P и K сам
говорит `No Soil Data`.

**ДВА НАШИХ ПРОГОНА ПЕЧАТАЮТ В ОДНОЙ КОЛОНКЕ РАЗНОЕ, и это сама находка.** Паритетный прогон
дал `docx delivered = —/—/—`, вечерний — `224.8/21.4/154.1`.

~~**Первое объяснение, координатора Herdr: в отчёте перепутали колонку `Current` с
`Delivered`.**~~ **Опровергнуто самим файлом результатов.** В записи паритетного прогона у
нутриента N стоит: `current "—"`, `removal "200"`, `required "200.0"`, **`delivered "—"`**,
`range "—"`, `balance "—"`, `status "—"`. Колонка названа своим именем, пустой была именно
`delivered`, и вместе с ней `balance` и `status`.

~~**Второе объяснение, координатора проекта: паритетный прогон ставит флажок программно
(`cb.checked = true` плюс событие `change`, `calc-audit-export-parity-live.test.js:289-305`), и
обработчик его не видит.**~~ **Снято собственным чтением до того, как на нём построили работу:**
`word-export-combined.js:5646` читает выбор прямо из DOM в момент сборки —
`querySelectorAll('input[data-sample-uid]:checked')`, своей модели состояния у него нет.

**Третье объяснение, снятое взглядом в уже снятый вывод, а не новым замером.** Паритетный
прогон сам записывает, какой флажок выбрал. В `export-results.json` стоит:
`exportPick = {"found": true, "uid": "019f7d28-50ed-71e2-b817-c252b0160460::sample_118",
"text": "BackyardSoil", "checked": 1}`. **Тот же самый флажок, ровно один.** Промаха подбора не
было, и выбор из объяснений выбывает целиком.

**ЧТО ОСТАЁТСЯ, И ЭТО ЕДИНСТВЕННОЕ ИЗМЕРЕННОЕ РАЗЛИЧИЕ МЕЖДУ ПРОГОНАМИ — ВРЕМЯ.**
`generatedAt` паритетного файла: **`2026-09-18T02:24:16.600Z`**, то есть 12:24 по местному
времени. Вечерний прогон — около 20:00 того же дня. Между ними легла вся работа дня: стадия 2,
стадия 3 и записи GH-529…GH-536, среди которых правки самого печатающего пути —
`sumDelivered` в `nutrition-delivery-core.js` и `_sumPrinted` в `word-export.js`.

**Прямое следствие для постановки: поведение изменилось между двумя прогонами, и обе прежние
причины были построены на замере, который к вечеру устарел.** Это то же свойство, что у плана и
у номера строки, применённое к СОХРАНЁННОМУ замеру: у файла результатов есть дата, и она была в
нём всё время.

**ЧЕГО НЕ ИЗМЕРЕНО и чем вопрос закрывается:** какая именно правка дня изменила поведение.
Закрывается повторным прогоном `calc-audit-export-parity-live` на сегодняшнем дереве — если
пара сойдётся, дефект закрыт чужой работой и это надо назвать; если разойдётся снова,
различие не во времени и объяснение придётся искать дальше. Прогон ЖИВОЙ.

**Оба прежних объяснения оставлены зачёркнутыми, с именами авторов и с тем, чем опровергнуты.**
Иначе через неделю их выведут заново из тех же слов.

**ПОВТОРНЫЙ ПРОГОН СДЕЛАН 18.09.2026, 20:12. ПАРА СОШЛАСЬ. РАСХОЖДЕНИЯ БОЛЬШЕ НЕТ.**

Прогон сужен до одной пары решением координатора: вопрос про Westview, а полный прогон жмёт
`#plan-nut-generate-btn` на одиннадцати площадках и двигает одиннадцать конфигов средством,
которое девять из двенадцати не вернёт.

**Из новой записи прогона** (`generatedAt 2026-09-18T10:12:16.024Z`), тот же флажок, что и
утром — `exportPick.uid = 019f7d28-…::sample_118`, `checked: 1`:

| нутриент | current | removal | required | delivered | balance | status |
|---|---|---|---|---|---|---|
| N | — | 200 | 200.0 | **224.8** | 24.8 | On Track |
| P | — | 20 | 20.0 | **21.4** | 1.4 | No Soil Data |
| K | — | 125 | 125.0 | **154.1** | 29.1 | No Soil Data |

Утром на том же месте стояло `delivered "—"`, `balance "—"`, `status "—"`.

**РАСХОЖДЕНИЕ БЫЛО НАСТОЯЩИМ, И ЭТО ТЕПЕРЬ ДОКАЗАНО, А НЕ ПРЕДПОЛОЖЕНО.** Разработчик нашёл в
захватах консоли ВТОРОЙ утренний замер той же пары, при годовом N 150:
`plan del=168.1/17.3/110.2 | docx del=—/—/—`. Прочерки не зависели от чисел. Значит утренний
результат не был случайностью одного прогона.

**ЧЕМ ЗАКРЫТО — НЕ НАЗВАНО, И ЭТО ЧЕСТНЫЙ ИСХОД, А НЕ ПРОБЕЛ.** Слова разработчика:
«Назвать GH-529 или GH-531 значило бы выдать совпадение по времени за причину.»

Что он проверил, прежде чем отказаться называть: `nutrition-delivery-core.js` и
`word-export.js` правлены сегодня в 16:24, между последним захватом с прочерками (11:18) и этим
прогоном — совпадение по времени и по файлам есть. Но **ни одна запись от GH-529 до GH-536 не
описывает правку колонки `delivered` в блоке ANR документа**; обход по «Annual Nutrient
Requirements» даёт последней GH-423. И GH-529 говорит прямо обратное: «delivered, the Annual
Nutrient Requirements figure, did not [move]». Диффом проверить нельзя — агенты git не трогают.

**ЧТО ЭТО ГОВОРИТ О НАШЕЙ СОБСТВЕННОЙ ФОРМЕ ОЖИДАНИЯ, записано как находка о методе.** Ожидание
было написано ДО прогона двумя ветвями: «сойдётся — назвать, какой сдачей закрыто; разойдётся —
объяснений нет». Первая ветвь сработала, а её вторая половина оказалась невыполнимой: **схождение
показывает, что расхождения нет, и не указывает на причину.** Ожидание написано так, будто
«сойдётся» влечёт «знаем чем». Не влечёт.

**СРЕДСТВО: НЕИЗМЕРЕННОЕ МЕСТО ИЗМЕРЕНО, И ОТВЕТ ПРОТИВОПОЛОЖЕН ОПАСЕНИЮ.** `guardStand`
поставлен вместо capture-and-restore. Двенадцать строк `tr.gilba-nut-row` **дошли** до экрана
при задержанной записи, прогон прошёл прессы и выдал числа. Сторож удержал **4 записи** в
`site_configs`. Стенд не сдвинулся вовсе: 0 различающихся строк, `analysis_cache` Westview
`a580c178…` до и после, Canberra на месте, `predictions` 1534. Файл теста возвращён побайтово,
md5 `5802b54c7bb990e9392bce306ddf5049`.

**Вывод к Вопросу 39:** для этого теста `guardStand` не просто годится — он СТРОГО ЛУЧШЕ, потому
что не даёт записи лечь, тогда как возврат для девяти площадок из двенадцати невозможен.

**Один тест упал — следствие сужения, не находка.** У прогона есть именованное исключение
`test4 - USA / Green 13`; при сужении эта пара не запускалась, и утверждение о ней не
выполнилось. 3 passed, 1 failed.

**ОТКРЫТО: заводить ли отдельным вопросом «расхождение закрылось, автор неизвестен».** Решение
владельца. Разработчик сам его тихо не вычеркнул и поднял вопрос — это правильная половина
ожидания, которая сработала.

**ВТОРОЕ, ПОДНЯТОЕ ТЕМ ЖЕ ЗАМЕРОМ, и это отдельный вопрос:** проба Westview содержит ОДИН
показатель — pH. Документ при этом печатает по P и K и `Required` (20 / 125), и `Delivered`
(21.4 / 154.1), а рядом ставит статус `No Soil Data`. Должна ли проба из одного показателя
участвовать в рекомендациях по нутриентам, которых в ней нет, — вопрос владельцу, задан
отдельно.

**ОТВЕТ ВЛАДЕЛЬЦА 18.09.2026, дословно: «первый вопрос оставляем как есть, потому что это
тестовый пример. Я его сама завела и ввела просто один показатель. Поэтому такого не будет.
И как считается, это нормально».**

**ВОПРОС ЗАКРЫТ, ПРОДУКТ НЕ ТРОГАЕМ.** Поведение подтверждено владельцем как верное.

**Отдельно о том, чем был закрыт вопрос, потому что это не «она согласилась».** Она назвала
ПРОИСХОЖДЕНИЕ данных: проба заведена ею вручную как пример, с одним показателем. Ни один наш
замер этого видеть не мог — мы видели строку с тремя полями и пустым `client_uid` и считали её
рядовой записью клиента. **Происхождение данных не лежит в данных.** Отсюда и разница в цене
вопроса: то, что выглядело как поведение продукта на неполной пробе, оказалось поведением
продукта на заготовке, которой у клиента не будет.

**Связано:** Вопрос 32 — сравнение двух поверхностей друг с другом. Здесь тот же тест сработал ПРАВИЛЬНО, потому что расхождение одностороннее.

**Вопрос 34. Подпись документа не сходится с суммой его же напечатанных строк. Найдено разработчиком 18.09.2026 при починке gh401, тест сработал верно.**

**Что измерено.** Подпись — **245.5**, сумма напечатанных выше строк — **245.6**. Расхождение на **обеих поверхностях**: это не расхождение страницы с документом, а несогласованность округления ВНУТРИ продукта. Площадка Test5 - NZ, нутриент N.

**Это видит клиент:** итог документа не сходится с суммой строк, которые стоят прямо над ним.

**Кто поймал.** `gh401-delivery-volumes-live`, утверждение «the caption is the sum of the rows printed above it, on both surfaces». Тест сделал ровно свою работу; чинить его не нужно.

**Решение координатора 18.09.2026: утверждение остаётся КРАСНЫМ НАМЕРЕННО**, над ним рамка — что измерено, почему красное и чем станет зелёным. Остальные утверждения файла зеленеют. Порядок тот же, что у `gh428`: зелёный поверх дыры — то, что мы убираем.

**Продукт в этой сдаче не трогается.** Как сводить округление — решение не разработчика.

**ЧЕГО НЕ ИЗМЕРЕНО:** встречается ли это на других площадках и других нутриентах или только здесь. Замер был по одной паре, вышедшей красной; остальные пары этого прогона не разбирались на предмет копеечного расхождения.

**Вопрос 35. Сторож стенда пропускает записи, и тест gh401 нестабилен между прогонами на неизменном стенде. Заведён 18.09.2026 ДО замера, намеренно.**

**Почему заводится до замера.** Это вопрос не про один файл. Сторож стенда (`tests/e2e/lib/stand-guard.js`) — то, на чём держится каждое сегодняшнее «стенд не сдвинулся»: приёмка пятнадцати живых тестов, обе сдачи методологии, два круга починок, две приёмки ревьюера. Если он пропускает записи, под вопросом оказываются все эти приёмки, а не один тест.

**Что известно на момент заведения, со слов разработчика, замером НЕ подтверждено:**
- `gh401-delivery-volumes-live` даёт разные результаты между прогонами на НЕИЗМЕННОМ стенде;
- защита в нём ПРОПУСКАЕТ записи.

**Что назначено измерить, и это замер, а не вывод:**
1. Какие именно записи прошли — метод, путь, таблица.
2. Почему прошли: не подошёл шаблон в `SITE_STATE_WRITES`; запись идёт не через перехваченный путь; или сторож в этом тесте вообще не поднят.
3. Сдвинулся ли стенд по итогу, и если нет — что его вернуло.
4. **Касается ли причина ТОЛЬКО `gh401` или любого теста с таким же устройством.**

**До замера не говорим ни что это местное, ни что общее.**

**Связанный факт, уже подтверждённый:** 18.09.2026 в 14:38:43 строка `site_configs` площадки Federal Golf (namespace `gaip`) была переписана внутри мутационного прогона `gh401` ревьюера, хотя захват и возврат в том прогоне отработали. Возврат случился раньше записи. Это же и есть повод к пункту 3.

**Состояние правок в `gh401`:** сделаны, переворотом проверены, **контрольным прогоном НЕ подтверждены** — контрольный прогон нестабильного теста ничего не означает. Так и считаются: сделано, не подтверждено, причина названа.

**Вопрос 36. Одно отсутствие — два представления: колонка `methodology_snapshot` умеет быть и `null`, и пустой строкой. Найдено разработчиком 18.09.2026 при починке дефекта GH-520.**

**Что измерено.** Миграция сделала `methodology_snapshot` nullable в `samples` и `site_summaries` (проверено по `information_schema`: обе `IS_NULLABLE = YES`). При этом `default('')` у колонки ОСТАВЛЕН. То есть «методология не задана» выражается теперь двумя способами — `null` и `''`.

**Почему это существенно.** Весь день 18.09 ушёл на то, чтобы у методологии был ОДИН владелец: колонка `sites.methodology_override` упразднена, три правила NZ сняты, штамп переведён на конфиг. Устранив «один факт — два хранилища», мы тут же завели «одно отсутствие — два представления», внутри одной колонки.

**Почему не закрыто походя, словами нашедшего:** «Сужать это — отдельное решение о существующих строках, а миграция была о том, чтобы запись стала возможной.»

**ЧЕГО НЕ ИЗМЕРЕНО:**
- сколько существующих строк несут пустую строку, а сколько `null`, в обеих таблицах;
- читает ли кто-нибудь эти два случая по-разному — то есть есть ли код, для которого `''` и `null` означают разное.

Первое — один запрос к базе, назначен разработчику в том же заходе; числа впишутся сюда.

**В работу не берётся.** Это класс, а не срочный дефект: запись сейчас возможна, 500 больше нет.

**Вопрос 37. Мягкое удаление держится на двух разных основаниях: у пяти таблиц — на механизме, у двух — на внимательности. Замер координатора 18.09.2026, сделан по вопросу владельца. В работу не берётся.**

**ЧТО СЕГОДНЯ РАБОТАЕТ.** Мягкое удаление стоит на семи таблицах, и обе половины работают.

**Через модель, механизмом Laravel — пять:** `samples`, `sites`, `accounts`, `site_summaries`, `precinct_groups`. Проверено по дереву: `SoftDeletes` объявлен ровно в пяти моделях — `Sample.php`, `Site.php`, `Account.php`, `SiteSummary.php`, `PrecinctGroup.php`. Скрытие работает само: любой запрос через модель удалённого не видит.

**Вручную, без модели — две:** `spray_logs` и `field_log_entries`. Колонка `deleted_at` есть, **модели нет вовсе** (в `app/Models/` файлов под эти таблицы не существует). Удаление и чтение идут прямыми запросами к базе (`SampleController.php:238`, `:241`). Скрытие держится на том, что в каждое чтение ВРУЧНУЮ дописано условие.

**НА ЧЁМ ЭТО ДЕРЖИТСЯ — и в этом весь пункт.** У первых пяти сторожит МЕХАНИЗМ. У журналов — внимательность того, кто в следующий раз добавит чтение. Это тот же класс, который разбирался весь день в нашей работе, только в продукте: правило есть, исполнителя нет.

**ЛЕКАРСТВО ГОТОВОЕ И ДЕШЁВОЕ:** завести журналам модели с `SoftDeletes`, как у остальных пяти. Тогда скрытие перестанет зависеть от памяти автора.

**ЧТО ИЗМЕРЕНО ДОПОЛНИТЕЛЬНО, координатором Никой, и это расширяет вопрос.** Координатор Ева отметила как неизмеренное: есть ли чтения этих двух таблиц ВНЕ `SampleController` (шестнадцать посчитал разработчик в своей сдаче, число не перепроверялось). Замер по дереву: **упоминания есть в ПЯТИ контроллерах**, не в одном —

| контроллер | упоминаний | из них с `whereNull('deleted_at')` |
|---|---|---|
| `SprayLogController` | 7 | 4 |
| `SampleController` | 6 | 2 |
| `FieldLogEntryController` | 5 | 1 |
| `DataController` | 2 | 2 |
| `DashboardController` | 1 | 1 |

**Число `whereNull` меньше числа упоминаний в трёх контроллерах из пяти.** Это НЕ доказывает пропущенного чтения: упоминание может быть записью, удалением или частью одного запроса, а один `whereNull` может покрывать несколько строк с именем таблицы. Но это и есть форма, при которой «шестнадцать проверенных чтений» нельзя перепроверить счётом — проверять надо каждое обращение по отдельности.

**ЧЕГО НЕ ИЗМЕРЕНО:** какие именно из этих обращений являются ЧТЕНИЯМИ и все ли они несут условие. Здесь нужен разбор каждого, а не счёт; счёт только показывает, что вопрос не закрывается одним взглядом.

**Решение за владельцем.** Сегодня продукт скрывает удалённое верно — это не дефект, это наблюдение о том, чем это обеспечено.

**Вопрос 38. Серверный набор НЕ МОЖЕТ покраснеть на обращении к несуществующей колонке. Найдено 18.09.2026 при откате D-3, замер разработчика, обе стороны перепроверены координатором.**

**ЧТО ИЗМЕРЕНО.** Серверный набор ходит в **SQLite в памяти** (`app/phpunit.xml:26-27`, `force="true"`), продукт работает на **MySQL**.

**Схемы совпадают:** `Schema::hasColumn` даёт `false` по обеим базам; миграция применяется к тестовой схеме так же, как к продуктовой.

**Расходятся ДВИЖКИ:**
- **SQLite 3.43.2** на `select count(*) from "spray_logs" where "deleted_at" is null` отвечает **0 строк БЕЗ ОШИБКИ**, потому что читает закавыченное неразрешённое имя как СТРОКОВЫЙ ЛИТЕРАЛ. Тот же запрос **без кавычек** падает: «no such column: deleted_at».
- **Laravel закавычивает идентификаторы всегда.**
- **MySQL** на то же самое отвечает `ERROR 1054 (42S22) Unknown column 'deleted_at' in 'where clause'`.

**СЛЕДСТВИЕ, формулировкой нашедшего:** «Это не дыра в откате. Это дыра в ИНСТРУМЕНТЕ ПРИЁМКИ: условие, вернувшееся в код завтра, придёт из набора зелёным, а у клиента даст 500.»

**ВЫВОД, ПЕРЕПИСАННЫЙ ПО ЗАМЕРУ. Прежний был шире измеренного.**

~~«Наш серверный набор не может покраснеть на обращении к несуществующей колонке — ни на этой, ни на любой другой. Опечатка в имени поля, переименование, снятая колонка — всё это проходит тесты и ломается у клиента.»~~

**Чем опровергнуто.** Ревьюер 18.09.2026 поставил `whereNull('deleted_at')` ОБРАТНО на два пути чтения — `index` обоих журналов и `buildFilteredQuery` (`SprayLogController.php:310`) — и каждый раз получил **по два красных** в `SiteApiTest`.

**Набор ловит не ошибку запроса, а ПУСТОТУ СПИСКА:** условие по несуществующей колонке даёт ноль строк, а тест утверждает, что строки есть.

**Верно так:**
- расхождение движков настоящее и измерено обеими базами;
- следствие «условие вернётся из набора зелёным» **НЕ ДЕРЖИТСЯ**: на двух проверенных путях чтения набор краснеет — косвенно, но краснеет;
- молча пройдёт только там, **где результат этого запроса никем не утверждается**.

**ГДЕ НА САМОМ ДЕЛЕ ДЫРА:** не в том, что набор слеп к несуществующей колонке, а в том, что он видит её **ТОЛЬКО через утверждение о результате**. Где утверждения нет — не увидит.

**Граница замера, названа самим ревьюером:** все пути чтения он не перебирал и этого не утверждает.

**КАК ВОЗНИКЛА ПРЕЖНЯЯ ФОРМУЛИРОВКА — это полезнее самой поправки.**

Трое — координатор Herdr, координатор проекта и автор замера — прочитали ВЕРНЫЙ факт («движки
отвечают по-разному») и достроили из него следствие («значит набор не покраснеет»). Ни один из
троих не поставил условие обратно и не посмотрел. Поставил четвёртый, ревьюер.

**СОГЛАСИЕ ТРОИХ ВЫГЛЯДЕЛО ПРОВЕРКОЙ.** Это опаснее одиночной ошибки: одиночную ловит второй
читатель, а согласованную не ловит никто — каждый видит, что остальные не возражают.

**Проверяемая форма, чтобы отличить согласие от проверки:** спросить, кто из согласившихся
ИСПОЛНИЛ действие, а не прочитал вывод. Здесь ответ был «никто», и выяснилось это только
когда исполнил четвёртый.

Без этого абзаца зачёркнутая строка выше читается как чья-то одиночная невнимательность, и
следующий читатель решит, что достаточно быть внимательнее. Достаточно не было.

**ЧЕГО НЕ ИЗМЕРЕНО:**
- сколько мест в `app/` обращаются к полям по имени в закавыченном виде;
- есть ли среди них уже сейчас несуществующие;
- **на скольких путях результат запроса вообще кем-то утверждается** — это и есть мера того,
  сколько путей набор способен увидеть косвенно.
  **Частично снято 18.09.2026, на пяти точках, которые тронул откат D-3.** Ревьюер исполнил
  каждую по отдельности против зелёного контроля (197 passed, 844 assertions):
  `buildFilteredQuery` → 2 красных; пакетное чтение по ids → 1; `resolveOwnedLog` → 1;
  `FieldLogEntryController::index` → 1; **перечитывание после update → 0 красных, МОЛЧИТ.**
  **Четыре из пяти видны косвенно, одна нет** — и это ровно та «непокрытая точка чтения»,
  которую вывод предсказал. Число по всему приложению по-прежнему не снято: пять — это те,
  которых коснулся откат, а не все.
  **Не измерено внутри этого:** что именно происходит на молчащей точке. По поведению
  `firstOrFail()` на пустом результате update сохранит запись, а ответ станет 404 —
  видимо пользователю, невидимо набору; замера под этим нет, только вывод из устройства.

Сегодня известен только механизм, не его распространённость.

**ЛЕКАРСТВА, названы без выбора — выбирать владельцу:**
1. Гонять серверный набор на MySQL, как продукт. Дороже и медленнее, зато движок тот же.
2. Держать отдельную короткую проверку на MySQL только для схемы и имён полей.
3. Оставить как есть, зная границу.

**В работу не берётся.** Приёмку отката D-3 это не останавливает: доказательство там требует MySQL и кода 1054, и это условие уже стоит.


**ЗАКРЫТ 18.09.2026 — НО НЕ СДЕЛАН, И РАЗНИЦА ЗДЕСЬ НЕСУЩАЯ.**

Замер после отката D-3, снят через `information_schema`: колонка `deleted_at` осталась ровно на
ПЯТИ таблицах — `accounts`, `precinct_groups`, `samples`, `site_summaries`, `sites`. Двух
таблиц, которые держались не на механизме, а на внимательности, в списке больше нет:
`spray_logs` и `field_log_entries` потеряли колонку сегодняшним откатом D-3 по решению владельца
(«давай жёстко только журнал удалять, оставим как было с пробами»).

**Вопрос снят ПОБОЧНЫМ СЛЕДСТВИЕМ другого решения, а не работой по нему.** Никто не приводил
два основания к одному; просто исчезли те две таблицы, на которых основание было слабым.

**Отсюда условие возврата, ради которого эта запись и стоит:** если мягкое удаление когда-нибудь
вернут журналам, вопрос вернётся вместе с ним, потому что вернётся ровно та конструкция —
колонка есть, а соблюдается она вниманием пишущего. Записать здесь «сделано» значило бы обещать,
что конструкция разобрана. Она не разобрана, она сегодня отсутствует.

---

**Вопрос 39. Средство возврата стенда отказывает честно и при этом не может вернуть 11 строк из 24. Замер разработчика 18.09.2026, повод — упавший живой прогон.**

**ЧТО ИЗМЕРЕНО.** `restoreConfigs()` вернул **0 строк** и сам назвал отказ:
`exec /usr/bin/mysql: argument list too long`. Из 24 строк `site_configs` средство не может
вернуть **11** — все `analysis_cache`, от 210 до 234 КБ в base64. Это ровно те строки, которые
переписываются простым открытием страницы.

**ПРИЧИНА НАЙДЕНА, и она не в размере данных.** Предел — **128 КБ на один аргумент внутри
контейнера**, не `ARG_MAX` хоста (там 1 МБ). Первая догадка разработчика про argv была неверна и
названа им неверной. Предел в СПОСОБЕ ПЕРЕДАЧИ: содержимое идёт аргументом командной строки.

**НАПРАВЛЕНИЕ, которое из этого следует:** средство чинится не уменьшением строк, а другим
способом передачи — **НЕ ЧЕРЕЗ АРГУМЕНТ**. ~~файлом вместо аргумента~~

**Формулировка исправлена 19.09.2026, после того как работа развела слово и дело.** Предмет
вопроса — предел на ОДИН АРГУМЕНТ внутри контейнера; чинит его то, что содержимое перестаёт
быть аргументом. Файл на диске был ОДНИМ ИЗ способов это сделать и попал в формулировку потому,
что Canberra возвращали руками через `docker cp`: из разового приёма сделали описание
направления.

**Замер решает спор прямо:** запрос в 468 827 символов через argv даёт
`argument list too long`, через stdin возвращает значение. Аргумента нет — предела нет.

**Реализация, принятая владельцем очереди 19.09.2026: stdin.** Файла на диске нет, и
разработчик назвал это отклонением сам, прежде чем его спросили.

**СДЕЛАНО И ДОКАЗАНО 19.09.2026. Чем именно — два круговых прогона, оба напечатаны.**

- Строка СВЕРХ предела: `Test1 - Sports`, `analysis_cache`, 168 100 байт, 224 136 base64 — не
  помечена, не Canberra, не Test5 - NZ. Снято, ИЗМЕНЕНО, возвращено: md5 `6e265e4a…` →
  `6e265e4a…`, длина 168100 → 168100, `updated_at` 1786331199 → 1786331199.
- Строка ПОД пределом: `Westview`, md5 `a580c178…` → `a580c178…`, 68306 → 68306.
- Оба значения сверены с базой координатором проекта после прогона.
- `restoreConfigs` отчитался сам: put back 2 row(s), 22 were untouched, отказов нет.
- Положительный контроль: вернули старый argv-путь — 3 красных из 5, `Westview` восстановился,
  `Test1 - Sports` отказал. Мутация дошла до предмета.
- Пять вызовов `sqlRaw` прошли новым путём и все исполнены круговым прогоном.
- Стенд: 0 различающихся строк против снимка до всей работы, **через три прогона, два из
  которых намеренно портили по две строки.**

**СТРАХОВКА СРАБОТАЛА ПО-НАСТОЯЩЕМУ, И ЭТО ГЛАВНОЕ В ЭТОЙ ЗАПИСИ.** Требование «если новый путь
не сработает с первого раза, строка возвращается НЕМЕДЛЕННО проверенным ручным способом;
испорченная строка не ночует» было записано в задание за час до работы — и понадобилось в тот
же день: в положительном контроле, где старый путь намеренно вернули, `Test1 - Sports`
осталась испорченной и была починена из собственной копии прямо в том же прогоне —
`MANUAL REPAIR … ["Test1 - Sports: repaired"]`.

**Правило проверено не рассуждением, а случаем**, и случай нашёлся в первый же час его
существования. Это тот признак, по которому мы отличаем введённое правило от записанного.

**ПРИНЯТО РЕВЬЮЕРОМ 19.09.2026. Приёмка независима: другая строка, свой круговой прогон.**

- Сверх предела он взял **`test4 - USA`** (168 043 байта), а не `Test1 - Sports`, чтобы не
  доказывать тот же случай второй раз.
- Под пределом — `Westview`, и **он сам назвал это ограничением, а не выбором**: под пределом
  свободна только она, `Russley` и `Federal Golf` помечены.
- Копии обеих строк сняты на диск и сверены с базой ДО порчи.
- **Контрольный прогон БЕЗ мутации — 5 из 5 зелёных.** Обе строки испорчены до 51 байта и
  возвращены байт в байт: md5, длина и `updated_at` совпали.
- **Мутация — возврат старого argv-пути: 3 красных из 5.** Крупная строка отказала с
  `argument list too long [statement 224252 chars]`, `Westview` вернулась. Мутация дошла до
  предмета, и различила она ровно размер.
- Испорченную `test4 - USA` починил сам прогон из независимых копий —
  «MANUAL REPAIR from the independent copies: test4 - USA: repaired». **Требование
  «испорченная строка не ночует» сработало по-настоящему второй раз за сутки.**
- PHPUnit 200 / 864. Jest 3059 / 45 skipped / 233 suites.
- Стенд после всего сверен координатором проекта: 24 / 148 / 109 / 1534; `test4 - USA`
  `8624e2a1…` / 168043, `Westview` `a580c178…` / 68306, `Test1 - Sports` `6e265e4a…` / 168100,
  `Canberra` `fd90871f…` / 168087 с меткой `2026-09-17 12:28:42` — все как до работы.

**ПОПРАВКА К ПЕРЕЧНЮ ВЫЗОВОВ: их ЧЕТЫРЕ, а не пять.** `:293`, `:316`, `:329`, `:336`. Пятое
вхождение — само объявление `function sqlRaw` на `:261`; «пять» получается, если считать
`grep`-ом вместе с ним, и это ровно тот счёт, который сделала координатор проекта.

**И утверждение ревьюера сильнее перечня:** в `stand-guard.js` ровно ОДИН `execFileSync` — на
`:263`, внутри `sqlRaw` (второе вхождение имени, `:229`, — это `require`). Другого пути к базе в
файле нет вовсе, **поэтому «все вызовы пошли новым путём» верно по построению, а не по
перечислению.** Перечень можно не полагать полным; построение полноты не требует.

**ЗАМЕТКА, НЕ НАХОДКА: размер в base64 считается двумя способами и расходится на ~1,3%.**
`TO_BASE64` в MySQL ставит перенос строки каждые 76 символов, тест считает форму без них: для
`test4 - USA` это 227 008 против 224 060, для `Westview` — 92 274 против 91 076. **Таблица
размеров выше снята первым способом, а предел проверяется вторым.** Проверено: ни одна строка от
этого не меняет разряда — девять остаются над 131 072 в обеих формах, три под ним в обеих. Но
если это число куда-то попадёт как «вес строки», знать надо, что их два.

**СЛЕДСТВИЕ, шире повода.** Каждый живой тест, объявивший `captureConfigsOnce`/`restoreConfigs`
своим средством, защищён для 13 мелких строк и **не защищён для 11 крупных**. По переписи
GH-532 таких файлов 21 из 35.

**ТОЧНАЯ ГРАНИЦА ВОПРОСА, иначе он читается шире, чем он есть.** УВИДЕТЬ изменение мы могли
всегда: снимок сравнивает md5 всех строк, и именно так сдвиг Canberra и обнаружился.
Не могли **ВЕРНУТЬ**. Обнаружение работало, восстановление нет.

**ПОЧЕМУ ДЫРА ЖИЛА ДЕНЬ.** Отказ у средства ГРОМКИЙ — оно печатает его само. Формулировка
разработчика: «средство отказывает честно, но не может». Дыра была не в молчании, а в том, что
громкий отказ приняли за покрытие: средство объявлено — значит защищено.

**ТРЕТЬЕГО РАЗРЯДА У ПЕРЕПИСИ НЕТ, И ЭТО ТЯЖЕЛЕЕ САМОГО ОТКАЗА.** Перепись GH-532 делит живые
файлы на два разряда: «средство названо» и «средства нет, стоит в исключениях с датой».
Разряда **«средство названо и на этих данных не работает»** у неё не существует.

Повод — `calc-audit-export-parity-live`. В `EXEMPT` его нет, значит перепись числит его среди
файлов со средством. Строка 30 файла: `const { fillOwnAnnualN, captureConfigsOnce,
restoreConfigs } = require('./lib/stand-guard')`, захват на `:212`, возврат на `:394`. Средство
у него ровно то, которое не возвращает крупные строки, а жмёт он `#plan-nut-generate-btn` на
одиннадцати площадках — ту самую кнопку, что переписывает `analysis_cache`.

**Это хуже, чем отсутствие средства: отсутствие помечено и имеет дату, а неработающее выглядит
покрытием.** Четырнадцать без средства перечислены поимённо и запрещены к запуску; этот файл не
запрещён ничем.

**ПОСТРОЧНЫЙ ЗАМЕР КООРДИНАТОРА 18.09.2026, вечер. Он уточняет число и даёт запасной путь.**
Строк `analysis_cache` в таблице **двенадцать**, и предел бьёт не по всем:

| площадка | байт | base64, байт | base64, КБ | против предела 128 КБ |
|---|---|---|---|---|
| Test5 - NZ | 175 773 | 237 447 | 232 | над |
| New test - location | 174 538 | 235 782 | 230 | над |
| Burns | 172 918 | 233 593 | 228 | над |
| Test - GC - NZ - delivery | 171 798 | 232 077 | 227 | над |
| Test6 - UK | 171 279 | 231 376 | 226 | над |
| Test1 - Sports | 168 100 | 227 085 | 222 | над |
| Canberra | 168 087 | 227 064 | 222 | над |
| test4 - USA | 168 043 | 227 008 | 222 | над |
| Test - GC - NZ - warm season grass test | 158 061 | 213 520 | 209 | над |
| **Westview** | **68 306** | **92 274** | **90** | **ПОД** |
| Russley | 1 325 | 1 791 | 2 | под |
| Federal Golf | 783 | 1 057 | 1 | под |

**Девять строк над пределом, три под ним.** Практическое следствие: для площадки, чья строка
под пределом, capture-and-restore ГОДИТСЯ, и выбор средства делается замером размера, а не
правилом «это средство сломано».

**РАСХОЖДЕНИЕ С ЧИСЛОМ РАЗРАБОТЧИКА, названо, а не сглажено.** Он измерил «11 строк из 24, все
`analysis_cache`, base64 от 210 до 234 КБ». Под определением выше не сходится ни 11 (у меня 9
над пределом), ни диапазон (нижняя строка над пределом — 209 КБ, а не 210). Вероятное
объяснение: он мерил длину всего аргумента команды, а не только base64 — в аргумент входит и
текст SQL вокруг него. Это ВЕРСИЯ, не замер. Закрывается одним вопросом к нему: что именно он
считал.

**ОТВЕТ РАЗРАБОТЧИКА И ПРОВЕРКА ЕГО: версия координатора неверна, его объяснение тоже не
сходится, расхождение остаётся открытым.** Он ответил: считали одинаково, только base64, а
разошлись ДАТЫ — за день две строки `analysis_cache` схлопнулись с ~228 КБ до 1768 и 1044 байт,
и в его счёте они ещё были крупными.

Объяснение стройное, и оно НЕ СХОДИТСЯ с метками. Обе схлопнувшиеся строки — Russley и Federal
Golf (Вопрос 31) — несут `updated_at` **18.09.2026 01:26:24** и **01:26:16**, то есть
схлопнулись в 11:26 по местному, ДО обоих наших замеров: его — около 19:45, моего — около 20:30.
На момент его счёта они уже были мелкими, и по его же определению он должен был получить 9.

Второе, мельче, но того же рода: его размеры схлопнувшихся строк — 1768 и 1044 — не равны моим
1791 и 1057, снятым через `LENGTH(TO_BASE64(config))`, при том что `updated_at` между замерами
не двигался. Разница в 23 и 13 байт — это разница СПОСОБА, а не момента.

~~**Итог: 11 против 9 не объяснено ни его версией, ни моей.** Записано открытым.~~

**РАСХОЖДЕНИЕ ЗАКРЫТО 19.09.2026 РАЗРАБОТЧИКОМ. Оба числа верны, каждое для своего момента, и
ошибка была в МОЁМ возражении.**

Его источником был не живой стенд, а бэкап, снятый **18.09 в 09:20:38**. Russley и Federal Golf
схлопнулись в **11:26:16** и **11:26:24** — на два часа ПОЗЖЕ. В его данных обе строки ещё были
крупными, поэтому над пределом их было 11; в моём замере, снятом с живой базы вечером, их 9.
Считали мы одинаково.

**Моё возражение строилось на времени, когда он ВЫПОЛНИЛ команду, а считать надо время, к
которому относятся ДАННЫЕ.** Его формулировка: «твоё возражение исходило из даты моей команды,
а считать надо дату данных». Я применила правило «у замера есть дата» к замеру и не применила к
его источнику.

**Заодно закрылось мелкое расхождение 1768 против 1791, и тоже замером, а не версией.**
`TO_BASE64` вставляет перенос строки каждые 76 символов: 1791 − 1768 = 23 при 23 ожидаемых
переносах, 1057 − 1044 = 13 при 13. Проверено пересчётом. Для средства правильна форма БЕЗ
переносов — именно её оно и шлёт.

**ЗАМЕР ПОДТВЕРДИЛ ЛЕКАРСТВО, 18.09.2026, 20:12.** `guardStand` поставлен вместо
capture-and-restore в `calc-audit-export-parity-live` и **поток его пережил**: двенадцать строк
`tr.gilba-nut-row` дошли до экрана при задержанной записи, прогон выдал числа, сторож удержал
**4 записи** в `site_configs`, стенд не сдвинулся ни на строку. Опасение, что guardStand
задержит запись, без которой нечего мерить, на этом тесте не подтвердилось.

**Отсюда направление уточняется:** для тестов, которые только ЧИТАЮТ результат пресса, guardStand
строго лучше возврата — он не даёт записи лечь, тогда как возврат для девяти строк из двенадцати
невозможен. Переделка средства на передачу файлом остаётся нужной для тех случаев, где запись
обязана лечь.

**Для вывода вопроса расхождение ничего не меняет:** крупные строки не возвращаются при обоих
счётах, и разряд «средство названо и не работает» существует при любом из них.

**ЧЕГО НЕ ИЗМЕРЕНО:** сработает ли передача файлом на крупных строках; есть ли среди прочих
наших средств то же устройство передачи; что именно считал разработчик.

**В работу не берётся.**

---

**Вопрос 40. Расхождение паритета по Westview закрылось само, и мы не знаем чем. Заведён 18.09.2026 по требованию координатора: тихо вычёркивать нельзя.**

**ЧТО УСТАНОВЛЕНО.** Пара `Westview / Backyard` расходилась утром 18.09.2026 и сходится вечером
того же дня на том же стенде, с тем же выбранным флажком.

- Утро, `generatedAt 2026-09-18T02:24:16.600Z`: `docx delivered = —/—/—`, `balance = —`,
  `status = —`, при `plan delivered = 224.8/21.4/154.1`.
- Второй утренний захват, годовой N 150: `plan del=168.1/17.3/110.2 | docx del=—/—/—`.
  **Прочерки не зависели от чисел** — значит утро не было случайностью одного прогона.
- Вечер, `generatedAt 2026-09-18T10:12:16.024Z`: `docx delivered = 224.8 / 21.4 / 154.1`,
  `balance = 24.8 / 1.4 / 29.1`, `status = On Track / No Soil Data / No Soil Data`.
- Флажок в обоих прогонах один и тот же: `exportPick.uid = 019f7d28-…::sample_118`, `checked: 1`.

**КАКОЙ СДАЧЕЙ ЗАКРЫТО — НЕ УСТАНОВЛЕНО.** Слова разработчика: «Назвать GH-529 или GH-531
значило бы выдать совпадение по времени за причину».

Что против привязки по времени: `nutrition-delivery-core.js` и `word-export.js` правлены сегодня
в 16:24, между последним захватом с прочерками (11:18) и вечерним прогоном — совпадение есть.
Что против: **ни одна запись от GH-529 до GH-536 не описывает правку колонки `delivered` в блоке
Annual Nutrient Requirements**, последняя запись по этому блоку — GH-423, а GH-529 говорит прямо
обратное: «delivered, the Annual Nutrient Requirements figure, did not [move]».

**ЧЕМ УСТАНАВЛИВАЕТСЯ:** прогоном пары на промежуточных состояниях дерева.

**ПОЧЕМУ НЕ ДЕЛАЕМ:** дефект закрыт, а поиск автора стоит дороже пользы. Решение владельца.

**ЧЕГО ЭТО СТОИТ НАМ ЗНАТЬ, и ради этой строки вопрос и заведён: мы починили дефект, не зная,
что чиним его.** Закрытый вопрос без названного автора выглядит как решённый; через месяц никто
не вспомнит, что автор неизвестен. Поэтому он стоит здесь отдельным номером, а не зачёркнутой
строкой в Вопросе 33.

**НАХОДКА О НАШЕЙ СОБСТВЕННОЙ ФОРМЕ ОЖИДАНИЯ, попутная и проверенная на себе.** Ожидание было
написано ДО прогона двумя ветвями: «сойдётся — назвать, какой сдачей закрыто; разойдётся —
объяснений нет». Первая ветвь сработала, а её вторая половина оказалась невыполнимой:
**схождение показывает, что расхождения нет, и не указывает на причину.** Форма ожидания была
написана так, будто «сойдётся» влечёт «знаем чем». Не влечёт.

---

## Закрытые вопросы — здесь только след, в работу не берутся

Номера сохранены: на них ссылаются переписка и changelog, поэтому в живом списке выше остаются пропуски. Это сознательно — перенумеровать значит рассинхронизировать документ со всем, что на него уже сослалось.

**Вопрос 3. Программа без штампа координат молча заменяется более старой со штампом. ЗАКРЫТ ПО ДОСТИЖИМОСТИ GH-404 — и прежняя проверка была неверной.** В `SiteController::resolveGaipConfigWrite()` программа без штампа не проходит проверку доверия и отбрасывается; если координаты в этом запросе не менялись, из базы возвращается прежняя. Нажал «Сгенерировать», увидел новые цифры, перезагрузил — старые, без единого сообщения.

**Чем была плоха проверка, которой мы его почти закрыли.** Мы смотрели в базу: программ без штампа там нет — и сочли дефект недостижимым. Но сервер сам стирает такую программу перед записью (`SiteController.php:534-536`), поэтому в базе её не может оказаться никогда — ни когда дефект срабатывает, ни когда нет. Пустой результат запроса одинаково совместим с обоими исходами. Урок общий: достижимость проверяется тестом на пути записи, а не запросом к тому, что этот путь как раз и отфильтровывает.

**Настоящая причина.** Штамп ставится в `nutrition-calendar.js:2447-2456` только если `meta.lat`/`meta.lon` — числа, а `meta.lat` берётся как `typeof inputs.latitude === 'number' ? inputs.latitude : null` (`:1787`). Широта собирается цепочкой из пяти источников (`:416`) и имеет право оказаться `null` — так и написано в коде. То есть окно открывалось ровно тогда, когда у сайта не было координат.

**Закрыто с другой стороны, решение пользователя 10.09.2026:** чинить надо не штамп, а то, что координаты вообще были необязательными — «без координат не будет никакого корректного расчёта и анализа». GH-404 не даёт сохранить сайт без координат в Settings, и окно закрывается. Сам разбор доверия в `resolveGaipConfigWrite()` не трогали.

**Вопрос 4. Одиночный экспорт недостижим из нового хаба — ЗАКРЫТ 10.09.2026 как неверно сформулированный, переведён в мёртвый код, см. вопрос 12.** Прежняя формулировка звучала как «одиночный экспорт недостижим из нового хаба», то есть как утраченная возможность. Возможности такой нет и не должно быть: экспорт у продукта **один**. Страница `/reports/export` вызывает `GAIP_CombinedExport.exportWithPicker('all')` (`export.blade.php:336`) — одна кнопка, один путь; сколько проб отмечено в пикере, столько и попадает в документ, будь их одна или пять. Так и задумано. «Одиночный экспорт» — это старая ветка внутри `word-export.js`, оставшаяся от прежнего хаба; файл ещё подключается, но из нового хаба в эту ветку никто не заходит, дотянуться можно только с `/hub`. **Хвоста от GH-388 не осталось, проверено:** пометка «цель по азоту взята из настроек» есть в комбинированном экспорте (`word-export-combined.js:3432`), появилась ещё в GH-383 и была проверена живьём в GH-388 — у сайта временно убрали сохранённую программу, и в выгруженном документе над таблицей Annual Nutrient Requirements напечаталось «Note — annual nitrogen target source: Burns (Site Settings → Turf)…», конфиг затем восстановили и сверили по контрольной сумме. GH-388 добавил ту же формулировку во вторую, мёртвую ветку, чтобы текст был один на обоих путях, — вот её и не смотрели глазами, и смотреть незачем. Остаётся только удалить ветку: перенесено в пункт 12.


**Вопрос 9. Решения, без которых нельзя слить помесячное распределение (Stage 4 объединения движков).** Возвращены разбором GH-394 с замерами на реальных модулях; блокируют пункт 1 списка работ. Все решены 10.09.2026, кроме одного, который отложен вместе со своим режимом.
   ✅ **(а) Потолок азота на месяц — РЕШЕНО: отчёт делает как план,** включая перераспределение излишка и отслеживание неразмещаемого остатка. Проверено, что расхождение живое, а не теоретическое: у календаря есть `applyNCap` (`nutrition-calendar.js:1427`) — режет месяц по `maxNPerMonth` и итеративно раскладывает излишек в месяцы с запасом; у движка экспорта такой функции нет вовсе. В базе потолок 15 стоит у четырёх сайтов из десяти, и он реально срабатывает: у Federal Golf сохранённая программа `[15, 15, 15, 12.7, 9.9, 0, 0, 0, 10.1, 12.3, 15, 15]` — пять месяцев обрезаны ровно по потолку, сумма 120 сохранена перераспределением. Отчёт для того же сайта напечатал бы неограниченный ряд. На Test5 не срабатывает (пик ~35 при потолку 50) — поэтому харнесс этого и не поймал. Реализовать вызовом существующей функции, не копией. **Остаётся решить одну деталь:** при обрезке часть азота может не поместиться никуда, и тогда сумма месяцев окажется меньше годовой цели; план это отслеживает отдельным числом. Отчёт должен показывать такую ситуацию явно — молча печатать несходящуюся сумму нельзя.
   ✅ **(г) Округление — РЕШЕНО: считать в полной точности, округлять только при выводе.** Сегодня движок округляет потенциал роста до сотых *до* раскладки и каждый месяц до десятых, календарь держит сырое до печати; отсюда расхождения в десятых и годовая сумма 250.0 против 249.9. Округление промежуточных значений и есть источник дрейфа.
   ✅ **(д) Помесячная раскладка остальных элементов — РЕШЕНО: общая функция берёт на себя все элементы, но в отчёте пока печатается только азот.** То есть объединяем целиком, без второй копии в календаре, а показывать ли клиенту помесячные P/K/Ca/Mg/S — отдельное решение на потом, когда понадобится. Считаются они в любом случае.
   ✅ **(е) Сторож против новых копий — РЕШЕНО: делать,** вместе с самим слиянием. Тест, который падает, если собственная помесячная арифметика появляется в календаре или в экспорте вместо вызова ядра. По образцу `gh383-input-contract-guard`, который за эту сессию уже дважды поймал возврат удалённого чтения. Писать его **до** слияния бессмысленно — сегодня обе копии существуют законно, и тест был бы красным by design.
   ✅ **(б) Смешение C3/C4 при подсеве — РЕШЕНО: берём подход экспорта, доли оставляем как есть.** План сегодня считает подсеянное поле чистым C3 или чистым C4 все двенадцать месяцев, то есть описывает дернину, которой не существует: летом там работает тёплосезонная основа, зимой подсеянный райграс. Экспорт считает потенциал роста для смеси помесячно, линейной интерполяцией между двумя кривыми, с долей C3 из четырёх профилей «летнего намерения» (`transition` 0.10 лето / 0.40 межсезонье / 0.90 зима, и три других), сезоны определяются по полушарию. Разница на подсеянном поле большая: пять месяцев у одной стороны нулевые против 13.5 кг у другой, январь 62.4 против 27.7. **Довод в пользу экспорта не «сложнее, значит лучше»:** сама модель потенциала роста процитирована — PACE Turf, Gelernter & Stowell 2005, и работа называется «Improved overseeding programs 1. The role of weather», то есть опубликована именно про подсев и применяется здесь по назначению. **Оговорка, которую надо не потерять:** сами четыре профиля не процитированы — комментарий над ними ссылается на другой наш же файл (`nutrition-summary-integration.js v1.1.3`), тот же признак, на котором таблица трафика оказалась тупиком. Числа задают форму годовой программы, поэтому их стоит показать Спенсеру, как показывали коэффициенты клиппинга. Сегодня не горит: сайтов с подсевом в базе нет.
   ⏸ **(в) Что делает `front_loaded` в северном полушарии** — не решается здесь: сам режим отложен в конец списка работ (пункт 14), потому что им никто не пользуется. Вернуться вместе с ним.

   **Итог: все решения приняты, пункт 1 списка работ разблокирован.**

**Вопрос 14. Тёплосезонная трава считалась как прохладносезонная — ЗАКРЫТ GH-408, найдено пользователем в интерфейсе 10.09.2026.** Признак «база C4» был зашит как `false` в трёх местах сразу: в умолчании движка требований и в обоих экспортах, причём в `word-export.js:7165` прямо над этой строкой стоял комментарий «пустое состояние подсева = чистый C4» — код противоречил собственному описанию. Обе поверхности зовут одну общую функцию `monthlyGP()`, им просто давали разные доли C3. То есть расхождение входное, а не арифметическое — тот же класс, что три случая, закрытых в D31. **Как проявлялось:** на гольф-сайте в Крайстчёрче с кувчем план печатал GP 16 % за январь, документ — 90 %, и дальше документ раскладывал все 250 кг N/га по девяти «активных» месяцах, которых у тёплосезонного тёрфа на этой широте нет, тогда как план под тем же потолком мог запланировать только 100. Проверено прогоном самого движка: Крайстчёрч, январь — кувч **15 %**, бентграсс **89 %**. Красная проверка возвращает `Couch 89%, Bentgrass 89%`, то есть в точности исходный симптом. **Случай не редкий:** тёплосезонных сайтов в базе два из одиннадцати (кувч и буффалограсс), а кувч на австралийских гольф- и спортивных полях обычное дело. **Урок для меня:** первый фикс закрыл только умолчание движка, потому что я оборвала вывод `grep` на десяти строках и решила, что конфиг подсева никто не передаёт. Передают оба экспорта. Одна таблица починилась, вторая нет — и это увидел пользователь, а не тест.

**Вопрос 15. ЗАКРЫТ GH-409 — таблицы продуктов в отчёте печатают колонки и единицы как в плане.** Колонки сведены к N, P, K; единица определяется поверхностью и стоит у каждой строки. Проверено на живых документах: `400 / 90 / 616 / 20 / 90` стало `40 g/m² / 90 L/ha / 61.6 g/m² / 2 g/m² / 90 L/ha`. Попутно вскрылось, что рендереров этой ячейки не три, а четыре, и таблица закупки не пользовалась общей функцией колонок вовсе. Исходная формулировка ниже.

   совпадают с планом по колонкам и единицам — В РАБОТЕ GH-409.** Две находки пользователя, одна суть: документ печатает не то, что экран. (а) В отчёте появились колонки Mg и S: колонка включается, если хоть один подобранный продукт несёт этот элемент, поэтому набор колонок меняется от сайта к сайту. В плане всегда N, P, K. **Решение пользователя: показываем N, P, K; считается по-прежнему всё.** (б) Для гринов план печатает норму в граммах на квадратный метр (`40 g/m²`), отчёт — в килограммах на гектар (`400 kg/ha`). Количество одно, запись разная. Правило берётся из плана: жидкость идёт литрами и побеждает гриновое правило, растворимый порошок всегда в килограммах. Литры в таблице закупки уже сделаны в GH-406; не хватает гринов.

**Вопросы 17–22 — из аудита расчёта и подбора, 11.09.2026.** Полный отчёт дословно в `AUDIT-calculation-and-selection-RU.md`; замеры — 50 генераций плана и 14 сравнений с документом, харнессы `tests/e2e/calc-audit-all-sites-live.test.js` и `calc-audit-export-parity-live.test.js`. Что аудит **подтвердил**: арифметика сходится на всех 50 генерациях по семи инвариантам, потолок азота перераспределяет а не теряет, округление одно, план и документ совпадают цифра в цифру на семи парах, GH-408 держится.

**Вопрос 17. ЗАКРЫТ GH-413 — pH пробы доходит до движка, и диапазон в документе считается от того же пола.** Живьём: «New test» Green 5 Required P 15.6 → 32.4, диапазон в документе 37.8–75.6 → 71.4–75.6; Green 4 → 15.7; Green 2 → 14.5. Обе поверхности совпадают посимвольно. Исходная формулировка ниже.

   жок — настоящее неверное число.** Пикер плана копирует в состояние P/K/Ca/Mg/S и микроэлементы, но **не pH и не CEC**; на всех 50 генерациях в движок ушло `pH = null`. На «New test - location» (SLAN) проба Green 5 с pH 8.26: план печатает Required P **15.6**, документ — **32.4**; Green 4 с pH 7.52 — **12.0 против 15.7**. Доставка при этом одинаковая, то есть на плане фосфора «почти достаточно», а в документе вдвое меньше нужного. Внутри документа тоже противоречие: колонка Range для Green 5 напечатана от пола 27, а Required посчитан от пола 51. Затронуты минимум четыре пробы с pH > 7.5 и две с pH < 6.0; на MLSN-сайтах сейчас не проявляется только потому, что их пробы лежат в 6.7–7.4, но лестница MLSN на плане тоже мёртвая. **Побочно: комментарий в коде утверждает, что проб вне 6.0–7.5 в базе нет — неверно**, аудит перечислил семь.

**Вопрос 18. ЗАКРЫТ GH-414 — одна текстура и ткань только своего грина.** Настройка сайта побеждает снимок пробы; сопоставление ткани с зоной вынесено в общий модуль и применяется на обеих поверхностях, подстановки «единственной ткани на сайте» больше нет. Живьём: Russley Green 18 P 0.0 / K 119.0 и диапазоны 7–21 / 109.5–273.7 на обеих поверхностях; Green 1 и Green 13 остались без ткани, в документе печатается «No tissue sample for this zone». Исходная формулировка ниже.

   азные требования и разные программы.** План даёт Required P 0 и K 119, документ — P 20 и K 106.7, другие диапазоны, статус фосфора «Deficit (−54%)» вместо «On Track», и в программе появляются три продукта, которых на плане нет. Две причины: **(а)** разрешение текстуры почвы ставит снимок пробы выше настройки сайта — у проб Russley в снимке `loam`, у сайта задан `sand`; план снимка не видит и берёт `sand`, экспорт берёт `loam`; **(б)** тканевая проба помечена «18th Green», почвенные — «Green 18», «Green 1», «Green 13», и экспорт связывает ткань с почвой по метке, а план берёт активную ткань без сверки. На Test5 обе поверхности совпали — метки одинаковые и текстура одна. **Значит это не общий сбой, а зависимость от согласованности данных пробы.** Для клиента — два документа с разными цифрами на один и тот же грин.

**Вопрос 19. ЗАКРЫТ GH-417 — заметка печатает доставленное.** Живьём: `Strategic P application: 5.4 kg P/ha (of 19.8 requested)`. Попутно выяснилось, что записанная ранее версия про счётчик неверна: его никто не читает, и правка не двигает ни одной цифры. Исходная формулировка ниже.

   ет запрошенное, а не доставленное.** GC-NZ delivery, март: «Strategic P application: 19.8 kg P/ha», продукт даёт **5.4**. На кувч-сайте — «25.0 kg P/ha» при тех же 5.4. Внутренний счётчик тоже прибавляет запрошенное, поэтому программа считает фосфор закрытым и повторных внесений не делает.

**Вопрос 20. ЗАКРЫТ GH-416 — «нет пробы» так и говорится.** Баннер на плане берётся из собственного признака движка и показывается на Canberra, Test1, Westview и USA, больше нигде. Исходная формулировка ниже.

   т полную программу продуктов.** Canberra и Test1 - Sports живых проб не имеют; пикер пишет «No soil samples for this site», но генерация работает и выдаёт семь продуктов на 207/23.6/122.7 и 148/11.9/88.4 соответственно. Баннера «программа построена без пробы» нет ни на плане, ни в документе. **Прямо против правила «без частичных расчётов — показывать, чего не хватает».**

**Вопрос 21. ЗАКРЫТ GH-416 наполовину, вторая половина снята решением.** Документ Westview получил таблицу требований и помесячное распределение и совпал с планом. Отсутствие каталога для США закрыто решением пользователя — США вне работ наравне с Британией. Исходная формулировка ниже.

   з требований, а для США молчит вовсе.** Экспорт исключает из таблицы требований пробы без фосфора и калия, поэтому у Westview в документе нет ни требований, ни помесячного распределения — но есть таблица продуктов на 224.8 кг азота и расписание; читатель не видит, подо что подобраны продукты. **США исключены из работ решением пользователя 11.09.2026, наравне с Британией.** У `test4 - USA` нет региональной интеграции вовсе: на плане рендерится один календарь без сводки доставки и без сообщения, что каталога для этой локации нет. Каталога продуктов для США у нас нет и сейчас не делаем, поэтому вопрос сводится к британскому — вернёмся, если появится рынок. В работе остаётся только половина про Westview: документ печатает продукты без требований.

**Вопрос 22. Подбор объясним, но не объяснён. Решение пользователя 11.09.2026: австралийский алгоритм пока не меняем.** Перебор азота на австралийских гринах +5…+21 % принят как есть. Причина отсрочки названа явно: **возможно, позже мы перенесём в Австралию новозеландский алгоритм целиком** — там фаза снижения излишка уже есть, — и тогда точечная правка сейчас была бы выброшена. Пока этого не произошло, австралийский подбор не трогаем.

   *Закрыто там же:* строку-объяснение про запас от минимальных норм в отчёте **тоже не печатаем** — решение пользователя 11.09.2026, оставляем всё как есть. Следствие, которое надо помнить: у азота нет верхней границы статуса, поэтому документ печатает «Required 120, Delivered 145» и подписывает «On Track» без пояснения.

   *Ниже — что именно замерил аудит.*  Занижает системно только фосфор: на австралийских гринах он физически не превышает ≈12.3 кг/га, потому что берётся продукт с гриновым ограничением 15 кг/га и внесения начинаются только с весны — отсюда Burns Green 10 (2014) 26.3 → 12.3. Агрономически защитимо, но **годового недобора нигде не сказано**. Завышает азот на 5–21 % на австралийских гринах из-за минимальных норм этикетки и перекрытия медленных продуктов; **в австралийском рекоммендере нет фазы снижения излишка, а в новозеландском есть** — отсюда систематическая разница между регионами. Калий часто идёт прицепом к азотному продукту (Test5 +29 %). Отдельно Test1: перебор от границы календаря — декабрьский продукт «покрывает» январь и февраль следующего года, о которых рекоммендер не знает.

**Мелочи оттуда же, в работу пока не берутся:** дублирующиеся метки проб в пикере различимы только датой (у Burns четыре «Green 4»); годовой азот на плане и в настройках расходится на девяти сайтах из одиннадцати — известное поведение по решению D-4b, но клиент это увидит; на Burns 12th Fairway `pH` 6.6 при `pH_Water` 5.5, и какое из двух верно — из кода не следует.

**Вопрос 23. Кривые высвобождения заданы по температуре почвы, а мы даём им температуру воздуха. Решение пользователя 11.09.2026: оставляем как есть, в продукте не пишем. Сама температура исправлена в GH-427.**

   Новозеландский подбор продуктов решает, сколько процентов заявленного продукт реально отдаст, по кривой «эффективность от температуры». Кривые сослаются на Spencer 2008 (таблица 18) и Cornell Turf BMP, механизм высвобождения микробный, и порог у Корнелла задан прямо: «азот почти не высвобождается, пока **температура почвы** ниже 10 °C». Мы подаём туда воздух.

   **Исправлено в GH-427.** Теперь месячная температура берётся из строки календаря — двадцатилетней нормы NASA по координатам сайта, той же, на которой стоит потенциал роста. Функция достройки года из одного дня удалена целиком, 67 строк. Живьём сдвинулась ровно одна пара из одиннадцати — документ Test5, и теперь он равен своему плану; ни одна цифра плана не сдвинулась нигде, за пределами Новой Зеландии ничего. `npm run test:e2e` на фикстуре Test5 стал 22/22 — красным он был с GH-415.

   **Что было до этого:** раньше туда шло среднее прогноза погоды на ближайшие дни, объявленное среднегодовым, и двенадцать месяцев достраивались из этого одного числа зашитой таблицей отклонений. Замерено: кривая уезжала целиком на 8.4 °C в зависимости от месяца генерации, а семь реальных суточных средних подряд давали **две разные программы за одну неделю**. Теперь берутся двадцатилетние месячные нормы по координатам сайта — те же, на которых стоит потенциал роста.

   **Почему не перевели в почву.** Наша модель `estimateSoilTemp()` рассчитана на **суточный** период (86400 с) — она описывает, как дневной нагрев проникает вглубь. На месячном среднем её поправка сокращается: замерено, что вариант «нормы воздуха напрямую» и вариант «нормы воздуха через модель» дают **побайтно одинаковые программы на всех четырёх новозеландских сайтах**. Достижимая без новых данных ветка модели — «воздух минус 1.5» с надёжностью 30, и она делает почву холоднее воздуха во все двенадцать месяцев, тогда как реальная почва зимой теплее.

   **Что осталось бы сделать, если вернёмся.** (1) Климатология по измеренной температуре почвы: Open-Meteo отдаёт `soil_temperature_0_to_7cm` из ERA5-Land с 1950 года, сетка 9 км — тот же механизм, что уже работает для воздуха у NASA, только другой поставщик и параметр. (2) Годовая синусоида из наших же двенадцати норм: сезонная амплитуда есть, нужна температуропроводность по текстуре. (3) Регрессия по воздуху **отпала** — модели такого рода калиброваны по регионам (Zheng, Hunt & Running 1993, шесть регионов США, отдельное уравнение на каждый), коэффициентов для Австралии и Новой Зеландии нет, и вывести их можно только имея измерения — то есть путь (1).

   **Оповещения в интерфейсе и отчёте не добавляем** — решение пользователя. Записано здесь, чтобы знать, как считается.

**Вопрос 16. ЗАКРЫТ 10.09.2026 без правки — клиент этого не просил. Перебор фосфора при подборе продуктов.** Программа даёт больше фосфора, чем нужно: «New test» 18.7 против 14 (+34 %), Canberra 23.9 против 19.9, Westview 21.4 против 20, Federal 13.2 против 11.9; Burns и Test1, наоборот, слегка недобирают. Причина установлена и замерена: продукт выбирают ради азота и калия, фосфор приезжает довеском вместе с составом, а проверка, которая должна это ловить, не срабатывает — оценщик считает продукт по минимальной норме этикетки, тогда как программа применяет полторы-две таких нормы (GH-337 отмасштабировал дозу, но не оценщик). Решение тоже найдено и замерено: ограничение фосфорного балла остатком годового бюджета даёт «New test» 14.4, Canberra 20.9, остальные четыре сайта не двигаются вовсе.

   **Почему не делаем.** Перебор **виден**: в документе рядом стоят и потребность, и поставка, клиент получает не спрятанную ошибку, а объяснимое расхождение — в отличие от тёплосезонной травы, которую чинили в GH-408. Правка при этом меняет **подбор продуктов** перед сдачей, на Canberra азот уезжает с −15.3 на +18.4, и она остаётся заплаткой: корень в том же оценщике, и при его пересмотре эти цифры сдвинутся ещё раз, то есть платить пришлось бы дважды. **Никто этого не просил.** Возвращаемся, если скажет клиент.

   Разбор и замеры не пропали: план в `PLAN-GH410-GH411-phosphorus-overshoot-and-status-ceiling.md`, измерительные тесты `tests/gh410-phosphorus-overshoot-probe.test.js` и `tests/gh410-phosphorus-lever-measurement.test.js`. Сюда же сложено смежное, тоже не берущееся в работу: двухмесячный продукт в последнем месяце программы высвобождает половину дозы за пределами года, и врущий комментарий у `soilPSufficient` (`au-fertiliser-products.js:4846`).

**Закрыты по ходу как нереальные сценарии:** проба, в которой заполнен только pH, даёт документ вообще без раздела требований — решено 10.09.2026 не разбирать, такой пробы в реальности не бывает; паразитная секция «климат недоступен» у сайта без координат (сайт без координат не работоспособен в принципе — нет ни GP, ни программы, ни рисков болезней), и тихая подмена непроштампованной программы (достижима только в том же состоянии). Оба разобраны и задокументированы с доказательствами на случай, если предпосылка изменится.

## Предложенный порядок сборки

**Пересобран 11.09.2026.** Прежний порядок отвечал на вопрос «что делать после объединения движков» и строился от зависимостей, которых больше нет: D31 закрыт, движок один, и пометки «ждало объединения движков» у пунктов 8 и 9 ниже больше ничего не значат — эти работы разблокированы.

**Главное, что изменилось в самой постановке.** Продукт идёт клиенту **на тестирование**, а не пользователям. Значит порядок ниже — предположение, и первое же письмо клиента его перепишет. Держать его надо как список работ, а не как график: то, что клиент назовёт, пойдёт вперёд всего остального.

**Три яруса, по убыванию обязательности:**

**Ярус 1 — до передачи клиенту. ЗАКРЫТ 12.09.2026.** Пробы больше не теряются и не воскресают, права на эндпоинте проверяются, единицы и подписи в документе не противоречат экрану. Подробности в пункте 1 ниже и в разделе про финальное ревью. Продукт можно отдавать.

**Ярус 2 — до релиза для пользователей.** Этапы 1–3 по пробам (там же, пункт 1) и Вопрос 2 — программа не пересчитывается ни при смене расчётных входов, ни при нашем деплое. Второй родствен первому: данные устаревают молча, и клиент об этом не узнаёт.

**Ярус 3 — после, в порядке, который назовёт клиент.** Всё, что ниже пункта 3. Оценка объёма осталась прежней и приведена в конце раздела.

Соблюдает собственные приоритеты клиента (раздел 12) с поправками, отмеченными там, где этот проход проверки изменил картину. **Обновлено 10.09.2026:** пункты про D30, D06, кластер D01 и объединение движков D31 удалены — они сделаны, см. таблицу «какой тикет какой дефект закрывает» в начале документа. Ниже — то, что действительно осталось, начиная с ближайшего.

1. **Потеря лабораторных проб — этап 0 закрыт (GH-430), плюс две правки из ревью (GH-431, GH-432). Этапы 1–3 остаются.** Это было единственное место во всём списке, где терялись данные, а не показывалась неверная цифра, и воспроизводилось обычным добавлением пробы. Направление решено: кэш удаляем, база единственный источник, офлайна не было и не будет.

   **Что закрыто.** **GH-430** — сервер перестал удалять пробу за то, что её не было в присланном снимке; замерено живьём: было `synced: 1, deleted: 6`, стало `deleted: 0`, и то же для правки пробы, импорта и Capture. **GH-431** — удалённая на странице Data проба больше не воскресает при следующей синхронизации; это нашло финальное ревью, потому что запись GH-430 исходила из того, что настоящего эндпоинта удаления ещё нет, а он уже был. **GH-432** — на `sync()` добавлена проверка роли: раньше это был единственный эндпоинт данных без неё, и viewer мог стереть сайт.

   **Что осталось.** **Этапы 1–3** — переход на поштучную запись и удаление браузерной копии. Делаются параллельно с тестированием у клиента и должны быть в релизе для пользователей. Восемь вопросов к ним в `PLAN-samples-sync-FINAL.md`; к этапу 0 не относился ни один, все они про 1–3.
2. **Из трёх вопросов по таблицам продуктов живым остался один.** (а) Раздел сверки калия не печатается на поле, где программа калий не даёт — Вопрос 5, **вынесен пользователем за релиз**: это не неверная цифра и не потеря данных, а полнота изложения, и решать её лучше, когда клиент увидит документ целиком. (б) и (в) — итог «доставлено» и объединение накопителей — **закрыты** в GH-391 и GH-399…GH-401.
3. **ЗАКРЫТО — доставка объединена целиком: GH-399 (общее ядро), GH-400 (фосфор у австралийских жидких), GH-401 (этапы 3–5).** Пять ручных накопителей заменены одной реализацией. Что реально сдвинулось в цифрах на живых сайтах: объёмы многократных жидких печатались за одно опрыскивание вместо всех — Long Paddock Rapid Uptake на «New test» `1 / 7 L/ha` → **`4 / 28 L/ha`**, Urea Tech на Federal Golf `2 / 30 kg/ha` → **`4 / 60 kg/ha`**; это та колонка, по которой клиент заказывает. Двойное округление убрано, счёт идёт в полной точности, округление одно — при выводе. В таблицу продуктов отчёта добавлена строка **Total Delivered** и подпись под таблицей, когда в таблице есть мелиоранты и строки поэтому не сходятся с итогом. Проверки: Jest 2183 / 7 пропущено, PHPUnit 120, E2E 96/96 на семи наборах, шесть красных проверок с наблюдаемым выводом.

   **Прогноз задания про Burns 126 → 125 не подтвердился, и это важно.** GH-399 уже перевёл план с `program.delivered` рекоммендера (125.49999999999999) на общий накопитель, а тот даёт ровно **125.5** — округляется вверх до 126, тогда как строки дают 113 + 7 + 5 = 125. Расхождение настоящее, осталось, и вынесено в GH-403. Побочно вылезло: калий на «New test» в двоичной арифметике даёт 104.49999999999999 и печатался бы как **104** при строках, дающих 105; поставлен допуск 1e-9, записанный в коде как явное правило. С ним **ни одна цифра на десяти dev-сайтах не сдвинулась** — артефакт убран, живые числа на месте.

   **ЗАКРЫТО GH-403 — «Required» стала одной величиной, и итог таблицы продуктов сошёлся со своими строками.** Оба хвоста были про одно: одно имя, две величины. **Предпосылка задания оказалась неверной, и это главная находка тикета.** Я написала, что причина расхождения одна — округление годовой потребности до целого килограмма перед раскладкой. Причин было **две**: вторая — все три региональные панели пересобирали Required, складывая двенадцать помесячных строк, каждая уже округлённая до 0.1, а такая сумма к годовой не сходится никогда. Починка только первой сделала бы местами хуже: Federal Golf по фосфору ушёл бы с 11.9 на 11.8 при 12.0 в отчёте, «New test» — на 14.3 при 14.2, то есть знак расхождения перевернулся бы. Исправлены обе половины; панели читают ту же годовую из общего ядра, что печатает отчёт. Живьём Required совпадает посимвольно на всех четырёх сайтах: New test P 14.2/14.2 и K 103.0/103.0, Federal Golf P 12.0/12.0 и K 64.0/64.0, Burns P 14.1/14.1, Test5 K 136.1/136.1.

   **Решение пользователя 10.09.2026 по второму хвосту: оставляем десятые ради сходимости.** Вариант «итог = сумма показанных строк» замерами исключён — округление вверх копится с числом строк, а истинный итог нет: у Test6 сумма целых строк по калию даёт **127** при истинных 125.4, и это стояло бы в одном документе с таблицей требований, печатающей 125.4. Поэтому строки получили десятую, и теперь на каждом сайте напечатанные строки складываются в напечатанный итог точно. Burns в настоящем `.docx`: 113.1 + 7.0 + 5.4 = **125.5** (было: итог 126 при строках 113 + 7 + 5). **Это отменяет GH-318, где целые числа в этой таблице попросил сам клиент** — тест GH-318 не удалён, а переписан на запись об отмене и замерах за ней. Отмена записана и в самой записи GH-318 в changelog, чтобы читатель не решил, что целые числа всё ещё в силе.

   **Одна ячейка вышла за поставленный в задании порог, и порог был неверным, а не фикс.** Я велела остановиться, если хоть один месяц сдвинется больше чем на шаг округления. Сдвинулась ровно одна на десяти сайтах: **Westview, сера, январь 3.5 → 3.3**. Разбор верный: округление до целого килограмма искажает годовую до 0.5 кг, у Westview по сере именно этот случай (13 против истинных 12.5), а январь несёт 27% программы, поэтому сырое значение сдвигается на 0.134 и пересекает две границы. Все прочие ячейки двигаются максимум на 0.1. **Потолок азота не тронут** — годовой азот на всех десяти сайтах целый, через убранное округление он не проходил; сравнение всех двенадцати значений, флага срабатывания и перераспределения до и после — идентично. Попутно закрыт хвост GH-399: британская панель читает общий накопитель, как остальные три (Test6 фосфор 27.9 → 27.8). Проверки: Jest 2327 / 8 пропущено, PHPUnit 120, E2E 104/104, семь красных проверок с наблюдаемым выводом.

   *Ниже — исходная формулировка, как она была найдена при сдаче GH-396.*

   **Доставленный фосфор считается по-разному в плане и в отчёте.** На SLAN-фикстуре план печатает P Delivered **14.0**, отчёт — **14.5**; один и тот же вердикт выходит как −7% и −5%. Причина в накопителе доставки: экспорт предпочитает вектор `delivers` из заявки продукта и падает на «анализ × норма», а план всегда считает «норма × число внесений × анализ». Азот и калий сходятся до последнего знака на всех трёх фикстурах, фосфор — нет на этой. **Это уже не подача, а расчёт**, поэтому в GH-396 сознательно не трогали: правка двигает цифры в Annual Product Summary и Purchasing Summary во всех трёх региональных интеграциях. Харнесс расхождение не прячет — он сверяет Current/Removal/Range точно, а Balance у каждой поверхности против её же строки. Чинить в одной общей точке накопления, как делали с остальными расхождениями.
   **Разобрано планировщиком 10.09.2026, полный план — в `PLAN-delivery-unification.md`.** Ручных накопителей доставки оказалось не два, а **пять**, и они расходятся между собой: NZ на Plan пересчитывает сам, AU и UK печатают объявленное рекоммендером, у комбинированного экспорта свой фолбэк без учёта числа обработок, плюс «доливка» фосфора из анализа в одиночном экспорте, из-за которой он противоречит сам себе — строки таблицы против колонки Delivered. Причина конкретно 14.0 против 14.5: два австралийских жидких объявляют `delivers.P: 0` при анализе 1.7% и 2%, экспорт считает объявленный ноль «нет данных» и подставляет свой расчёт, игнорируя четыре обработки. Права ни одна сторона — физически там 14.9.
   **Решения пользователя 10.09.2026:** ✅ **D-3** объёмы и число обработок для многократных жидких печатать как `норма × обработки` и считать каждое опрыскивание — клиент заказывает по этой колонке; ✅ **D-4** двойное округление свернуть в эту же работу (Burns 126 → 125); ✅ **D-5** добавить строку «Total Delivered» в таблицу продуктов отчёта. **Нумерация:** GH-399 (общий модуль доставки и перевод всех пяти мест на него), GH-400 (корневой фикс объявления фосфора у AU-жидких — отдельным номером, потому что он единственный меняет сам подбор продуктов и должен откатываться сам по себе), GH-401 (объёмы, округление, итоговая строка).
   ✅ **D-1 — РЕШЕНО 10.09.2026: считать фосфор, объявлять его в корне.** Объявлять ли фосфор у австралийских жидких. Комментария, объясняющего нынешний ноль, в коде нет; гранулированная ветка в том же файле фосфор считает, а функция подбора жидких его не вычисляет вовсе — её докблок объявляет контракт без P. То есть код ничего не решал, он просто не посчитал. **Но под этим лежит настоящий агрономический вопрос, который в проекте не задавали ни разу:** идёт ли внесённое по листу в зачёт почвенной потребности. Фолиарный фосфор усваивается листом, а не почвой, и запас в почве не пополняет — а требование измеряется от почвы. Довод не объясняет нынешний код (фолиарные азот и калий засчитываются полностью), но он настоящий, и ответ определит поведение сразу для трёх элементов. Стоит задать Спенсеру одним вопросом — но реализацию он не блокирует: сейчас код не отвечает на него ни для одного элемента, а после фикса будет отвечать одинаково для всех трёх, что уже согласованнее нынешнего состояния.
   **Чинить обе части, решение пользователя 10.09.2026.** Проверено по коду: подбор продуктов **не грубый** — в австралийском рекоммендере есть настоящая оценка по фосфору (`au-fertiliser-products.js:4976`), которая штрафует перебор по нарастающей: попадание в 0.7–1.3 от потребности даёт 100 баллов, двукратный перебор 30, трёхкратный 10, свыше — минус 20. Но её кормят неверным числом: потребность считается как цель минус доставленное, а в накопитель доставленного фосфор из жидких не попадает никогда. Уже внесённый фосфор невидим, потребность завышена, и продукт, который переливает, получает оценку «идеальная доставка». Тот же слепой участок в блоке добора: `остаток = цель − доставлено`. **Поэтому править надо и объявление в `delivers`, и накопитель, питающий расчёт потребности.** Если поправить только объявление, отчёт станет честным, а подбор продолжит считать по завышенной потребности. Хорошая новость: правила подбора переделывать, скорее всего, не придётся — это снова верная логика на неверном входе.
   **Два хвоста после GH-400, решено пользователем 10.09.2026 — чинить после GH-401, каждый своим тикетом.**
   **(1) ЗАКРЫТО 10.09.2026 без правки, решение пользователя: перебор фосфора не чиним, пока не попросит клиент.** Причина найдена и замерена, решение найдено и замерено — см. вопрос 16, там же ссылки на план и измерительные тесты. Коротко: перебор виден в документе, а не спрятан; правка меняет подбор продуктов перед сдачей и остаётся заплаткой поверх известного корня, то есть платить пришлось бы дважды. Заодно снята записанная ранее версия про «месячное против годового» — она проверена и неверна.
   **(2) ЗАКРЫТО 10.09.2026 без правки — так и задумано. Статус «On Track» не имеет верхней границы.** Прежняя формулировка была моей ошибкой: я написала, что после GH-400 фосфор на «New test» печатается «On Track» при 18.7 против требуемых 14. **Проверено при планировании GH-411 — неверно.** У фосфора есть диапазон достаточности, он судится другой веткой классификатора, и там верхняя граница уже есть: выше потолка печатается `Excess (+X%)` красным. Фосфор показывается корректно.

   Ветка без верхней границы (`nutrient-balance-status.js:106-129`) — это **ветка азота, на всех сайтах**, потому что у азота почвенного диапазона нет. Считается процент выполнения: ≥90 % «On Track», ≥70 % `Monitor (−X%)`, ниже `Deficit (−X%)`. Сверху ничего. Замерено: азот печатается как «On Track» при 115 % на «New test», 112 % на Westview, 113 % на Federal Golf, 123 % на Test1.

   **Решение пользователя: для азота верхнюю границу не вводим — так было задумано.** Предложение планировщика (110–130 `Monitor (+X%)`, свыше 130 `Excess (+X%)`) отклонено. Фосфор и калий по этой ветке не проходят, значит менять нечего, и тикет GH-411 не заводится.

   *Побочная находка, в работу не берётся:* британская интеграция (`nutrition-uk-fertiliser-integration.js:1066-1072`) считает статус своим кодом, мимо общего классификатора. **Решение пользователя 10.09.2026: по британской интеграции сейчас ничего не делаем и не обсуждаем** — вероятно, расчёты будут объединены позже, и тогда это отпадёт само. Записано только как след, чтобы не искать заново.
   **Следствие, которое надо посмотреть при реализации, а не принять вслепую:** после фикса New test передаёт фосфора на 4.7 кг сверх цели. Планировщик специально это выделил: либо рекоммендер честно говорит, что столько и выходит из выбранных продуктов, либо это второй дефект — уже в его подборе под фосфор. Разбираться с этим в GH-400, увидев живые цифры, а не заранее.
   ✅ **D-2 — РЕШЕНО 10.09.2026 вместе с D-1: доверяем объявленному рекоммендером.** Решение по D-1 осмысленно только при этом правиле — объявление чинят именно потому, что ему доверяют. Что означает «доставлено» в одной строке. Медленное высвобождение показывает один мешок в каждом из покрытых месяцев, объявляя долю за месяц; сложение физического содержимого всех строк даёт тройной счёт — Test6 - UK 294.5 против настоящих 199.8. Планировщик рекомендует доверять объявленному. Важен порядок: D-2 задаёт правило, D-1 чинит ложное объявление; принять только D-2 значит закрепить «верь объявленному» поверх объявления, которое врёт.

4. **Добавление схемы D33 — сдвинуто вперёд, согласно собственной инструкции клиента "урегулировать это рано независимо от позиции" и собственной находке этого плана, что D41 и D44 оба это потребляют.** Добавить поля `slowReleaseFraction`, `density`, `sourceDoc` по всем файлам каталогов продуктов как самостоятельное изменение схемы, отделённое от заполнения значений каждого продукта. ~1-2 дня на схему + заполнение конкретно Ammos (нужно, чтобы тест D41 прошёл); остальные продукты могут заполняться постепенно, ничего не блокируя.
5. **D41 → раскрытие D09 — конвертация плотности, обе поверхности, затем её раскрытие.** Теперь, когда D33 предоставляет схему, добавить конвертацию плотности в единую общую точку, где происходит математика L→kg (проверить и код выбора продукта UI, и подтвердить, что отдельного шага конвертации на стороне экспорта не нужно, поскольку проверка нашла, что ни то, ни другое сейчас не конвертирует). Это изменение значения, которое клиент должен явно одобрить (цифры Purchasing Summary меняются). ~2-3 дня, включая аудит файлов интеграции AU/UK/NZ.
6. **D40 — фикс окна переноса**, управляемый балансом массы согласно рекомендованному варианту, плюс страховочная сетка громкого отказа для непокрытого месяца. Должен приземлиться после D41, поскольку собственный рабочий пример зависит от скорректированных цифр доставленного N D41. ~2-3 дня.
7. **Пакет рендереров Tier A — остальное, примерно в порядке, заявленном клиентом, сгруппированное по общему файлу/механизму там, где этот проход проверки нашёл естественные группировки:**
   - D44 (сужение S) — оформить вместе с D05 и D31 ниже, поскольку все три трогают одни и те же циклы нутриентов; делать этот пакет из трёх вместе, а не в строгом порядке приоритета. ~1 день на один D44, как только последовательность правильная.
   - D42, D43, D34 (фиксы единиц/меток пайплайна продуктов Prebbles) — тот же файл (`prebbles-products.js`/`nutrition-prebble-integration.js`), делать вместе. ~2 дня.
   - D08 (фикс meq Ca:Mg) — тривиально, одна строка плюс одна функция, делать в любое время, без зависимости. Полдня.
   - D10, D18, D14, D26, D23 — независимые фиксы Tier A, без общей зависимости, можно распараллелить между командой или делать в любом порядке. ~3-4 дня всего.
   - D13a, D13c, D21, D19, D20 — кластер реестра цитат; сделать перепрокладку реестра D21 первой (добавляет запись SLAN, подключает три рендерера читать из неё), затем D13a (добавляет запись Spencer, переиспользует подключение), затем D19/D20 (фильтровать глоссарий/References, используя теперь заполненный, теперь подключенный реестр). ~3-4 дня как один последовательный подпакет.
   - D16, D17, D25 (строка Na + решение по Base Saturation), D28, D29, D46, D47, D22, D36, D35 — независимые фиксы рендереров, без общей зависимости, помимо того, что D47 разделяет фикс композита D24 (см. ниже), а D25 нуждается в ответе на Решение 2 до своей половины по Base Saturation (её половина со строкой Na может выпускаться немедленно, разблокирована). ~1 неделя всего, распределяемо между участниками.
8. **D05 → D44 — расширение сверки. Разблокировано:** объединение движков закрыто, ждать больше нечего. Ждало его, чтобы циклы нутриентов пришлось тронуть один раз, а не дважды по двум движкам. Теперь разблокировано. ~2-3 дня.
9. **D13b / D24 / D48 — общий контракт модели вердикта. Разблокировано**, согласно собственной группировке клиента.** Построить контракт четвёртого состояния вердикта один раз (Решение 5), затем: gate по порогу GP D24 первым (разблокирован, агрономический параметр уже установлен, наивысшая серьёзность из трёх) с согласованностью D46/D47/карточки Wear Resistance в том же изменении; таблица вид-по-высоте D48, как только будут отвечены Решения 6/7; тегирование качества диапазона ткани D13b может приземлиться независимо от любого из них, тот же контракт. Это второй по величине оставшийся кусок инженерной работы — рекомендуется 1.5-2 недели на весь кластер, с основным gate D24, поставляемым в первые несколько дней этого окна, поскольку он независимо ценен и наименее заблокирован.
10. **D32 — gate PGR + раскрытие перерасхода.** Небольшой, переиспользует константу порога GP D24 — приземлить сразу после того, как gate D24 существует, а не как отдельный слот планирования. ~1 день.
11. **Общее правило D03 — оставшиеся места тихого фолбэка** (климат Event Planner, паттерн-фолбэк прогноза болезней, устаревший путь питания). Независимо от всего остального в этом списке; может выполняться параллельно с любым из вышеперечисленного, как только позволят ресурсы, поскольку не разделяет файлы с другими кластерами. ~2-3 дня по трём местам, каждое независимо тестируемо.
12. **D27 — только перемерить. Разблокировано, и это задача проверки, а не сборки.** Ждало D01 и D31, оба приземлились, так что мерить можно. Отдельного слота сборки нет; задача проверки, а не сборки.

13. **Режим «Front-loaded» не сохраняет годовую сумму — в самый конец, решено пользователем 10.09.2026.** Отложен сознательно, а не забыт: по базе **все десять сайтов сидят на `gp_weighted`**, режим `front_loaded` не выбран ни у одного, так что дефект достижим только если пользователь сам переключит распределение на странице Plan. Суть: `distributeByGP()` в этой ветке (`nutrition-calendar.js:1379-1389`) раздаёт каждому активному месяцу `total/10`, затем умножает месяцы 8, 9, 10 на 1.5, и не нормализует к цели вообще. Замерено независимо дважды: при годовом N 250 южный температурный ряд даёт по месяцам 312.5, северный — 200, а `annual_totals` в обоих случаях печатает 250, поэтому с экрана расхождение не видно. Бьёт по всем элементам сразу. После GH-394 складывается с трафиком: спортивное поле на этом режиме получит около ×1.55 от базы. Код лежит **с первого коммита** и никем не менялся; собственный комментарий автора признаёт незавершённость — «60% in spring (months 8-10 south, 2-4 north)», сразу за ним «Simplified: weight first half more», и северная половина так и осталась в комментарии. Когда дойдут руки, чинить две вещи: нормализацию к заявленной цели (это дефект, решений не требует) и месяцы буста для северного полушария (это уже вопрос конвенции — сегодня подпись «spring emphasis» в северном полушарии попадает на осень).

Общая грубая оценка масштаба на сегодня: пакет рендереров Tier A — порядка 3 недель ограниченной, в основном независимой работы; контракт модели вердикта D13b/D24/D48 — единственный оставшийся по-настоящему инженерный кусок, 1.5-2 недели; добавление схемы D33 стоит сделать рано, поскольку из него потребляют D41 и D44. Ничто в этом списке не заблокировано ничем вне его — никакое ограничение WordPress-плагина не применяется, что согласуется с собственным переосмыслением v6 клиента. Единственное, что стоит перед всем перечисленным по серьёзности, а не по последовательности — удаление образцов при синхронизации (пункт 2).
